Runs Cypher, inspects the schema / data model via `neo4j-cli query :schema`, discovers and loads example datasets from GitHub repos into Docker, Neo4j Desktop, or new Aura instances, manages Neo4j Aura, Neo4j connection (dbms) and embedding-provider credentials, manages local Neo4j Docker containers (create / list / get / start / stop / delete, incl. ephemeral runs emitting a .env for `query --env`), and installs the self-skill or any catalog skill into AI agents. Use to execute or pipe Cypher, introspect the schema or generate a data model via `:schema`, embed text inline as a parameter, list example datasets (`dataset list`) or load one into a target (`docker load`, `desktop dbms load`, `aura instance load`), list/create/get/delete/resize Aura instances or tenants, manage Aura Agents, manage Aura/dbms/embed credentials, run a local Docker container, install/remove/list/refresh agent skills, or self-update via `neo4j-cli update`. Skip for Cypher syntax, drivers, Kubernetes, Browser, or other databases.
