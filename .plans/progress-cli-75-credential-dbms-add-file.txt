## task-001 — 2026-05-13T07:50:00Z
Removed MarkFlagRequired for --name/--username/--password/--uri on `credential dbms add`; required validation now in RunE returns clierr.NewUsageError citing matching --file env key (AURA_INSTANCENAME, NEO4J_USERNAME, NEO4J_PASSWORD, NEO4J_URI). Updated 4 missing-flag test cases to match new shape.
Files: ~neo4j-cli/internal/subcommands/credential/dbms/add.go, ~neo4j-cli/internal/subcommands/credential/dbms/add_test.go

## task-002 — 2026-05-13T08:15:00Z
Added --file flag to `credential dbms add` parsing Aura-exported credentials (.env) files via gotenv. Merge order file→flag→default with per-key presence tracking so `KEY=` (empty) is distinguishable from missing. 12 new table-driven test cases cover happy path, --name/--password overrides, NEO4J_DATABASE from file, comments/blank-lines, empty file, NEO4J_URI=/NEO4J_DATABASE= empty cases, missing path, embed-credential happy + missing. Helper extended with `files map[string]string` seeded into in-memory FS. Bundle references/credential.md regenerated.
Files: ~neo4j-cli/internal/subcommands/credential/dbms/add.go, ~neo4j-cli/internal/subcommands/credential/dbms/add_test.go, ~neo4j-cli/internal/skill/bundle/references/credential.md
Decisions: regenerated bundle inside task-002 (not task-003) so `make test` round-trip stays green; forward-slash literal paths in command strings dodge shlex backslash mangling on Windows.

## task-003 — 2026-05-13T08:20:00Z
No-op: skill bundle was regenerated as part of task-002 to keep `make test` round-trip green. Verified `go generate ./neo4j-cli/internal/skill/...` produces no diff and `references/credential.md` already documents `--file`. Marked completed.
Files: (none)
