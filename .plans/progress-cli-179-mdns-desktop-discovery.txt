## TASK-001 — 2026-06-01T09:05:00Z
Added github.com/hashicorp/mdns (MIT) and scaffolded discovery_mdns.go isolating all mDNS lib imports + the macOS dns-sd exec, with browseDesktopMDNS / lookupDesktopDNSSD seam fns (Set*ForTest restorers) returning (port, ok).
Files: +neo4j-cli/internal/desktopclient/discovery_mdns.go, ~go.mod, ~go.sum
Decisions: silenced hashicorp/mdns's log.Default() output via an io.Discard logger so socket-bind warnings don't leak to CLI stderr; mdns.QueryContext honors the caller-supplied params.Entries (the reassignment is only in Lookup, not QueryContext).

## TASK-002 — 2026-06-01T08:41:41Z
Added Discover orchestrator + DiscoverViaMDNS / discoverPinned / mdnsOrigin to discovery.go and wired the NEO4J_CLI_DESKTOP_E2E_MDNS_PORT seam. mDNS/dns-sd paths yield 127.0.0.1 origins (auth-coupled), legacy port scan keeps localhost; ErrNoDesktop only after the tier-3 scan misses.
Files: ~neo4j-cli/internal/desktopclient/discovery.go, ~neo4j-cli/internal/desktopclient/seams_e2e.go
Decisions: Discover's e2ePortOverride branch reuses origin() (localhost) to mirror ProbePort exactly; discoverPinned only adopts the 127.0.0.1 origin when an mDNS/dns-sd responder confirms the exact pinned port.

## TASK-003 — 2026-06-01T10:05:00Z
Added hermetic seam-driven tests for Discover/DiscoverViaMDNS/discoverPinned covering mDNS hit, dns-sd hit (darwin), non-darwin dns-sd skip, port-scan fallback, all-miss ErrNoDesktop, pinned confirm (127.0.0.1), pinned silent (localhost), pinned different-port, and cancelled-ctx prompt return. All via SetMDNS/DNSSD/GOOS/HTTPClient seams + the existing pinProbeTo roundtripper helper; no real multicast/dns-sd/network.
Files: +discovery_mdns_test.go
Decisions: cancelled-ctx test stubs mDNS/dns-sd seams to honour ctx.Err() so Discover falls through to the port scan, whose ctx.Err() check returns the cancellation; asserts return within 2s via a goroutine+timeout instead of relying on real timeouts.

## TASK-004 — 2026-06-01T11:05:00Z
Migrated all five ProbePort callers to Discover (passing the same pinned --port value; 0 in query). ErrNoDesktop branching unchanged at every site. Remaining production ProbePort callers are only Discover/discoverPinned (intended fallback) and doctor (task-006 scope).
Files: ~query/desktop.go, ~.../desktop/helpers.go, ~.../dbms/helpers.go, ~.../connection/create.go, ~.../dbms/plugin/helpers.go (+ query/desktop_test.go)
Decisions: the two _RealClient tests in query/desktop_test.go are the only call-site tests hitting the real Discover(ctx,0) path; stubbed SetMDNSBrowseFnForTest/SetDNSSDLookupFnForTest to (0,false) so they fall through to the httptest-backed port scan and stay hermetic (no real multicast / dns-sd on darwin CI). All subcommand tests override newDesktopClientFn so they never reach Discover.
