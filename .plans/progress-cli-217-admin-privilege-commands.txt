## task-001 — 2026-06-22T00:00:00Z
Scaffolded the privilege package mirroring role/: NewCmd seam (no leaves yet), privilegeExecFn/privilegeFields/outputPrivileges helpers (using adminutil.NewRows), and test helpers (testConn, withFakePrivilegeExecFn, withSequencedPrivilegeExecFn). All four gates pass.
Files: +privilege/privilege.go, +privilege/helpers.go, +privilege/privilege_helpers_test.go
Decisions: Foundation symbols are unused until leaf tasks land, so each carries a scoped //nolint:unused with a reason; these drop out as task-003+ consume them.

## task-002 — 2026-06-22T00:00:00Z
Added buildPrivilegeCypher + privilegeOpts, actionCategory constants, validActions map (FIND absent), normalizeAction, and propertyClause/entityClause helpers; full direct unit tests cover one happy path per category plus all REQ-F-011 error paths with pinned-message assertions. All four gates pass.
Files: ~privilege/helpers.go, ~privilege/privilege_helpers_test.go
Decisions: buildPrivilegeCypher returns the Cypher fragment WITHOUT the trailing TO/FROM $role target (leaf commands append it) and a non-nil empty params map — matches the task's acceptance-criteria examples (e.g. "GRANT READ {*} ON GRAPH * ELEMENTS *").

## task-003 — 2026-06-22T00:00:00Z
Implemented the `admin privilege list` leaf: SHOW PRIVILEGES by default, --role/--user switch to SHOW ROLE/USER $name PRIVILEGES, both-flags usage error before SilenceUsage and any seam call, output via adminutil.NewRows + PrintBodyMap. Wired newListCmd into privilege.go and dropped the now-consumed nolint:unused on privilegeExecFn/privilegeFields. All four gates pass.
Files: +list.go, +list_test.go, ~privilege.go, ~helpers.go
Decisions: list_test.go uses a local capturing exec fake (records cypher/params) rather than the shared withFakePrivilegeExecFn, since the acceptance criteria assert on the exact statement and params["name"].

## task-004 — 2026-06-22T00:00:00Z
Implemented the `admin privilege grant` leaf: required --action/--role pre-validated as clierr.NewUsageError before SilenceUsage, delegates to buildPrivilegeCypher("GRANT", ...), sends `<cypher> TO $role` then outputPrivileges (SHOW ROLE $name PRIVILEGES). Wired newGrantCmd into privilege.go and dropped now-consumed nolint:unused comments on buildPrivilegeCypher/validActions/privilegeOpts/normalizeAction/outputPrivileges/clause helpers and testConn. All four gates pass.
Files: +grant.go, +grant_test.go, ~privilege.go, ~helpers.go, ~privilege_helpers_test.go
Decisions: Role target is parameterised (`TO $role`) rather than inlined, mirroring role/grant.go; grant_test uses a local recording sequenced exec fake (captures cypher/params) since acceptance criteria assert exact statements/params.

## task-005 — 2026-06-22T00:00:00Z
Implemented the `admin privilege deny` leaf as a structural mirror of grant: required --action/--role pre-validated, delegates to buildPrivilegeCypher("DENY", ...), sends `<cypher> TO $role` then outputPrivileges. Wired newDenyCmd into privilege.go. Tests reuse the shared sequencedCall/withRecordingSequencedExecFn/twoOK helpers from grant_test.go (same package) and cover propertyBearer, dbms, required-flag/flag-conflict errors, and mutation-error-skips-follow-up. All four gates pass.
Files: +deny.go, +deny_test.go, ~privilege.go

## task-006 — 2026-06-22T00:00:00Z
Implemented the `admin privilege revoke` leaf mirroring grant/deny: required --action/--role pre-validated, optional --revoke-type (grant|deny) resolves the verb via revokeVerb (plain REVOKE / REVOKE GRANT / REVOKE DENY), delegates to buildPrivilegeCypher, sends `<cypher> FROM $role` then outputPrivileges. Wired newRevokeCmd into privilege.go. All four gates pass.
Files: +revoke.go, +revoke_test.go, ~privilege.go
Decisions: --revoke-type normalises with ToLower+TrimSpace (consistent with --action leniency); invalid values are a usage error before any seam call.

## task-007 — 2026-06-22T13:40:00Z
Wired privilege.NewCmd into admin.go (import + AddCommand after role), updated Short to "Manage Neo4j databases, users, roles, and privileges", appended `privilege` (list, grant, deny, revoke) to Long, and refreshed the package/NewCmd doc comments. Regenerated the skill bundle and added a Minor changelog entry. All four gates plus build/--help check pass; reviewer LGTM.
Files: ~admin.go, +.changes/unreleased/neo4j-cli-Minor-20260622-133720.yaml, ~skill/bundle/SKILL.md, ~skill/bundle/references/admin.md

## REVIEW — 2026-06-22T00:00:00Z
Added 3 review tasks (task-008..task-010).

## task-008 — 2026-06-22T14:10:00Z
Deleted the two unused test helpers (withFakePrivilegeExecFn, withSequencedPrivilegeExecFn) plus their //nolint:unused pragmas and stale comment from privilege_helpers_test.go, and trimmed the now-unused context/clicfg imports. Leaf tests already use withRecordingSequencedExecFn from grant_test.go. All four gates pass; reviewer LGTM.
Files: ~privilege_helpers_test.go

## task-009 — 2026-06-22T14:40:00Z
Extracted addPrivilegeFlags + runPrivilegeMutation into helpers.go and collapsed the grant/deny/revoke RunE bodies and eight flag registrations to call them; each leaf keeps its own constructor and Short/Long/Example. Emitted Cypher, required-flag validation, and exec sequence unchanged; all existing tests pass. All four gates plus no bundle drift; reviewer LGTM.
Files: ~helpers.go, ~grant.go, ~deny.go, ~revoke.go
Decisions: verbWord parameterises the flag usage strings (rolePreposition yields to/from); revoke resolves revokeVerb before runPrivilegeMutation per the task spec (a benign reorder of two usage-error messages in an unreachable-in-practice edge).

## task-010 — 2026-06-22T15:10:00Z
Collapsed the identical setLabel/removeLabel action-category constants into a single labelScoped category; validActions routes both SET LABEL and REMOVE LABEL to it and the switch arm became `case labelScoped:`. Emitted Cypher unchanged (uses the normalized keyword), both keywords still accepted; all four gates pass and no bundle drift. Reviewer LGTM.
Files: ~helpers.go

## REVIEW — 2026-06-22T16:00:00Z
Added 1 review task (task-011) from CI security-review (Cypher injection in buildPrivilegeCypher).

## task-011 — 2026-06-22T16:40:00Z
Added cypherIdentifier helper (returns `*` unchanged; otherwise backtick-wraps with internal backticks doubled) and applied it via escapeIdentifiers to onGraph, onDatabase, and every nodeLabels/relTypes/properties element at all buildPrivilegeCypher inlining sites, closing the `--`-comment injection that could redirect the trailing $role. Updated happy-path/leaf-test expectations to backtick-quoted output and added injection + backtick-doubling unit tests. All four gates pass and no bundle drift; reviewer LGTM.
Files: ~helpers.go, ~privilege_helpers_test.go, ~grant_test.go

## REVIEW — 2026-06-22T16:30:00Z
Added 1 review task (task-012) from CI conventions-review (shared test helpers misplaced in grant_test.go).

## task-012 — 2026-06-22T17:00:00Z
Relocated the cross-leaf shared test helpers (sequencedCall, withRecordingSequencedExecFn, twoOK) verbatim from grant_test.go into privilege_helpers_test.go, satisfying AGENTS.md's resource-level shared-helper rule; grant_test.go keeps only its grant-specific runGrant helper and tests. Moved context/clicfg imports accordingly. All four gates pass and no bundle drift; reviewer LGTM.
Files: ~grant_test.go, ~privilege_helpers_test.go

## task-013 — 2026-06-23T00:00:00Z
Added a graphWhole action category for WRITE / ALL GRAPH PRIVILEGES that emits `<ACTION> ON GRAPH <name>` with no entity/property clause (the prior graphOnly mapping produced server-invalid `ELEMENTS *`); TRAVERSE/CREATE/DELETE/LOAD stay graphOnly. graphWhole rejects --node-label/--relationship-type/--property and --on-database/--on-dbms; replaced the bug-encoding test assertions. All four gates pass and no bundle drift; reviewer LGTM.
Files: ~helpers.go, ~privilege_helpers_test.go, ~deny_test.go
Decisions: The combined-qualifier guard for graphWhole fires before the generic --property guard so the message lists all three rejected qualifiers; license-check run via the gate set.

## task-014 — 2026-06-23T00:00:00Z
Re-categorised LOAD into a new `load` actionCategory emitting `LOAD ON ALL DATA` (default) or `LOAD ON CIDR "<cidr>"` (via new --cidr flag); load rejects all scope flags and qualifiers, and --cidr on any non-LOAD action is a usage error. All four gates pass and no bundle drift; reviewer LGTM.
Files: ~helpers.go, ~privilege_helpers_test.go, ~skill/bundle/references/admin.md
Decisions: CIDR is rendered as a double-quoted Cypher string literal via new cypherStringLiteral (backslash escaped before quote) — NOT backtick-quoted, since ON CIDR takes a string literal not an identifier. The --property guard for load is caught by the pre-switch generic property guard (different message) before the load arm's node-label/rel-type guard.

## task-015 — 2026-06-23T00:00:00Z
Changed the labelScoped arm of buildPrivilegeCypher to emit all --node-label values comma-joined and backtick-escaped via escapeIdentifiers (was inlining only nodeLabels[0]); added a multi-label unit case. Single-label output unchanged; guards/mutual-exclusion untouched. All four gates pass and no bundle drift; reviewer LGTM.
Files: ~helpers.go, ~privilege_helpers_test.go

## task-016 — 2026-06-23T00:00:00Z
Added a server-validated note atop the buildPrivilegeCypher happy-path test table (all 7 categories already had REQ-NF-005-confirmed cases), and extended grant/deny/revoke Long strings to mention --cidr (LOAD-only) and the no-qualifier rule for WRITE / ALL GRAPH PRIVILEGES; regenerated the skill bundle.
Files: ~4 (privilege_helpers_test.go, grant.go, deny.go, revoke.go), ~1 generated (bundle/references/admin.md)
Decisions: No code/grouping comments still mis-classified WRITE/LOAD/ALL GRAPH PRIVILEGES as graphOnly (tasks 13/14 already corrected those), so requirement 2 needed no edit.

## TASK-017 — 2026-06-24T00:00:00Z
Added the categoryMeta single-source table (name/flags/required/help/example per category) + categoryOrder slice, flag-name consts, actionsForCategory/kebabAction/kebabActionsForCategory/renderCategoryExample helpers, and extended normalizeAction to accept `-` as a separator (kebab positional support). Pure additions; existing grant/deny/revoke behaviour unchanged. New symbols kept live by unit tests (no //nolint). All four gates pass; no bundle drift. Reviewer LGTM.
Files: ~privilege/helpers.go, ~privilege/privilege_helpers_test.go
Decisions: category action lists are derived from validActions (not duplicated in categoryMeta); flag long names are shared consts so the per-category flag sets stay in sync with what buildPrivilegeCypher reads.

## TASK-018 — 2026-06-24T13:30:00Z
Added the newCategoryCmd factory (Use "<name> <action>", ValidArgs = kebab actions, ExactArgs(1), Short/Long/Example from categoryMeta, only the category's flags + --role, --revoke-type for REVOKE, write annotation); RunE resolves the positional via normalizeAction and either names the correct category command on a cross-category action or calls runPrivilegeMutation. Refactored addPrivilegeFlags to compose a new per-flag registerPrivilegeFlag; factory NOT wired into grant/deny/revoke yet (task-019). New category_test.go covers structure/flags/cypher-parity/kebab/revoke-verb/cross-category/unknown-action/missing-role. All four gates pass, no bundle drift.
Files: ~privilege/helpers.go, +privilege/category_test.go
Decisions: Args is ExactArgs(1) (not OnlyValidArgs) so a cross-category action reaches RunE for the "use 'admin privilege grant database access'" hint (REQ-F-030) instead of cobra's generic invalid-arg error. Short kept terse (full action list lives in ValidArgs/Long) to keep the parent help readable.

## TASK-019 — 2026-06-24T14:30:00Z
Converted grant/deny/revoke into RunE-less parents that loop categoryOrder registering the seven per-category subcommands via newCategoryCmd; removed --action and the orphaned addPrivilegeFlags helper; migrated the leaf tests to the `<category> <action>` form and regenerated the skill bundle. All four gates pass; reviewer LGTM after removing the now-unreachable `action == ""` check and stale addPrivilegeFlags comments.
Files: ~grant.go, ~deny.go, ~revoke.go, ~helpers.go, ~grant_test.go, ~deny_test.go, ~revoke_test.go, ~bundle/references/admin.md
Decisions: Dropped the three MissingAction leaf tests (action is now a positional, no longer a clierr usage error); re-pointed the FlagConflict tests to an in-surface conflict (--node-label + --relationship-type) since --action-on-graphOnly is no longer expressible. Deeper structure/parity tests deferred to task-020.

## TASK-020 — 2026-06-24T15:30:00Z
Added a per-verb structure test (7-category registration, exact per-category flag set, ValidArgs==kebab actions with union==validActions, --revoke-type only on revoke, flush-left Example per leaf) and a single-table per-category Cypher-parity test plus a per-category cross-category-positional error test; removed the dead categoryInfo.requiredFlags field (buildPrivilegeCypher already enforces those requirements). All gates pass; no bundle drift.
Files: +privilege/structure_test.go, ~privilege/category_test.go, ~privilege/helpers.go
Decisions: Removed requiredFlags rather than wiring it (would be a second unenforced source of truth); deleted the now-subsumed TestNewCategoryCmd_CrossCategoryPositional_NamesCorrectCommand per reviewer; kept the parity table (only single-table per-subcommand REQ-NF-005 check) despite partial overlap with grant_test.go.

## TASK-021 — 2026-06-24T16:30:00Z
Closeout for the per-category redesign: confirmed no bundle/agent-context drift (tasks 018-020 already migrated source+bundle), verified agent-context lists all 21 category subcommands and that `__complete` tab-completion lists the 7 categories and each category's positional actions; rewrote the existing unreleased changelog body to the per-category form and added the AGENTS.md one-file-per-leaf documented exception. README has no privilege content; only --action refs remaining are in .plans/ (historical) and a negative test assertion. All gates green.
Files: ~AGENTS.md, ~.changes/unreleased/neo4j-cli-Minor-20260622-133720.yaml, ~.plans/tasks-cli-217-admin-privilege-commands.yml

## TASK-022 — 2026-06-24T18:00:00Z
Added actionSummary(cat) helper (reuses kebabActionsForCategory; <=6 -> full list, else first-three preview + count + "see --help" with U+2026 ellipsis and em dash) and spliced it into the Short built by newCategoryCmd; added unit tests for the helper and the wired Short; regenerated the skill bundle.
Files: ~2 (privilege/helpers.go, privilege/structure_test.go), ~1 generated (bundle/references/admin.md)
Decisions: kebabActionsForCategory returns SORTED order, so Short lists are sorted (e.g. "(match, merge, read, set-property)") not the PRD's illustrative reading order — chosen to honor REQ-F-037 (no second ordering source) over the PRD's illustrative examples; tests assert the actual sorted output.

## TASK-023 — 2026-06-24T12:55:31Z
Dropped the redundant --on-dbms flag: removed flagOnDbms const, the registerPrivilegeFlag case, privilegeOpts.onDbms, and emptied categoryMeta[dbms].flags/exampleFlags + reworded its longRule; the dbms arm of buildPrivilegeCypher now emits "<ACTION> ON DBMS" unconditionally. Deleted the now-impossible opts.onDbms cross-scope guards (scope mutual-exclusion collapsed to hasGraph && hasDatabase) and updated tests + skill bundle. All gates pass.
Files: ~mod (privilege/helpers.go, grant.go, deny.go, grant_test.go, deny_test.go, category_test.go, privilege_helpers_test.go), ~gen (bundle/references/admin.md), -del (TestGrant_DbmsMissingOnDbms_ReturnsUsageError)
Decisions: dbms.flags set to nil (not empty slice) — wantFlagSet/registration treat nil the same; the load/mutual-exclusion guard messages were reworded since --on-dbms no longer exists.

## TASK-024 — 2026-06-24T15:03:19Z
Replaced the global Cypher-form unknown-action enumeration with a category-aware resolver (resolveCategoryAction) in newCategoryCmd.RunE: in-category returns canonical, cross-category keeps the kebab hint, unknown yields a category-scoped kebab "unknown <category> action" list. Deleted sortedActions(); normalizeAction now reuses a new canonicalAction helper and carries no enumeration (internal-only).
Files: ~privilege/helpers.go, ~privilege/category_test.go
