## TASK-001 — 2026-05-25T09:00:00Z
Built common/confirm package: Flags{Yes,Force}, Register, (*Flags).Require, ErrCancelled, stdinIsTerminal seam, SetStdinIsTerminalForTest. Table-driven tests cover all TTY/flag-state combos; resourceID="" degrades to "this <type>".
Files: +common/confirm/confirm.go, +common/confirm/confirm_test.go

## TASK-002 — 2026-05-25T09:30:00Z
Wired confirm gate into all 9 Aura destructive leaves: instance, deployment, agent, customer-managed-key, data-api graphql, graphanalytics session, auth-provider, deployment token, cors-policy allowed-origin. Extended Long/Example per REQ-F-030/F-031; added SetStdin to AuraTestHelper; added 4 gating tests per leaf. Bundle regen deferred to task-005.
Files: ~neo4j-cli/aura/internal/subcommands/{instance,deployment,agent,customermanagedkey,dataapi/graphql,dataapi/graphql/authprovider,dataapi/graphql/corspolicy/allowedorigin,deployment/token,graphanalytics/session}/{delete.go,delete_test.go,remove.go,remove_test.go}, ~neo4j-cli/aura/internal/test/testutils/auratesthelper.go
Decisions: gate placed after pre-flight ownership validation but before destructive HTTP call so org/project-missing tests need no flag update; AuraTestHelper.SetStdin chosen over per-test cobra tree rebuild for ergonomics.

## TASK-003 — 2026-05-25T10:00:00Z
Migrated docker/delete, desktop/dbms/delete, desktop/connection/delete to shared confirm helper. Removed local force/yes vars, stdinIsTerminal/deleteStdinIsTTYFn seams, and inline promptForDelete func; both --yes and --force now bound on each leaf. Cancelled TTY prompt writes "cancelled." to stderr and exits 0 via errors.Is(err, confirm.ErrCancelled). Deliberate back-compat break: non-TTY single-flag invocations now exit 2 ("pass both --yes and --force"). Extended Long per REQ-F-030 and Example per REQ-F-031 on all three. Test files rewritten: pre-confirm GET/list lookups for prompt naming are gone (confirm uses the positional id); prompt asserts moved from stdout to stderr; added explicit OnlyYes/OnlyForce/NeitherFlag/BothFlags non-TTY tests plus TTY y/N tests. Bundle regen deferred to task-005.
Files: ~neo4j-cli/internal/subcommands/docker/{delete.go,delete_test.go}, ~neo4j-cli/internal/subcommands/desktop/dbms/{delete.go,delete_test.go}, ~neo4j-cli/internal/subcommands/desktop/connection/{delete.go,delete_test.go}

## TASK-004 — 2026-05-25T11:10:00Z
Wired confirm gate into 3 credential remove leaves: neo4j-cli credential dbms/embed remove and aura credential remove. Extended Long/Example per REQ-F-030/F-031; added stdin support to dbms/embed test helpers; added 4 gating tests per leaf. Bundle regen deferred to task-005. Cherry-picked in parallel with task-003 from a worktree fork; merge resolved by appending the TASK-004 block.
Files: ~neo4j-cli/internal/subcommands/credential/dbms/{remove.go,remove_test.go,add_test.go}, ~neo4j-cli/internal/subcommands/credential/embed/{remove.go,remove_test.go,helpers_test.go}, ~neo4j-cli/aura/internal/subcommands/credential/{remove.go,remove_test.go}
Decisions: alphabetised expected JSON in non-mutating gate tests because in-place credentials.json (seeded via sjson) preserves alphabetical key order until a save re-serialises via the credential struct.

## TASK-005 — 2026-05-25T11:40:00Z
Regenerated skill bundle via `go generate ./neo4j-cli/internal/skill/...`. Bundle diff isolated to 4 references files: aura.md (+46/-15), credential.md (+26/-8), desktop.md (+10/-8), docker.md (+5/-4). All changes are flag-table / Long / Example additions for the destructive leaves wired in tasks 002-004. TestGenerator_RoundTrip green; make fmt-check / lint / test / license-check all pass.
Files: ~neo4j-cli/internal/skill/bundle/references/{aura,credential,desktop,docker}.md
Notes: deployment, dataapi/graphql (incl. authprovider + corspolicy/allowedorigin), and import subtrees are gated behind `flag.aura-beta` so they are absent from the generated bundle — bundle correctly reflects the default (non-beta) cobra tree. Observed scope gap: the user-facing `credential aura-client remove` leaf in `neo4j-cli/internal/subcommands/credential/credential.go` was NOT wired in task-004 (only the aura subtree's remove.go was), so the bundle still shows that leaf without --yes/--force. Flagging for follow-up; out of scope for task-005 (regen only).

## TASK-006 — 2026-05-25T11:35:00Z
Added Minor changelog entry covering the new --yes --force gate and the docker/desktop back-compat break. Closed the task-005 scope gap by wiring confirm.Register/Require into the user-facing `credential aura-client remove` leaf (was previously missed in task-004; only the test-only standalone-aura mount was wired). Regenerated skill bundle — diff isolated to references/credential.md (aura-client remove flags/Long/Example). All gates green: fmt-check, lint, test, license-check. Smoke: `aura instance delete x --rw` exit 3 (DB-not-found from live Aura creds; ownership check runs before gate by design); `aura instance delete x --rw --yes` exit 3 (same path); `credential aura-client remove nonexistent --rw` exit 2 with "pass both --yes and --force" (confirms gap-fix gate); `credential dbms remove nonexistent --yes --force --rw` exit 2 with "could not find credential" (NOT the gate's exit 2 — gate passed, fell through to store lookup).
Files: ~credential/credential.go, ~credential/credential_test.go, ~bundle/references/credential.md, +.changes/unreleased/neo4j-cli-Minor-20260525-113126.yaml

## TASK-007 — 2026-05-25T12:30:00Z
Collapsed two-phase command construction: `confirm.Register(cmd)` is now side-effect-only and a package-level `confirm.Require(cmd, id)` reads --yes/--force from cmd.Flags() at call time. Deleted `Flags` struct + `(*Flags).Require` method. All 16 destructive leaves moved RunE back inside the cobra.Command struct literal; `confirm.Register(cmd)` called as a single statement after the literal. Bundle untouched (flag bindings + help strings identical). All gates green: fmt-check, lint, test, license-check; TestGenerator_RoundTrip passes without bundle regen.
Files: ~common/confirm/confirm.go, ~common/confirm/confirm_test.go, ~neo4j-cli/aura/internal/subcommands/{instance,deployment,agent,customermanagedkey,deployment/token,graphanalytics/session,credential}/{delete,remove}.go, ~neo4j-cli/aura/internal/subcommands/dataapi/graphql/{delete.go,authprovider/delete.go,corspolicy/allowedorigin/remove.go}, ~neo4j-cli/internal/subcommands/{docker/delete.go,desktop/{dbms,connection}/delete.go,credential/{credential.go,dbms/remove.go,embed/remove.go}}

## TASK-008 — 2026-05-25T13:00:00Z
Centralised confirm cancellation handling: confirm.Require now writes "cancelled.\n" to stderr and sets cmd.SilenceErrors + cmd.SilenceUsage on the TTY-decline path; main.go intercepts errors.Is(err, confirm.ErrCancelled) before clierr.Render and exits 0. Removed the 6-line errors.Is + fmt.Fprintln block from all 16 destructive leaves (gate-call collapses to `if err := confirm.Require(...); err != nil { return err }`) plus the matching cancellation-tied //nolint:errcheck narration annotations. Leaf tests that expected RunE to return nil on cancel updated to expect confirm.ErrCancelled. Added integration test TestConfirmCancellation_EndToEnd in main_test.go that drives `credential aura-client remove` via app.NewCmd with TTY=true + empty stdin and asserts cmd.Execute → ErrCancelled, stderr contains "cancelled.", no "Error:" prefix, empty stdout, credential unchanged. All gates green: fmt-check, lint, test, license-check.
Files: ~common/confirm/confirm.go, ~common/confirm/confirm_test.go, ~neo4j-cli/main.go, ~neo4j-cli/main_test.go, ~neo4j-cli/aura/internal/subcommands/{instance,deployment,agent,customermanagedkey,deployment/token,graphanalytics/session,credential}/{delete,remove}.go, ~neo4j-cli/aura/internal/subcommands/dataapi/graphql/{delete.go,authprovider/delete.go,corspolicy/allowedorigin/remove.go}, ~neo4j-cli/internal/subcommands/{docker/delete.go,desktop/{dbms,connection}/delete.go,credential/{credential.go,dbms/remove.go,embed/remove.go}}, ~16 leaf _test.go files updated to expect confirm.ErrCancelled

## TASK-010 — 2026-05-25T10:25:27Z
Deduped SilenceUsage placements across 8 destructive leaves: net -3 lines (docker/delete.go collapsed from 3 calls to 1; 7 Aura leaves moved their single existing call to be first statement of RunE for consistency). End state: 12 of 16 leaves have exactly one cmd.SilenceUsage=true as first RunE statement; 4 credential leaves (aura credential remove, credential aura-client remove, credential dbms remove, credential embed remove) had none and remain untouched — no SilenceUsage to add per task scope (audit-only). All gates green.
Files: ~neo4j-cli/aura/internal/subcommands/{instance,deployment,agent,customermanagedkey,deployment/token,graphanalytics/session}/delete.go, ~neo4j-cli/aura/internal/subcommands/dataapi/graphql/corspolicy/allowedorigin/remove.go, ~neo4j-cli/internal/subcommands/docker/delete.go
