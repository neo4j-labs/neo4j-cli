## TASK-010 — 2026-06-01T17:10:00Z
Closed the Cypher-injection seam on the source --database name in docker/deploy.go. Added validateDatabaseName + conservative databaseNamePattern (`^[a-zA-Z][a-zA-Z0-9.-]{0,62}$`); PushToAura validates the name first, before resolving creds or running any STOP/START DATABASE statement or neo4j-admin call, returning a clierr.UsageError on a bad name. Chose validation-then-plain-name (over backtick quoting) per task guidance — keeps emitted Cypher readable and rejects injection regardless of the upstream deploy-leaf system check. Table-driven test TestPushToAura_InjectionDatabaseName_Rejected covers `;`/backtick/space/newline names plus empty + leading-digit, asserting the name never reaches stopStartFn or docker Exec. All gates green: make test (no failures), make fmt-check, make lint (0 issues).
Files: M neo4j-cli/internal/subcommands/docker/deploy.go, M neo4j-cli/internal/subcommands/docker/deploy_test.go

## TASK-008 — 2026-06-01T15:45:00Z
Added the Minor changelog entry via `changie new --projects neo4j-cli --kind Minor` at .changes/unreleased/neo4j-cli-Minor-20260601-154147.yaml (body: "Add `neo4j-cli aura instance deploy` to clone a local Neo4j database (from a managed Docker container or a Neo4j Desktop DBMS) into a new Aura instance."). Ran all final feature gates — all green: make test (pass, no failures), make fmt-check (ok, no gofmt drift), make lint (golangci-lint 0 issues), make license-check (pass), generate-check (no internal/skill/bundle/** drift; only the .plans tasks status flip in the working tree). Removed a stray compiled `desktop_fixture` Mach-O test binary left at repo root by a prior e2e run (not git-ignored, must not be committed). All 8 feature tasks now completed.
Files: A .changes/unreleased/neo4j-cli-Minor-20260601-154147.yaml, M .plans/tasks-cli-164-deploy-to-aura.yml, M .plans/progress-cli-164-deploy-to-aura.txt

## TASK-007 — 2026-06-01T15:10:00Z
Extended the desktop e2e fixture with the two routes the Aura deploy desktop path needs: POST /fastify/api/dbmss/{id}/databases/upload (records the source/target body in state.uploads, registers a task tagged ["db:upload", <id>] with isLoading=true, returns the empty-object body relate returns; unknown DBMS -> 404 "Could not find DBMS") and GET /fastify/api/tasks (settles every still-loading db:upload task on each poll -> isSuccess by default, isError when uploadFail armed). Both routes inherit the existing requireAuth (single /fastify/api/ dispatcher). Added a /_scenario/upload_fail toggle (scenarioSetUploadFail) for the failed-task sad-path. New state: uploadTask/uploadTaskStat/uploadRecord types, uploadTasks/uploadFail/uploads fields + reset() clears them. Added deploy_test.go (fixture package, in-process httptest like main_test.go): TestDeployDesktop_UploadAndTasks_Succeeds drives the exact deployViaDesktop wire sequence (stop -> poll-to-stopped via autoProgress -> upload -> poll GET /tasks to isSuccess -> start), asserting the recorded upload body matches source/target verbatim; plus TestDeployDesktop_UploadTask_ReportsError (upload_fail -> isError) and TestDeployDesktop_Upload_UnknownDbms_404. Gates: make test (fixture pkg 25 pass, default matrix), make fmt-check, make lint, make license-check all green; subprocess e2e suite (-tags e2e_desktop) still builds the updated fixture and runs.
Files: M test/e2e/desktop_fixture/handlers.go, M test/e2e/desktop_fixture/state.go, M test/e2e/desktop_fixture/scenario.go, A test/e2e/desktop_fixture/deploy_test.go
Decisions: Scoped the e2e test to the desktop fixture routes the deploy path depends on (approach (b) per the iteration prompt) rather than driving `deploy --from-desktop` as a subprocess — the deploy command also provisions a real Aura instance (OAuth token + instance create/poll + seeded aura-client creds), which the desktop-only harness does not mock; standing that up would be substantial new machinery. The in-process fixture-package test exercises the precise relate wire sequence deployViaDesktop runs (stop/poll/upload/tasks/start) and lives in the default `go test ./...` matrix (no build tag), so it runs everywhere without the e2e_desktop gate.

## TASK-005 — 2026-06-01T14:05:00Z
Added the `aura instance deploy` leaf (deploy.go) with NewDeployCmd: mutually-exclusive+one-required --from-docker/--from-desktop, --database (default neo4j, rejects "system"), --desktop-port, and instance-create mirror flags (--type/--memory/--region/--cloud-provider/--version/--name + credential flags) with create's exact PreRunE validation. RunE: SilenceUsage -> ResolveAndValidateOrgProject -> auto-name via list GET when --name omitted -> buildCreateInstanceBody + createAndStoreInstance (reused from task-001) -> capture target {connection_url,username,password} BEFORE no-credential-print scrub -> PollInstance to running -> dispatch. Docker path wraps docker.PushToAura(NewDeployClient()); desktop path (deployViaDesktop) discovers a relate client, stops the DBMS if running (poll-to-stopped), UploadDatabase(overwrite=true), WaitForUploadTask, deferred StartDbms to restore prior running state. Output via PrintBodyMap: instance fields (tenant_id->project_id) + discrete deploy_status (succeeded/failed). On post-create push failure the instance is left in place, deploy_status=failed, stderr names the id + retry/delete hint, and the underlying error is returned verbatim. Both dispatch paths sit behind package-level seams (deployViaDockerFn/deployViaDesktopFn) for hermetic tests. deploy_test.go drives NewDeployCmd directly (leaf not yet registered — task-006) against a mock Aura mux: mutual-exclusion/one-required, system-db rejection, free-db vs non-free + version validation parity, create->poll->push ordering, deploy_status succeeded/failed, instance-left + id-in-output on failure, desktop-vs-docker dispatch routing, and target-password absent from stderr.
Files: A neo4j-cli/aura/internal/subcommands/instance/deploy.go, A neo4j-cli/aura/internal/subcommands/instance/deploy_test.go, M neo4j-cli/internal/subcommands/docker/client.go (added NewDeployClient)
Decisions: replicated the desktop relate-client factory inline (newDeployDesktopClient var seam) rather than importing the dbms package's package-private newDesktopClient — the leaf lives in the aura tree and importing the desktop dbms subtree would be an awkward cross-tree dependency. deploy_test.go is `package instance` (internal) so it can stub the unexported dispatch seams.

## TASK-003 — 2026-06-01T13:30:00Z
Added the docker PushToAura orchestrator: dump source DB from a running container and upload into Aura via neo4j-admin. Exported AuraTarget{URI,Username,Password} and PushToAura(ctx, cfg, client, containerName, database, target). Flow: resolve source password from the dbms credential named after the container (missing -> clierr.UsageError); STOP DATABASE over an injectable stopStartFn seam; client.Exec dump (--to-path=/tmp/neo4j-cli-deploy); client.Exec upload (--from-path/--to-uri/--to-user/--to-password/--overwrite-destination); deferred START DATABASE (always runs to restore source state); deferred best-effort `rm -rf` cleanup. Production stopStartFn (stopStartDatabase) opens a short-lived vendored neo4j-go-driver/v6 session against the source's Bolt URI and runs the admin statement on the system database (same import WaitForBolt uses). deploy_test.go asserts stop->dump->upload->cleanup+start ordering, exact upload argv, START-on-upload-failure (deferred restore), missing-credential usage error, and that --to-password is redacted in surfaced errors.
Files: A neo4j-cli/internal/subcommands/docker/deploy.go, A neo4j-cli/internal/subcommands/docker/deploy_test.go
Decisions: accepted dockerClient as a PushToAura param (per iteration prompt) so task-005 injects the resolved client; existing AUTH/PASSWORD redaction regex already masks `--to-password=<v>` (the PASSWORD substring matches), so no redaction extension was needed.

## TASK-004 — 2026-06-01T13:14:36Z
Added desktopclient upload/task surface for the Aura deploy path. New types UploadSource, UploadTarget, TaskStatus, Task (types.go). New upload.go: Client.UploadDatabase (POST /dbmss/{id}/databases/upload via authed doWithTimeout, body {source,target}), Client.ListTasks (GET /tasks), and WaitForUploadTask (polls a taskLister interface, matches a task tagged both "db:upload" and dbmsID, returns nil on IsSuccess / fatal err on IsError, bounded by uploadTaskTimeout). Unit tests in upload_test.go (newAuthedServer for request-shape/auth-header assertions; table-driven fakeTaskLister for polling termination incl. context-cancel).
Files: M types.go, A upload.go, A upload_test.go
Decisions: overwrite is passed through UploadTarget (defaulted true at the deploy call site per task-005 plan); WaitForUploadTask takes a taskLister interface so the poll loop is testable without a live server; transient ListTasks errors are treated as retryable rather than fatal.

## TASK-001 — 2026-06-01T13:14:05Z
Extracted buildCreateInstanceBody + createAndStoreInstance into create_core.go and refactored NewCreateCmd to call them with zero behavioural change; NewDeployCmd can now reuse the same provisioning + credential-storage path.
Files: neo4j-cli/aura/internal/subcommands/instance/create_core.go (new), neo4j-cli/aura/internal/subcommands/instance/create.go (refactored)
Decisions: createAndStoreInstance returns a nil instance map (nil error) for any status other than 202/200, preserving the original output guard; credential-store warnings route to an injected warnOut io.Writer instead of cmd.ErrOrStderr() directly.

## TASK-002 — 2026-06-01T13:13:20Z
Added Exec(ctx, name, args) to dockerClient interface and execClient (shells `docker exec <name> <args...>` via c.run, trims stdout; non-zero exit reuses the shared redacted clierr.UsageError path). Added recording Exec to fakeDockerClient (ExecCalls []ExecCall{Name, Args} + ExecFn override). Unit tests drive the real execClient against a POSIX shell stub docker binary on PATH.
Files: neo4j-cli/internal/subcommands/docker/client.go (M), client_test.go (M), helpers_test.go (M)
Decisions: Tested via real stub-docker-on-PATH rather than only the fake, so argv shaping + redaction in the production execClient path are covered; the two Exec tests skip on Windows (Unix-shell stub).

## TASK-006 — 2026-06-01T14:35:00Z
Registered the deploy leaf in instance.go (cmd.AddCommand(NewDeployCmd(cfg)) after NewCreateCmd) and added a flush-left Example to NewDeployCmd: two --rw invocations (one --from-docker free-db, one --from-desktop professional-db on AWS), each preceded by a # comment and blank-line separated. Ran `go generate ./neo4j-cli/internal/skill/...` which refreshed references/aura.md with the new `instance deploy` surface. Gates: make test (incl. TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples), make fmt-check, make lint all green; bundle diff limited to references/aura.md.
Files: M neo4j-cli/aura/internal/subcommands/instance/instance.go, M neo4j-cli/aura/internal/subcommands/instance/deploy.go, M neo4j-cli/internal/skill/bundle/references/aura.md

## REVIEW — 2026-06-01T16:00:00Z
Added 2 review tasks (task-009..task-010).

## TASK-009 — 2026-06-01T16:30:00Z
Extracted 3 duplicated blocks from create.go/deploy.go into create_core.go: validateInstanceFlags (shared PreRunE body via instanceFlags struct; create adds graph-analytics check after, deploy adds system-db check before), resolveInstanceName (auto-name lookup), renderInstanceResult (field slice + tenant_id->project_id rename + PrintBodyMap, variadic extraFields for deploy_status). Removed deploy.go printDeployResult; both leaves delegate. Net -6 LOC, no behaviour/output change. All gates green, no bundle drift.
Files: create.go, create_core.go, deploy.go

## REVIEW — 2026-06-01T17:30:00Z
Added 2 review tasks (task-011..task-012) — env-based secret passing for deploy + create.

## TASK-011 — 2026-06-01T14:16:17Z
Hardened the Aura upload so the admin password never lands in argv. Added ExecWithEnv(ctx, name, args, env) to the dockerClient interface + execClient + fakeDockerClient; execClient gained a runEnv(ctx, env, args...) variant that sets cmd.Env = append(os.Environ(), env...) and derives `-e KEY` passthrough flags (NAME only) placed before the container name (`docker exec -e NEO4J_USERNAME -e NEO4J_PASSWORD <name> neo4j-admin ...`). Exec is now ExecWithEnv with nil env. PushToAura's upload call dropped --to-user/--to-password from argv and passes NEO4J_USERNAME/NEO4J_PASSWORD via env. fakeDockerClient.ExecCall gained an Env field. Tests: happy-path asserts upload argv has no --to-* / secret and env carries the creds; new client_test ExecWithEnv test asserts argv layout `exec -e NAME ... <name> <args>` (names only, no value) against a stub docker; redaction test renamed to confirm secret can't leak via the (now secret-free) argv echo. make test / fmt-check / lint all green.
Files: M neo4j-cli/internal/subcommands/docker/client.go, M client_test.go, M deploy.go, M deploy_test.go, M helpers_test.go
Decisions: ExecWithEnv derives the `-e KEY` flags itself from the env entries, so the caller passes env once and flag-position correctness (OPTIONS before CONTAINER) is centralized; redactArgs needs no change since the secret is never in args.

## TASK-012 — 2026-06-01T14:20:59Z
Applied task-011's env-passthrough hardening to `docker create`. Added RunWithEnv(ctx, args, env) to the dockerClient interface + execClient (funnels through runEnv, prepending `run -d` like Run) + fakeDockerClient (records RunCall{Args,Env}); Run is now RunWithEnv with nil env so all existing RunCalls/RunFn assertions keep working. create.go now appends `-e NEO4J_AUTH` (NAME only) to argv and calls RunWithEnv(ctx, argv, []string{"NEO4J_AUTH=neo4j/"+resolvedPassword}) — the generated password no longer appears in the host docker CLI argv (/proc/<pid>/cmdline). Behaviour for the user is identical (container still boots with that password; credential storage + output unchanged). create_test.go: happy-path and explicit-password tests now assert (a) argv has `-e NEO4J_AUTH`, (b) the password value is absent from argv, (c) the value is present in RunEnvCalls[0].Env. make test / fmt-check / lint all green.
Files: M neo4j-cli/internal/subcommands/docker/client.go, M create.go, M create_test.go, M helpers_test.go
Decisions: kept RunCalls populated for both Run and RunWithEnv (added a parallel RunEnvCalls for env assertions) so the ~20 existing create tests assert on argv shape unchanged.

## REVIEW — 2026-06-02T09:00:00Z
Added 1 review task (task-013) — fast-fail deploy --from-docker on community-edition containers (EE-only path).
