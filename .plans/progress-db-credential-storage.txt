================================================================================
TASK-001: Add DatabaseCredentials storage layer to common/clicfg/credentials/
Date: 2026-05-06T00:00:00Z
================================================================================

Summary:
Created the DatabaseCredentials storage layer in common/clicfg/credentials/,
mirroring the existing AuraCredentials pattern. Added database.go with
DatabaseCredentials, DatabaseCredential, and PrintableDatabaseCredentials structs
and all required methods. Updated credentials.go to include the Database field
and fix a latent onUpdate re-wiring bug that existed for Aura too (JSON unmarshal
creates a new struct without the callback; now re-wired after unmarshal for both).
Added comprehensive table-driven tests.

Files Changed:
- common/clicfg/credentials/database.go (created — DatabaseCredentials storage layer)
- common/clicfg/credentials/database_test.go (created — table-driven tests for all methods)
- common/clicfg/credentials/credentials.go (modified — added Database field, fixed onUpdate re-wiring)

Key Decisions:
- GetDefault() returns (nil, nil) when no default is set, matching the acceptance criteria ("nil/empty when no default is set") rather than returning a usage error like AuraCredentials does
- Fixed a latent bug in Aura's onUpdate re-wiring (JSON unmarshal overwrites the struct pointer, losing the onUpdate callback) — applied the fix to both Aura and Database
- Password is excluded from AsArray() and MarshalJSON() in PrintableDatabaseCredentials to prevent credential leakage in output

Next Task: task-002

================================================================================
TASK-002: Add neo4j-cli credential database parent command and add leaf
Date: 2026-05-06T01:00:00Z
================================================================================

Summary:
Created the neo4j-cli/internal/subcommands/credential/database/ package with the
parent command (database.go) and the add leaf (add.go). Mounted the database
subcommand in the credential parent. Regenerated the neo4j-cli skill bundle after
the command tree changed (required to pass TestGenerator_RoundTrip). Added
table-driven tests in add_test.go covering all required and optional flags and
the duplicate-name error path.

Files Changed:
- neo4j-cli/internal/subcommands/credential/database/database.go (created — parent command, registers add leaf)
- neo4j-cli/internal/subcommands/credential/database/add.go (created — add leaf with --name/--username/--password/--uri required, --database-name/--insecure optional)
- neo4j-cli/internal/subcommands/credential/database/add_test.go (created — 8 table-driven test cases)
- neo4j-cli/internal/subcommands/credential/credential.go (modified — added database.NewCmd import and AddCommand)
- neo4j-cli/internal/skill/bundle/references/credential.md (modified — regenerated by go generate)

Key Decisions:
- Used GlobalScope (not AuraScope) for config in tests — database credentials are not Aura-namespaced
- assertErr uses assert.Contains (not Equal) so it works with cobra's "Error: " prefix prepended to returned errors
- Skill bundle must be regenerated (go generate ./neo4j-cli/internal/skill/...) whenever the neo4j-cli command tree changes, or TestGenerator_RoundTrip fails

Next Task: task-003
