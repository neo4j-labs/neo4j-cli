## TASK-001 — 2026-05-15T08:46:41Z
Scaffolded neo4j-cli/internal/subcommands/docker/ with dockerClient interface (Run/Start/Stop/RemoveForce/PsAll/Inspect), execClient using sync.Once-cached exec.LookPath("docker"), label constants, Container struct, parent NewCmd, fakeDockerClient, smoke tests, and app.go mount between query and skill. Skill bundle regenerated; references/docker.md committed.
Files: +neo4j-cli/internal/subcommands/docker/client.go, +neo4j-cli/internal/subcommands/docker/docker.go, +neo4j-cli/internal/subcommands/docker/docker_test.go, +neo4j-cli/internal/subcommands/docker/helpers_test.go, +neo4j-cli/internal/subcommands/docker/labels.go, +neo4j-cli/internal/skill/bundle/references/docker.md, ~neo4j-cli/app/app.go, ~neo4j-cli/internal/skill/bundle/SKILL.md
Decisions: parsePsOutput/parseInspectOutput stubs land here as named hooks so list/get leaf tasks (008/009) can fill in JSON parsing without renaming.

## TASK-002 — 2026-05-15T13:05:00Z
Added WaitForBolt(ctx, uri, user, pass, timeout) that polls a neo4j-go-driver/v6 RETURN 1 probe every 500ms (pollInterval var) until ready or timeout, returning a clierr.UsageError pointing at `docker logs <name>`. Hermetic tests via probeBoltFn seam cover immediate success, success-after-3-retries, and timeout — no real Bolt server required.
Files: +neo4j-cli/internal/subcommands/docker/bolt_ready.go, +neo4j-cli/internal/subcommands/docker/bolt_ready_test.go
Decisions: imported neo4j-go-driver/v6 directly (no cycle with clicfg) instead of the documented raw-Bolt-hello fallback; pollInterval kept as a package var (not parameter) so tests can shrink it without polluting the public API.

## TASK-003 — 2026-05-15T13:55:00Z
Added `neo4j-cli docker create` minimum-viable leaf: --name/--version/--edition/--accept-license/--bolt-port/--http-port/--password/--no-store-credential. Generates a 16-byte crypto/rand → base64.RawURLEncoding password (randSource seam) when --password empty; builds docker run argv with -p, NEO4J_AUTH, NEO4J_ACCEPT_LICENSE_AGREEMENT (eval|yes) for enterprise, and six org.neo4j.cli.* labels; image is `neo4j:<version>[-enterprise]`. Stores a dbms credential (name, neo4j, password, neo4j, neo4j://localhost:<bolt-port>) unless --no-store-credential. Rendered via shared output.PrintBodyMap with a singleRow ResponseData adapter; clientFactory package var lets tests swap fakeDockerClient. Skill bundle regenerated.
Files: +create.go, +create_test.go, ~docker.go, ~docker_test.go, ~bundle/references/docker.md
Decisions: singleRow.MarshalJSON returns the array form (matching credential dbms list shape) so JSON consumers see `[{...}]` consistently; clientFactory chosen over per-cmd dependency injection to mirror the simpler dbms-style closure layout.

## TASK-004 — 2026-05-15T14:30:00Z
Added port-conflict pre-flight to `docker create` (REQ-F-013). Equal-ports check fires first (--bolt-port == --http-port → usage error); then bolt-port and http-port are probed in order via a new listenerFactory seam that net.Listens on tcp and closes immediately on success. Conflicts return clierr.NewUsageError naming the port and the offending flag. fakeListener + stubListenerFactory keep all tests hermetic (no real sockets). No skill bundle change — pre-flight is internal behaviour, no Long/Example edits.
Files: ~create.go, ~create_test.go
Decisions: runCreate now always stubs listenerFactory with zero occupied ports so existing happy-path tests stay hermetic; runCreateWithOccupiedPorts variant is used by the new port-conflict cases.

## TASK-005 — 2026-05-15T15:05:00Z
Added name-collision auto-suffix to `docker create` (REQ-F-014). After port pre-flight, resolveContainerName builds a used-name set from dockerClient.PsAll(ctx, nil) (split PsEntry.Names on commas; unfiltered so unmanaged containers count) plus cfg.Credentials.Dbms.List(); requested name wins when free, else <name>-1 … <name>-99; all-taken → clierr.NewUsageError pointing at --name. Chosen name flows into container --name, dbms credential Add, and rendered output; an `info: name "X" already in use; using "Y"` line is written to cmd.ErrOrStderr() when a suffix is applied. Five table-style tests via new runCreateWithSeed harness (no collision, docker-only, credential-only, cascading walks both sources, all-99 taken). Long: gained a one-sentence note for discoverability; bundle regenerated.
Files: ~create.go, ~create_test.go, ~bundle/references/docker.md
Decisions: PsEntry.Names split on `,` and trimmed because docker emits multi-name entries as a single comma-joined string; collectUsedNames defends nil cfg/Credentials defensively even though unreachable in production.

## TASK-006 — 2026-05-15T15:35:00Z
Added `--wait` to `docker create` (REQ-F-018). Registered via flags.RegisterWait (moved from neo4j-cli/aura/internal/flags to common/flags so the docker subtree outside aura can use it; aura helper kept as a re-export for zero churn). New package vars waitTimeout (60s production) and waitForBoltFn seam let tests bypass the real driver. After credential persistence, when --wait is set, stderr gets `info: waiting for Bolt on localhost:<port>...` then waitForBoltFn polls; on timeout the error is returned with cmd.SilenceUsage=true and NO Stop/RemoveForce is invoked (container left running for `docker logs <name>` triage). Four new tests cover happy path, timeout (asserts no teardown), --await alias compatibility, and the negative case (no probe without --wait). Long: and Example: extended with --wait copy; bundle regenerated.
Files: ~create.go, ~create_test.go, +common/flags/wait.go, ~neo4j-cli/aura/internal/flags/wait.go, ~bundle/references/docker.md
Decisions: WaitForBolt called BEFORE the final render so a timeout returns an error with no stale success output; aura's RegisterWait kept as a thin delegating shim so the 15+ existing call sites compile unchanged.

## TASK-007 — 2026-05-15T16:05:00Z
Added --ephemeral and --env-file to `docker create` (REQ-F-017). --ephemeral prepends --rm to docker run argv, flips LabelEphemeral to "true", skips credential persistence, and replaces the table/JSON render with a .env blob (header + NEO4J_URI/USERNAME/PASSWORD/DATABASE in fixed order). --env-file routes the blob to cfg.Aura.Fs().OpenFile with O_WRONLY|O_CREATE|O_TRUNC mode 0600 and prints a one-line `info: wrote credentials to <path>` to stderr, leaving stdout silent for piping. --env-file without --ephemeral and --ephemeral + --no-store-credential both surface clierr.NewUsageError before any docker side effect. Six new tests cover happy path, env-file write (stat checks 0600 + contents on memfs), two usage errors, explicit-password passthrough, and literal line-order assertion.
Files: ~create.go, ~create_test.go, ~bundle/references/docker.md
Decisions: Long: enumerates the env-file pipe-into-`query --env` flow so the discoverability surfaces in `--help` AND the regenerated bundle; renderEnvFile/writeEnvFile kept as small package-private helpers so future leaves (e.g. a future `docker get --env`) can reuse them without re-litigating the format.
