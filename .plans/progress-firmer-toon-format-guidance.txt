================================================================================
TASK-001: Append agents-prefer-toon nudge to --format flag description
Date: 2026-05-06T19:50:00Z
================================================================================

Summary:
Appended ". (agents: prefer toon)" suffix to the --format flag description in
common/flags/flags.go RegisterOutputFlag. Single source of truth — propagates
to live --help and (after task-003 regenerate) to bundle/SKILL.md Global Flags
tables and references/*.md across both skill trees. No Sprintf escaping needed
(no literal %).

Files Changed:
- common/flags/flags.go (modified — appended nudge to --format help string)

Key Decisions:
- Wording per REQ-F-001 verbatim: "(agents: prefer toon)" — short, distinct.
- Bundle round-trip test (TestGenerator_RoundTrip) now fails as expected;
  task-003 will regenerate bundles to make it pass again.

Next Task: task-002

================================================================================
TASK-002: Add directive toon bullet to both skill additions.md files
Date: 2026-05-06T19:55:00Z
================================================================================

Summary:
Replaced the soft "Prefer `--format toon`..." bullet with a directive
"**Always pass `--format toon` (`-f toon`) on read commands**" bullet (per
REQ-F-002 wording) in both neo4j-cli/internal/skill/additions.md and
neo4j-cli/aura/internal/skill/additions.md. Factual bullet immediately above
preserved as-is. Wording identical across both files. Bundle regeneration
deferred to task-003 — TestGenerator_RoundTrip will fail until then.

Files Changed:
- neo4j-cli/internal/skill/additions.md (modified — soft bullet replaced with directive)
- neo4j-cli/aura/internal/skill/additions.md (modified — soft bullet replaced with directive)

Key Decisions:
- Used identical wording in both files (REQ-F-003) — directive has no
  binary-specific content.
- Kept the existing factual bullet (line 5) untouched; only swapped the
  bullet that follows it.
- make fmt-check + make lint pass; make test deferred (bundle round-trip
  drift is expected until task-003).

Next Task: task-003

================================================================================
TASK-003: Regenerate skill bundles and reference pages
Date: 2026-05-06T20:05:00Z
================================================================================

Summary:
Ran `go generate ./neo4j-cli/internal/skill/... ./neo4j-cli/aura/internal/skill/...`
to refresh committed bundles. Regenerated both bundle/SKILL.md files and the
references/*.md pages that surface the --format flag (skill.md per binary,
query.md under neo4j-cli). Bundle round-trip gate (TestGenerator_RoundTrip)
now passes; make test, fmt-check, and lint all clean.

Files Changed:
- neo4j-cli/internal/skill/bundle/SKILL.md (regenerated — flag desc + directive bullet)
- neo4j-cli/internal/skill/bundle/references/query.md (regenerated — flag desc)
- neo4j-cli/internal/skill/bundle/references/skill.md (regenerated — flag desc)
- neo4j-cli/aura/internal/skill/bundle/SKILL.md (regenerated — directive bullet only; aura SKILL.md has no --format in Global Flags table)
- neo4j-cli/aura/internal/skill/bundle/references/skill.md (regenerated — flag desc)

Key Decisions:
- aura SKILL.md Global Flags only lists --rw (no --format); --format lives at
  subcommand level. Acceptance criterion bullet about "aura SKILL.md Global
  Flags table shows updated --format" was technically a no-op — the directive
  bullet in Gotchas covers the substance for aura.
- generate is idempotent — re-running produces no further changes; the
  generate-check "stale" error only fires because the working tree has the
  uncommitted regenerate diff itself (per AGENTS.md guidance).

Next Task: task-004

================================================================================
TASK-004: Add changie Patch entry for the toon nudge
Date: 2026-05-06T19:51:00Z
================================================================================

Summary:
Hand-authored a changie Patch entry under .changes/unreleased/ for project
neo4j-cli (changie not installed locally). Body mentions both `toon` and the
surfaces affected (help text + skill bundles) per REQ. Schema mirrors the
existing aura-cli-Minor-*.yaml sibling: project / kind / body (single-quoted) /
time (RFC3339 with +00:00 offset).

Files Changed:
- .changes/unreleased/neo4j-cli-Patch-20260506-195030.yaml (new)

Key Decisions:
- Hand-authored rather than installing changie — schema is documented in
  AGENTS.md and the sibling aura-cli entry confirms the exact shape.
- Used RFC3339 with `+00:00` (not `Z`) to match the existing sibling file's
  format exactly.
- make fmt-check, make lint, make test all clean. make generate-check skipped
  per AGENTS.md (working tree has uncommitted source/bundle diffs from prior
  tasks, so the gate is meaningful only in CI).

Next Task: task-005
