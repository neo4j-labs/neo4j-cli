## task-001 — 2026-05-25T08:05:11Z
Extended databaseInfo with CypherVersions + GraphEngine fields; fetchDatabaseInfo now iterates all dbms.components() rows and matches by literal `name`. Tests cover plain Neo4j, VG row appended, and VG row first (order-insensitive).
Files: ~neo4j-cli/query/schema.go, ~neo4j-cli/query/schema_test.go

## TASK-002 — 2026-05-25T08:08:25Z
Updated `database` bullet in query-additions.md to mention `cypher_versions` and `graph_engine`, and added a new "Virtual Graphs / Graph Engine" H2 documenting the GAIAGT-768 forbidden construct set, detection rule, and simplify-rather-than-retry guidance. `make fmt-check` clean; `make test` only fails on the expected `TestGenerator_RoundTrip` bundle-drift for `bundle/query-additions.md`, which task-003 will fix via `go generate`.
Files: ~neo4j-cli/internal/skill/query-additions.md, ~.plans/tasks-detect-graph-engine-in-schema.yml, ~.plans/progress-detect-graph-engine-in-schema.txt

## TASK-004 — 2026-05-25T08:09:27Z
Added Patch changie entry for CLI-177 covering graph_engine detection + cypher_versions surface. Generated via `changie new` in an isolated worktree (ran in parallel with task-002), then cherry-picked into the feature branch.
Files: +.changes/unreleased/neo4j-cli-Patch-20260525-100733.yaml
Decisions: parallel-via-worktree to disjoin from task-002's main-tree edits; bookkeeping (YAML status flip + this entry) done in parent after cherry-pick.

## TASK-003 — 2026-05-25T08:12:30Z
Ran `go generate ./neo4j-cli/internal/skill/...`; only `bundle/query-additions.md` changed in the bundle tree, byte-identical to the source. `make test`, `make fmt-check`, `make lint` all green; `make generate-check` clean once the in_progress flip is committed.
Files: ~neo4j-cli/internal/skill/bundle/query-additions.md, ~.plans/tasks-detect-graph-engine-in-schema.yml, ~.plans/progress-detect-graph-engine-in-schema.txt
Decisions: no non-obvious bundle diff outside query-additions.md (no references/* drift, since task-002 didn't touch cobra help text).

## TASK-005 — 2026-05-25T08:20:00Z
All five gates pass (test, fmt-check, lint, license-check, generate-check — only diff is the harness-induced status flip in the tasks YAML, no bundle/source drift). Smoke test against ephemeral enterprise 2026.04.0 container confirms `cypher_versions: ["5","25"]` present and `graph_engine` absent on plain Neo4j; container torn down cleanly.
Files: ~.plans/tasks-detect-graph-engine-in-schema.yml, ~.plans/progress-detect-graph-engine-in-schema.txt

## TASK-006 — 2026-05-25T08:30:00Z
Collapsed `TestSchema_DetectsGraphEngine` and `TestSchema_GraphEngine_KernelRowOrderInsensitive` into one table-driven test with `vg_row_last` / `vg_row_first` sub-cases; shared kernel/cypher/vg row fixtures and single assertion block. Net −24 lines in schema_test.go. All gates green (fmt-check, lint, targeted test, full make test).
Files: ~neo4j-cli/query/schema_test.go, ~.plans/tasks-detect-graph-engine-in-schema.yml, ~.plans/progress-detect-graph-engine-in-schema.txt
