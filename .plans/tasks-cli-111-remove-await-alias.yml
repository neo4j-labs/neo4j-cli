feature: cli-111-remove-await-alias
prd: ./prd-cli-111-remove-await-alias.md
created_at: 2026-05-28T00:00:00Z
updated_at: 2026-05-28T00:00:00Z

tasks:
  - id: task-001
    title: "Drop --await binding from common/flags and aura delegator + prune wait_test.go"
    description: |
      Remove the `AwaitFlagAlias` const, the second `BoolVar(wait, AwaitFlagAlias, …)` line, and the
      `MarkDeprecated` block (including its panic-on-error guard) from `common/flags/wait.go`. Update the
      doc comment on `RegisterWait` to drop the alias narrative. In `neo4j-cli/aura/internal/flags/wait.go`,
      delete the `AwaitFlagAlias = commonflags.AwaitFlagAlias` re-export and the surrounding comment block;
      keep the thin delegator. In `neo4j-cli/aura/internal/flags/wait_test.go`, drop the `--await` alone row,
      the `--wait --await` row, the `deprecationMsg` const, the `wantDeprecated` field, and the stderr-capture
      machinery; keep the `neither flag` and `--wait` rows. Keep `TestRegisterWait_HidesAliasFromHelp` intact
      (positive `--wait` + negative `--await` assertions both stay as regression insurance).
    status: completed
    dependencies: []
    acceptance_criteria:
      - "common/flags/wait.go no longer contains AwaitFlagAlias, the second BoolVar, or the MarkDeprecated call"
      - "neo4j-cli/aura/internal/flags/wait.go no longer re-exports AwaitFlagAlias"
      - "neo4j-cli/aura/internal/flags/wait_test.go retains exactly two table rows (neither + --wait) and the HidesAliasFromHelp test"
      - "go build ./... compiles; go test ./common/flags/... ./neo4j-cli/aura/internal/flags/... passes"
    completed_at: 2026-05-28T00:00:00Z

  - id: task-002
    title: "Strip --await from docker start/stop Examples and delete create_test alias test"
    description: |
      In `neo4j-cli/internal/subcommands/docker/start.go`, delete the trailing two Example lines
      (`# Same as above using the deprecated --await alias` + `neo4j-cli docker start dev --await --rw`).
      In `neo4j-cli/internal/subcommands/docker/stop.go`, delete the equivalent two trailing Example lines
      AND add a third invocation so the leaf still has ≥3 Examples (the simplest form is another
      `--wait --rw` variant against a differently-named container, matching the existing tone).
      Delete `TestCreate_Wait_AwaitAlias_StillWorks` (currently ~lines 826-860) from
      `neo4j-cli/internal/subcommands/docker/create_test.go`.
    status: pending
    dependencies:
      - task-001
    acceptance_criteria:
      - "docker/start.go and docker/stop.go Example fields contain no --await text"
      - "docker/stop.go Example contains ≥3 invocations (gate: TestAllLeafCommands_HaveExamples)"
      - "TestCreate_Wait_AwaitAlias_StillWorks is removed from create_test.go"
      - "go test ./neo4j-cli/internal/subcommands/docker/... passes"
      - "go test ./neo4j-cli/internal/subcommands/agentcontext/... passes (Example-count gate)"
    completed_at: null

  - id: task-003
    title: "Drop --await sentence from CONTRIBUTING.md §3.5"
    description: |
      In `CONTRIBUTING.md` §3.5 (around line 211), drop the sentence
      "`--await` is accepted as a deprecated alias for one release and will be removed in the following release."
      The paragraph keeps its `--wait` narrative unchanged.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "CONTRIBUTING.md §3.5 paragraph mentions --wait only, no --await text"
      - "grep -n '\\-\\-await' CONTRIBUTING.md returns no hits"
    completed_at: null

  - id: task-004
    title: "Regenerate neo4j-cli skill bundle"
    description: |
      Run `go generate ./neo4j-cli/internal/skill/...` to refresh the skill bundle. The expected diff is
      the deletion of the two `--await` example blocks from
      `neo4j-cli/internal/skill/bundle/references/docker.md` (lines 167-168 and 194-195) plus any
      incidental flag-section cleanup. No other bundle file should change beyond the source-side edits
      in task-002 propagating through render. Verify with `make generate-check`.
    status: pending
    dependencies:
      - task-002
    acceptance_criteria:
      - "neo4j-cli/internal/skill/bundle/references/docker.md contains no --await text"
      - "make generate-check is clean (no unexpected drift beyond the docker.md update)"
      - "TestGenerator_RoundTrip passes"
    completed_at: null

  - id: task-005
    title: "Add Minor changelog entry for the alias removal"
    description: |
      Add a `neo4j-cli` changelog entry via
      `changie new --projects neo4j-cli --kind Minor --body "Removed deprecated --await alias; --wait is now the only spelling (CLI-111)."`.
      If changie is not installed locally, hand-author the YAML at
      `.changes/unreleased/neo4j-cli-Minor-<YYYYMMDD>-<HHMMSS>.yaml` with fields
      `project: neo4j-cli / kind: Minor / body: '<as above>' / time: <RFC3339>`.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "A new .changes/unreleased/neo4j-cli-Minor-*.yaml file exists with project=neo4j-cli, kind=Minor"
      - "Body mentions removal of --await and references CLI-111"
    completed_at: null

  - id: task-006
    title: "Final gates + manual CLI smoke verification"
    description: |
      Run the full local gate set: `make fmt-check && make lint && make test && make generate-check && make license-check`.
      Build the binary with `make build` and exercise the four manual smoke checks: `aura instance create --await` →
      unknown-flag error; `aura instance create --wait` → reaches API call; `docker start dev --await --rw` and
      `docker stop dev --await --rw` → unknown-flag errors; `aura instance create --help` shows `--wait` and not `--await`.
    status: pending
    dependencies:
      - task-001
      - task-002
      - task-003
      - task-004
      - task-005
    acceptance_criteria:
      - "make fmt-check && make lint && make test && make generate-check && make license-check all green"
      - "bin/neo4j-cli aura instance create --await ... exits with 'Error: unknown flag: --await'"
      - "bin/neo4j-cli aura instance create --wait ... reaches the API call (credential/API failure in dev env is acceptable)"
      - "bin/neo4j-cli docker start dev --await --rw and stop dev --await --rw both exit with 'Error: unknown flag: --await'"
      - "bin/neo4j-cli aura instance create --help shows --wait and does NOT show --await"
    completed_at: null
