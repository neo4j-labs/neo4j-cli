## TASK-001 — 2026-05-18T00:00:00Z
Added `RegisterOrgProjectFlags` to `neo4j-cli/aura/internal/flags/` with constants `OrgIDFlag`, `ProjectIDFlag`, `TenantIDFlag`; registers `--organization-id` and `--project-id` as persistent flags plus deprecated `--tenant-id` alias.
Files: +neo4j-cli/aura/internal/flags/orgproject.go, +neo4j-cli/aura/internal/flags/orgproject_test.go
Decisions: Persistent flag deprecation warnings go to os.Stderr (not cobra's cmd.SetErr), so tests assert `f.Deprecated` field directly rather than capturing output.

## TASK-002 — 2026-05-18T01:00:00Z
Added `ResolveAndValidateOrgProject` to `neo4j-cli/aura/internal/subcommands/utils/resolve.go`; resolves org/project from flags then `aura.default-workspace`, handles legacy `default-tenant` migration message, then validates project membership via `api.ListProjects`. Table-driven unit tests cover all 9 resolution branches.
Files: +neo4j-cli/aura/internal/subcommands/utils/resolve.go, +neo4j-cli/aura/internal/subcommands/utils/resolve_test.go
Decisions: Migration message lowercased to satisfy staticcheck (error strings must not start with capital or end with punctuation).

## TASK-003 — 2026-05-18T02:00:00Z
Added `RenameResponseField` helper to `utils/rename.go` that renames a key in `api.ResponseData` preserving the single vs list envelope shape. Updated all `output.PrintBody` field lists in instance, customermanagedkey, and graphanalytics/session commands to use `project_id` instead of `tenant_id`. Updated two table-output tests in `get_test.go` to use `project_id` in mock responses and expected output.
Files: +neo4j-cli/aura/internal/subcommands/utils/rename.go, +neo4j-cli/aura/internal/subcommands/utils/rename_test.go, ~neo4j-cli/aura/internal/subcommands/instance/{list,get,create,delete,update,pause,resume,overwrite}.go, ~neo4j-cli/aura/internal/subcommands/instance/get_test.go, ~neo4j-cli/aura/internal/subcommands/customermanagedkey/{get,list,create}.go, ~neo4j-cli/aura/internal/subcommands/graphanalytics/session/{get,list,create}.go
Decisions: `RenameResponseField` type-switches on `api.SingleValueResponseData` to preserve JSON envelope shape; default case handles list and any future response types.

## TASK-005 — 2026-05-18T04:00:00Z
Added `FetchAndVerifyInstanceInProject` to `utils/resolve.go`; updated `instance get`, `delete`, `update`, `pause`, `resume` to call `ResolveAndValidateOrgProject` then verify ownership via pre-flight GET before any mutating call; applied `RenameResponseField` to all responses; regenerated skill bundles.
Files: ~neo4j-cli/aura/internal/subcommands/utils/resolve.go, ~neo4j-cli/aura/internal/subcommands/instance/{get,delete,update,pause,resume}.go, ~neo4j-cli/aura/internal/subcommands/instance/{get,delete,update,pause,resume}_test.go, ~neo4j-cli/aura/internal/skill/bundle/references/instance.md, ~neo4j-cli/internal/skill/bundle/references/aura.md
Decisions: `delete` and `update` share the same `/instances/{id}` path for both pre-flight GET and the mutating call; tests use a single mock with `AddResponse` to handle both sequential calls.

## TASK-004 — 2026-05-18T03:00:00Z
Called `RegisterOrgProjectFlags` on the `instance` parent command; updated `instance list` to call `ResolveAndValidateOrgProject` and pass resolved projectID as `tenantId` query param; updated `instance create` to call `ResolveAndValidateOrgProject` in `RunE`, removing the legacy `DefaultTenant`/`MarkFlagRequired` approach; both commands now apply `RenameResponseField` to surface `project_id` instead of `tenant_id`; regenerated skill bundles.
Files: ~neo4j-cli/aura/internal/subcommands/instance/instance.go, ~neo4j-cli/aura/internal/subcommands/instance/list.go, ~neo4j-cli/aura/internal/subcommands/instance/create.go, ~neo4j-cli/aura/internal/subcommands/instance/list_test.go, ~neo4j-cli/aura/internal/subcommands/instance/create_test.go, ~neo4j-cli/aura/internal/skill/bundle/references/instance.md, ~neo4j-cli/internal/skill/bundle/references/aura.md
Decisions: Removed local `--tenant-id` flag from both `list` and `create`; the deprecated persistent `--tenant-id` from `RegisterOrgProjectFlags` on the parent covers backward compatibility. `PreRunE` no longer conditionally marks `tenant-id` as required; `ResolveAndValidateOrgProject` in `RunE` returns clear errors when org/project are missing. Tests for `PreRunE`-failing paths still work because `--rw` and `PreRunE` fires before `RunE`.

## TASK-006 — 2026-05-18T05:00:00Z
Updated all `instance snapshot` leaf commands (list, create, restore) to call `ResolveAndValidateOrgProject` then `FetchAndVerifyInstanceInProject` before executing snapshot operations; flags inherited from `instance` parent with no duplicate registration; table-driven tests added covering happy path, project-not-in-org, and instance-not-in-tenant.
Files: ~neo4j-cli/aura/internal/subcommands/instance/snapshot/{list,create,restore}.go, ~neo4j-cli/aura/internal/subcommands/instance/snapshot/{list,create,restore}_test.go

## TASK-008 — 2026-05-18T07:00:00Z
Added `RegisterOrgProjectFlags` to `session` parent command; updated `list` to call `ResolveAndValidateOrgProject` and pass resolved projectID as `tenantId` query param (removing local `--tenant-id`/`--organization-id` flags); updated `create` to resolve org/project and always inject `tenant_id` into request body (removing legacy `cfg.Aura.DefaultTenant()` path); added ownership pre-flight check in `get` and `delete` via new `FetchAndVerifySessionInProject` helper; applied `RenameResponseField` to all responses; regenerated skill bundles.
Files: ~neo4j-cli/aura/internal/subcommands/graphanalytics/session/{session,list,create,get,delete}.go, ~neo4j-cli/aura/internal/subcommands/graphanalytics/session/{list,create,get,delete}_test.go, +neo4j-cli/aura/internal/subcommands/graphanalytics/session/helpers_test.go, ~neo4j-cli/aura/internal/subcommands/utils/resolve.go, ~neo4j-cli/aura/internal/skill/bundle/references/graph-analytics.md, ~neo4j-cli/internal/skill/bundle/references/aura.md

## TASK-007 — 2026-05-18T06:00:00Z
Added `RegisterOrgProjectFlags` to `customermanagedkey` parent; updated `list` to pass resolved projectID as `tenantId` query param; updated `create` to inject projectID into request body; added pre-flight ownership check in `get` and `delete` via `FetchAndVerifyInstanceInProject`-equivalent; applied `RenameResponseField`; added `FetchAndVerifyCmekInProject` helper in `resolve.go`; regenerated skill bundles.
Files: ~neo4j-cli/aura/internal/subcommands/customermanagedkey/{customermanagedkey,list,create,get,delete}.go, ~neo4j-cli/aura/internal/subcommands/customermanagedkey/{list,create,get,delete}_test.go, +neo4j-cli/aura/internal/subcommands/customermanagedkey/helpers_test.go, ~neo4j-cli/aura/internal/subcommands/utils/resolve.go

## TASK-009 — 2026-05-18T13:37:00Z
Skill bundles were already up to date from prior tasks; `go generate` produced no diff. Added changelog entry for org/project enforcement and `--tenant-id` deprecation. All gates (`make test`, `make fmt-check`, `make lint`) pass.
Files: +.changes/unreleased/neo4j-cli-Minor-20260518-133700.yaml
