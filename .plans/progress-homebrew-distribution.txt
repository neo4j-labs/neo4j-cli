================================================================================
TASK-001: Add brews: block to .goreleaser.yaml
Date: 2026-05-05T00:00:00Z
================================================================================

Summary:
Appended brews: entry to .goreleaser.yaml. Generates Formula/neo4j-cli.rb
from neo4j-cli-archive only (aura excluded). skip_upload: auto skips prereleases.
Verified locally via `goreleaser release --snapshot --skip=publish,sign,notarize`
with stub HOMEBREW_TAP_GITHUB_TOKEN — formula written to
dist/homebrew/Formula/neo4j-cli.rb with url+sha256 populated for darwin/linux x
amd64/arm64.

Files Changed:
- .goreleaser.yaml (modified — added brews block)
- .plans/tasks-homebrew-distribution.yml (modified — status flip)

Key Decisions:
- Followed PRD spec verbatim for brews block; ignored goreleaser deprecation
  notice about brews -> homebrew_casks (PRD explicitly specifies brews).
- Local snapshot verification used `goreleaser release --snapshot --skip=...`
  rather than `make snapshot` (the latter runs `goreleaser build` which doesn't
  exercise the brews step). The PRD acceptance criterion mentions make snapshot;
  may need a Makefile tweak in a follow-up task to wire brews into snapshot.

Next Task: task-002

================================================================================
TASK-002: Mint GitHub App token in release.yml for tap push
Date: 2026-05-05T00:00:00Z
================================================================================

Summary:
Added `Mint Homebrew tap token` step (id: brew-token) before `Run GoReleaser` in
release.yml. Uses SHA-pinned `actions/create-github-app-token@1b10c78c... # v3`
(latest v3.1.1) with HOMEBREW_TAP_APP_ID + HOMEBREW_TAP_APP_PRIVATE_KEY secrets,
owner=neo4j-labs, repositories=homebrew-tap. Plumbs token into GoReleaser env as
HOMEBREW_TAP_GITHUB_TOKEN. YAML parses clean (yaml.safe_load), make test +
make fmt-check pass.

Files Changed:
- .github/workflows/release.yml (modified — added token mint step + env wiring)
- .plans/tasks-homebrew-distribution.yml (modified — status flip)

Key Decisions:
- Resolved v3.1.1 -> SHA via `gh api .../git/ref/tags/v3.1.1` (1b10c78c...).
  Comment is `# v3` per AGENTS.md `# v<major>` convention; goreleaser-action
  in the same file uses fuller `# v7.0.0` form but AC explicitly says v<major>.
- No `permissions:` block on the mint step — the GitHub App is the source of
  truth for what scopes the token gets (contents:write on homebrew-tap per
  PRD); a `permissions:` block on the action would only narrow further.

Next Task: task-003

================================================================================
TASK-003: Create distribution/homebrew/README.md
Date: 2026-05-05T00:00:00Z
================================================================================

Summary:
Wrote distribution/homebrew/README.md mirroring the npm/README.md tone +
section style. Covers tap location, brew install command, stable-only cadence
(skip_upload: auto), npm @alpha link for prereleases, make snapshot dry-run,
manual recovery via tap-repo hand-edit, and auth prereqs (App + 2 secrets).
Cross-links RELEASING.md, .goreleaser.yaml, release.yml, npm/README.md.

Files Changed:
- distribution/homebrew/README.md (new)
- .plans/tasks-homebrew-distribution.yml (status flip)

Key Decisions:
- Followed PRD verbatim — channel docs only, no publish.sh (GoReleaser native).
- Style matches npm/README.md (h2 sections, callout tables) so the two
  channel READMEs read consistently when added side-by-side.
- Did not include a "What gets released" table — that lives in RELEASING.md
  per task-004. Avoids duplication.

Next Task: task-004
