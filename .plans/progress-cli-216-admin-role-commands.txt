## TASK-001 — 2026-06-16T01:30:00Z
Scaffolded the role package foundation with helpers.go (roleExecFn seam, roleFields, normalizeRoleRow, outputRoleMembers, outputUserAfterRoleChange), skeleton role.go (NewCmd with zero AddCommand calls), and role_helpers_test.go (testConn, withFakeExecFn, withSequencedExecFn). Added //nolint:unused directives to satisfy the golangci-lint unused gate on pre-declared helpers before leaf commands exist.
Files: +neo4j-cli/internal/subcommands/admin/role/helpers.go, +neo4j-cli/internal/subcommands/admin/role/role.go, +neo4j-cli/internal/subcommands/admin/role/role_helpers_test.go

## TASK-002 — 2026-06-16T02:00:00Z
Implemented the `role list` command with optional `--user` in-process filter, normalizes nil member to empty string, and outputs role+member columns via PrintBodyMap. Wired newListCmd into role.go and removed nolint:unused from now-used helpers symbols.
Files: +neo4j-cli/internal/subcommands/admin/role/list.go, +neo4j-cli/internal/subcommands/admin/role/list_test.go, ~neo4j-cli/internal/subcommands/admin/role/role.go, ~neo4j-cli/internal/subcommands/admin/role/helpers.go

## TASK-003 — 2026-06-16T03:00:00Z
Implemented the `role get` command with two-step Cypher: existence check via `SHOW ROLES WITH USERS WHERE role = $name` (correct column name, not `name`), followed by `SHOW ROLE $name PRIVILEGES`. Zero existence rows → clierr.NewNotFoundError (exit 3). Zero privilege rows → empty list (exit 0). Dynamic field sorting from rows[0] keys. Tests cover happy-path JSON and table, existing role with no privileges, not-found, exec error propagation, and no-args cobra usage error.
Files: +neo4j-cli/internal/subcommands/admin/role/get.go, +neo4j-cli/internal/subcommands/admin/role/get_test.go, ~neo4j-cli/internal/subcommands/admin/role/role.go

## TASK-004 — 2026-06-16T04:00:00Z
Implemented the `role create` command with idempotent `CREATE ROLE $name IF NOT EXISTS` Cypher. On success, calls `outputRoleMembers` for the follow-up SHOW. On error, returns immediately without follow-up. Removed `//nolint:unused` from `outputRoleMembers` in helpers.go. Tests use `withSequencedExecFn` for two-call flows (mutation + follow-up). Covers empty member list, member rows, exec error propagation, Enterprise edition hint, and no-args cobra error.
Files: +neo4j-cli/internal/subcommands/admin/role/create.go, +neo4j-cli/internal/subcommands/admin/role/create_test.go, ~neo4j-cli/internal/subcommands/admin/role/role.go, ~neo4j-cli/internal/subcommands/admin/role/helpers.go

## TASK-005 — 2026-06-16T05:00:00Z
Implemented the `role drop` command with confirm gate (`confirm.Require`/`confirm.Register` for `--yes`/`--force`). Executes `DROP ROLE $name`. Translates `Neo.ClientError.Statement.ArgumentError` with message containing "does not exist" to `clierr.NewNotFoundError` (exit code 3). No stdout output on success. Tests use `confirmtest.AssertLeafGate` for the four canonical gate scenarios. Covers: confirm gate, yes-force success (no output), not-found error with correct error code, Enterprise edition hint, and no-args cobra error.
Files: +neo4j-cli/internal/subcommands/admin/role/drop.go, +neo4j-cli/internal/subcommands/admin/role/drop_test.go, ~neo4j-cli/internal/subcommands/admin/role/role.go
