## TASK-001 — 2026-05-28T11:20:00Z
Removed `aura tenant` command tree and its registration in aura.go.
Files: -neo4j-cli/aura/internal/subcommands/tenant/{tenant.go,get.go,list.go,get_test.go,list_test.go}, ~neo4j-cli/aura/aura.go

## TASK-002 — 2026-05-28T11:25:00Z
Removed `--tenant-id` flag (const, registration, MarkDeprecated, resolveIDs fallback) and the now-broken tests; kept `default-tenant` config-key migration test.
Files: ~neo4j-cli/aura/internal/flags/orgproject.go, ~neo4j-cli/aura/internal/flags/orgproject_test.go, ~neo4j-cli/aura/internal/subcommands/utils/resolve.go, ~neo4j-cli/aura/internal/subcommands/utils/resolve_test.go
Decisions: Also pruned two `--tenant-id` rows from `TestResolveAndValidateOrgProject_TableDrivenResolutionOrder` (not listed in PRD but blocking gates).

## TASK-004 — 2026-05-28T11:28:00Z
Swapped CONTRIBUTING.md no-arguments-between-commands example from `aura tenant`/`--tenant-id` to `aura project`/`--project-id`; rule wording untouched.
Files: ~CONTRIBUTING.md

## TASK-006 — 2026-05-28T11:30:00Z
Added Minor-kind changelog entry naming both removals and pointing at `aura project list` / `aura organization get` / `--project-id`.
Files: +.changes/unreleased/neo4j-cli-Minor-20260528-112749.yaml

## TASK-003 — 2026-05-28T11:35:00Z
Added two regression tests: flag-not-registered (literal `"tenant-id"`) in orgproject_test.go and unknown-command parse-time assertion appended to aura_test.go.
Files: ~neo4j-cli/aura/internal/flags/orgproject_test.go, ~neo4j-cli/aura/aura_test.go
Decisions: Reused existing `testfs.GetDefaultTestFs()` + `clicfg.NewConfig` pattern from aura_test.go; package stayed `aura` (not `aura_test`) to match the existing file.

## TASK-005 — 2026-05-28T11:40:00Z
Scrubbed two `'tenant get'` prose references in `instance/create.go` (Long + --region flag help) → `'project get'`; regenerated skill bundle.
Files: ~neo4j-cli/aura/internal/subcommands/instance/create.go, ~neo4j-cli/internal/skill/bundle/references/aura.md
Decisions: Original task-005 scope was bundle-only; the parallel worker correctly stopped because source prose still leaked. Caller expanded scope to scrub the source prose and re-run generate. Replacement is `project get` (not `organization get` as the CLI-99 deprecation message suggested): both `tenant get` and `project get` hit the legacy `/tenants/{id}` endpoint and return the supported-configurations payload, whereas `organization get` hits `/organizations/{orgID}` and returns only `{id, name}`.

## TASK-007 — 2026-05-28T11:45:00Z
Final-gates verification. All make targets green; all four smoke assertions pass (unknown command, unknown flag, no --tenant-id in --help, no tenant subcommand in --help).
Files: (verification only — no source changes)
