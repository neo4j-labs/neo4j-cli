================================================================================
TASK-001: Rename viper key, methods, and array in common/clicfg/clicfg.go
(+ TASK-002: Rename flag to --format/-f; update all non-test callers)
(+ TASK-003: Update all test files: --output → --format, config fixtures output → format)
Date: 2026-05-05T16:30:00Z
================================================================================

Summary:
Renamed the `--output` flag and related identifiers to `--format`/`-f` across the entire
codebase. Tasks 001, 002, and 003 were completed together because they are tightly coupled —
the build fails until all callers of the renamed methods are updated, and the test suite
fails until all test fixtures are updated. Completing them separately would leave the repo
in a broken state between tasks.

Core changes in clicfg.go:
- `ValidOutputValues` → `ValidFormatValues`
- Viper key `"output"` → `"format"` in SetDefault, ValidConfigKeys, Get(), Set()
- `GlobalConfig.Output()` → `GlobalConfig.Format()`
- `GlobalConfig.BindOutput()` → `GlobalConfig.BindFormat()`
- `AuraConfig.Get()` special-case guard: `key == "output"` → `key == "format"`
- `ResolveConfigKey` docstring and rejection guard: `"aura.output"` → `"aura.format"`
- Migration block updated: destination `"format"`, two source clauses (`"aura.output"` and `"output"`), stays commented out

Caller updates:
- `common/flags/flags.go`: `--output` → `--format`/`-f`, `ValidOutputValues` → `ValidFormatValues`, `BindOutput` → `BindFormat`, error message updated
- `common/output/output.go`: `cfg.Global.Output()` → `cfg.Global.Format()`
- `neo4j-cli/aura/internal/subcommands/tenant/get.go`: `cfg.Global.Output()` → `cfg.Global.Format()`
- `neo4j-cli/aura/internal/subcommands/import/job/get.go`: `cfg.Global.Output()` → `cfg.Global.Format()`

Test file updates (58 files total):
- All `--output` flag arguments replaced with `--format`
- All `{"output":` config fixture keys replaced with `{"format":`
- Config command tests: `config get/set output` → `config get/set format`
- Error messages updated: "invalid output value" → "invalid format value"
- `AuraTestHelper` default config fixture updated from `"output": "json"` to `"format": "json"`
- `common/skill/cmd_helpers_test.go`: fixture updated
- Skill bundle files regenerated via `make generate` to reflect new `-f, --format` flag

Files Changed:
- common/clicfg/clicfg.go (modified — core renames)
- common/clicfg/clicfg_test.go (modified — updated test cases)
- common/flags/flags.go (modified — flag renamed to --format/-f)
- common/output/output.go (modified — Output() → Format() call)
- common/skill/cmd_helpers_test.go (modified — config fixture key)
- common/skill/skill_test.go (modified — --output → --format in test command)
- neo4j-cli/aura/internal/skill/bundle/references/skill.md (regenerated)
- neo4j-cli/aura/internal/subcommands/config/get_test.go (modified)
- neo4j-cli/aura/internal/subcommands/config/list_test.go (modified)
- neo4j-cli/aura/internal/subcommands/config/project/list_test.go (modified)
- neo4j-cli/aura/internal/subcommands/config/set_test.go (modified)
- neo4j-cli/aura/internal/subcommands/config/standalone_config_test.go (modified)
- neo4j-cli/aura/internal/subcommands/import/job/get.go (modified)
- neo4j-cli/aura/internal/subcommands/import/job/get_test.go (modified)
- neo4j-cli/aura/internal/subcommands/tenant/get.go (modified)
- neo4j-cli/aura/internal/subcommands/tenant/get_test.go (modified)
- neo4j-cli/aura/internal/test/testutils/auratesthelper.go (modified — default config fixture)
- neo4j-cli/internal/skill/bundle/SKILL.md (regenerated)
- neo4j-cli/internal/skill/bundle/references/query.md (regenerated)
- neo4j-cli/internal/skill/bundle/references/skill.md (regenerated)
- neo4j-cli/internal/subcommands/config/get_test.go (modified)
- neo4j-cli/internal/subcommands/config/list_test.go (modified)
- neo4j-cli/internal/subcommands/config/project_test.go (modified)
- neo4j-cli/internal/subcommands/config/set_test.go (modified)
- [30+ aura subcommand test files with --output → --format replacements]

Key Decisions:
- Completed tasks 001, 002, and 003 together in one iteration because the build and tests would be broken between tasks
- Used Python for bulk text replacement in test files for correctness and speed
- Kept render/render_test.go's `--output` fixture unchanged (it's a test fixture for the renderer, not the real CLI flag)
- Ran `make generate` to update committed bundle files with new flag signature
- The migration block was updated in comments to include both "aura.output" and "output" source clauses

Next Task: task-004 (Update hand-authored docs)
