## task-001 — 2026-05-13T06:54:16Z
Introduced *CLIError struct (Code, Message, Err, RetryAfter) and 8 typed constructors in common/clierr; existing constructor signatures preserved.
Files: ~common/clierr/error.go, +common/clierr/error_test.go
Decisions: unwrapFormatted seeds CLIError.Err from %w-formatted inner so errors.Is reaches sentinels through CLIError.Unwrap.

## task-002 — 2026-05-13T07:05:00Z
Mapped handleResponseError HTTP branches to typed constructors: 400→Validation, 401/403→Auth, 404→NotFound, 409→Conflict, 429→RateLimit (with Retry-After on struct), 5xx/405→Upstream. formatAuthorizationError now returns NewAuthError on both branches. Added table-driven ExitCodeMapping test.
Files: ~neo4j-cli/aura/internal/api/response.go, ~neo4j-cli/aura/internal/api/response_test.go

## task-003 — 2026-05-13T07:03:34Z
Categorised Bolt-driver errors at the query-package boundary: Neo.ClientError.* → NewValidationError (exit 6); Neo.TransientError.* / Neo.DatabaseError.* / transport failures → NewUpstreamError (exit 8). Wrapping happens in runStatementResponse (dispatch layer that prod + test seam both flow through) and openDriver, so schema.go callers get typed errors for free without per-site edits.
Files: +neo4j-cli/query/errors.go, +neo4j-cli/query/errors_test.go, ~neo4j-cli/query/connect.go
Decisions: dual-mode classifier (typed *neo4j.Neo4jError via errors.As, plus string-prefix fallback) so tests injecting plain errors.New("Neo.ClientError…") via the seam still classify correctly without constructing real driver structs.

## task-004 — 2026-05-13T07:35:00Z
Reclassified four auth-flow sites to NewAuthError (exit 4): token.go 401, AuraCredentials.GetDefault no-default, openai.go missing-API-key, huggingface.go missing-API-key. All other clierr.New* sites stay (URL validation = usage; credential lookup-by-name in flags hook = usage; poll wrap = upstream; update.go per PRD; fatal panics intact). No validation-style NewUpstreamError sites found outside response.go (already covered in task-002).
Files: ~common/clicfg/credentials/aura.go, ~neo4j-cli/aura/internal/api/token.go, ~neo4j-cli/query/embed/openai.go, ~neo4j-cli/query/embed/huggingface.go, +common/clicfg/credentials/aura_test.go, ~neo4j-cli/aura/internal/api/api_test.go, ~neo4j-cli/query/embed/openai_test.go, ~neo4j-cli/query/embed/huggingface_test.go
Decisions: AuraCredentials.GetDefault has only one call site (api.go MakeRequest auth path), so reclassifying the store-level error is safe — no risk of misclassifying a `credential aura-client get-default` introspection path because none exists.

## task-005 — 2026-05-13T07:13:12Z
Registered FlagErrorFunc on neo4j-cli root (app.NewCmd) and aura standalone root (NewStandaloneCmd) wrapping cobra flag-parse errors into clierr.NewUsageError (exit 2). Cobra walks up to root so one registration per tree covers all subcommands. Added table-driven tests asserting *CLIError with Code==2 for both root-level and subcommand-level --bad-flag.
Files: ~neo4j-cli/app/app.go, ~neo4j-cli/aura/aura.go, ~neo4j-cli/app/app_test.go, ~neo4j-cli/aura/aura_test.go
