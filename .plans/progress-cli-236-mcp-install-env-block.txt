## TASK-001 — 2026-08-14T12:20:25Z
Created common/skill/mcpenv.go with five MCP env var constants, MCPGates struct, and MCPServerEnv function that always emits all five keys (gates as explicit "true"/"false"). Added mcpenv_test.go covering all 8 gate permutations.
Files: +common/skill/mcpenv.go, +common/skill/mcpenv_test.go
Decisions: envBoolStr uses non-conflicting name vs existing boolStr("yes"/"no") in inventory.go

## TASK-002 — 2026-08-14T12:32:00Z
Pointed serve.go and manifest.go at the shared skill.EnvMCP* constants; removed the five local consts from serve.go. manifest_test.go unchanged, confirming byte-identical manifest output.
Files: ~neo4j-cli/internal/subcommands/mcp/serve.go, ~neo4j-cli/internal/subcommands/mcp/manifest.go, ~neo4j-cli/internal/subcommands/mcp/serve_test.go
Decisions: manifest keeps its own map literal — MCPServerEnv returns concrete "true"/"false", but the manifest needs ${user_config.*} templates. Only NAME constants are shared, not values.

## TASK-003 — 2026-08-14T12:38:00Z
InstallMCPConfig now takes MCPGates and writes the five-key env block; MCPConfigEntry gained Env. Six test call sites updated via a noGates() helper, plus an assertFullEnv() helper used by the golden and cross-platform tests.
Files: ~common/skill/mcpconfig.go, ~common/skill/mcpconfig_test.go, ~neo4j-cli/internal/subcommands/mcp/install.go
Decisions: install.go passed a zero-value MCPGates as a placeholder, replaced in task-004.

## TASK-004 — 2026-08-14T12:44:27Z
Added --allow-writes/--allow-aura/--allow-credential-write to mcp install and resolveInstallGates, which reads --rw by VALUE and the --allow-* flags by Changed. Matrix test covers all 6 rows; a new end-to-end test asserts the env block actually written to the agent config.
Files: ~neo4j-cli/internal/subcommands/mcp/install.go, ~neo4j-cli/internal/subcommands/mcp/install_test.go, ~neo4j-cli/internal/subcommands/mcp/mcp_helpers_test.go, +neo4j-cli/internal/subcommands/mcp/export_test.go
Decisions: --rw read by VALUE not Changed — RequireWriteAccess only waives the gate, never sets the flag, so Changed would enable all three gates on an explicit --rw=false. Matrix test alone tests resolution in isolation; added newMCPInstallFixtureFS + an end-to-end test because the flags-to-disk wiring is what actually decides whether Desktop can write.

## TASK-005 — 2026-08-14T13:05:00Z
runInstallOne now returns the method actually used instead of deriving it from useBundle; runInstallBundle falls back to the config write when openFile fails, surfacing the .mcpb path. Added an openFileFn test seam.
Files: ~neo4j-cli/internal/subcommands/mcp/install.go, ~neo4j-cli/internal/subcommands/mcp/install_test.go, ~neo4j-cli/internal/subcommands/mcp/export_test.go
Decisions: when both open and the config fallback fail, the ORIGINAL open error is returned — it names the actual cause.

## TASK-006 — 2026-08-14T13:14:54Z
mcp check now reports drift when an installed entry's env block lacks the manifest marker, so pre-existing gate-dead installs get a signal. Added readMCPConfigEnv plus InstalledHasMCPManifest on AgentInstall.
Files: ~neo4j-cli/internal/subcommands/mcp/check.go, ~neo4j-cli/internal/subcommands/mcp/check_test.go, ~common/skill/mcpconfig.go, ~common/skill/mcpconfig_test.go, ~common/skill/installer.go
Decisions: path drift and env drift both report the single "drift" status — the remediation (mcp install refresh) is identical for both.

## TASK-007 — 2026-08-14T14:05:00Z
Rewrote mcp install's Long and Example to document the --rw implication rule, the three --allow-* flags, the config-vs-bundle difference (including the bundle-only display name and settings screen limitation), and the actual fallback behavior. Example flush-left with 4 invocations, comments, and a subset-form example.
Files: ~neo4j-cli/internal/subcommands/mcp/install.go
Decisions: none

## TASK-008 — 2026-08-14T15:26:16Z
Created Patch changelog entry for the env-block fix, the three --allow-* flags, and the mcp check drift signal. Every claim verified against actual test assertions (golden merge, gate matrix, drift detection).
Files: +.changes/unreleased/neo4j-cli-Patch-20260814-152616.yaml
Decisions: none

## TASK-009 — 2026-08-14T13:30:49Z
Ran the full gate suite: make test, make fmt-check, make lint all pass. Confirmed no drift in policy.golden or the skill bundle, and manifest_test.go is unmodified vs main.
Files: (verification only)
Decisions: manifest_test.go staying byte-identical is the proof the shared-constants refactor changed no manifest output.

## TASK-010 — 2026-08-14T13:37:38Z
Verified end to end against the real Claude Desktop config: install wrote the 5-key env block, the live neo4j-data-modeling entry and all unrelated top-level keys survived byte-identical, mcp check reported ok, and remove restored the file exactly. Drove mcp serve over JSON-RPC both with and without the env block.
Files: (verification only)
Decisions: proof is the A/B probe — without the env block a write tool call returns "Write operations are not allowed"; with it the same call passes the gate and reaches the CLI. Same for the aura gate. Confirms the env->serve->Gates chain is live.

## REVIEW — 2026-08-14T14:19:03Z
Added 2 review tasks (task-011..task-012).

## TASK-011 — 2026-08-14T14:35:01Z
Threaded gates into GenerateBundle/newManifest; added the three --allow-* flags to mcp bundle; new test asserts user_config defaults track passed gates. Env template values unchanged.
Files: ~manifest.go, ~manifest_test.go, ~mcp_helpers_test.go, ~install.go, ~bundle.go
Decisions: bundle.go replicates the same three flags as install.go and reuses resolveInstallGates to match install's behaviour.

## TASK-012 — 2026-08-14T14:35:01Z
Corrected install.go Long and flag usage strings to say flags seed bundle toggle state instead of having no effect; made bundle.go Example flush-left; amended changelog to cover bundle-mode seeding.
Files: ~install.go, ~bundle.go, ~.changes/unreleased/neo4j-cli-Patch-20260814-152616.yaml
Decisions: dropped the "(no effect with --bundle)" parenthetical from flag usage strings rather than adding a longer one; the Long covers both modes.
