================================================================================
TASK-001: Split aura.NewCmd to remove credential registration; add NewStandaloneCmd
Date: 2026-05-01T00:00:00Z
================================================================================

Summary:
Removed `credential.NewCmd(cfg)` from `aura.NewCmd` and introduced `aura.NewStandaloneCmd`
which composes `NewCmd` and appends the credential subcommand. Updated `AuraTestHelper` to
use `NewStandaloneCmd` so existing credential tests continue to pass.

Files Changed:
- neo4j-cli/aura/aura.go (modified: removed credential from NewCmd, added NewStandaloneCmd)
- neo4j-cli/aura/internal/test/testutils/auratesthelper.go (modified: use NewStandaloneCmd)

Key Decisions:
- AuraTestHelper.ExecuteCommand was updated to NewStandaloneCmd in task-001 (not task-002) to
  keep existing credential tests passing — without this, make test would fail after task-001 alone.
- The credential import in aura.go is still used by NewStandaloneCmd, so no unused import issue.
- aura/cmd/main.go still calls NewCmd (standalone binary temporarily lacks credential); tracked by task-002.

Next Task: task-002
