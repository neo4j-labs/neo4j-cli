## TASK-001 — 2026-06-01T00:00:00Z
Added GlobalScope config keys history-enabled (bool, default true) + history-limit (int, default 1000, 0 disables) with HistoryEnabled()/HistoryLimit() accessors, Set validation, and ValidConfigKeys registration so they surface via config get/set/list.
Files: +1 new (common/clicfg/history_test.go), ~3 mod (clicfg.go, list_test.go, standalone_config_test.go)
Decisions: keys sort alphabetically between format and skill-auto-refresh — updated both GlobalScope and AuraScope config-list snapshot tests.

## TASK-002 — 2026-06-01T00:00:00Z
Created history store package: Entry struct (RFC3339 UTC time, command, invoker, version, workspace/credential omitempty), path() helper, and best-effort Record/Load/Clear over cfg.Aura.Fs(). Record redacts via clievents.RedactArgs, detects invoker via a test-seam'd term.IsTerminal(stdin), honors history-enabled/limit, trims to last N, writes mode 0600 without panicking; Load skips corrupt lines.
Files: +neo4j-cli/internal/subcommands/history/store.go, +neo4j-cli/internal/subcommands/history/store_test.go, ~common/clicfg/fileutils/fileutils.go
Decisions: Added fileutils.WriteFileErr (error-returning wrapper around writeFileAtomic) so Record can swallow write errors instead of panicking like WriteFile.

## TASK-003 — 2026-06-01T08:44:11Z
Wired history.Record into the root PersistentPreRunE chain (after credential init, before versioncheck) gated by a new shouldRecordHistory(cmd) ancestry-walk that skips the history subtree, help, and cobra completion (__complete/__completeNoDesc). Best-effort/pre-run: Record swallows write errors and the chain still returns nil. Tests verify recording fires for a nested aura subcommand (workspace list) via EnableTraverseRunHooks and is skipped for help/completion.
Files: ~neo4j-cli/app/app.go, +neo4j-cli/app/history_hook_test.go
Decisions: Record's 2nd param is unused (uses os.Args internally) but the call passes cmd.CommandPath() per the call-site contract.

## TASK-004 — 2026-06-01T09:30:00Z
Added `neo4j-cli history` command tree (one-file-per-leaf): list (last 20 newest-first, --limit N, --format json|table|toon) + clear (--force or UsageError). Default/table view renders the human form `[time] <command> {invoker:..., workspace:..., credential:...}`; json/toon use the shared commonoutput.PrintBodyMap path via a historyRows ResponseData adapter. Registered history.NewCmd(cfg) in app.go next to config. Regenerated skill bundle (references/history.md + SKILL.md row).
Files: +.../history/history.go, +.../history/list.go, +.../history/clear.go, +.../history/{list_test,clear_test,helpers_test}.go, ~neo4j-cli/app/app.go, +bundle/references/history.md, ~bundle/SKILL.md
Decisions: --format inherited as a persistent flag from the root (no per-leaf registration); standalone leaf tests set format via config JSON rather than the --format flag. clear is annotated write:true so the Example gate skips the --format-json requirement.

## TASK-005 — 2026-06-01T08:55:00Z
Final gates task: confirmed skill bundle idempotent (go generate produced no diff post task-004), documented `neo4j-cli history` (list/clear, --format, --limit) plus a local-logging privacy note in README, and added a Minor changelog entry. make test, fmt-check, lint all green.
Files: ~README.md, +.changes/unreleased/neo4j-cli-Minor-20260601-105129.yaml

## REVIEW — 2026-06-01T09:00:00Z
Added 2 review tasks (task-006..task-007).

## TASK-006 — 2026-06-01T11:00:00Z
Dropped the unused 2nd param from history.Record: signature now Record(cfg *clicfg.Config). app.go call site no longer computes cmd.CommandPath(); os.Args remains the redaction source so recorded command string is unchanged. Updated 7 test call sites.
Files: ~neo4j-cli/internal/subcommands/history/store.go, ~neo4j-cli/app/app.go, ~neo4j-cli/internal/subcommands/history/store_test.go
