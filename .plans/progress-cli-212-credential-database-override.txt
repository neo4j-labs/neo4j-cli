## TASK-001 — 2026-06-09T20:38:16Z
resolveConn: --database/NEO4J_DATABASE now combinable with --credential (all 3 paths) via applyDatabaseOverride; conflict list shrunk to uri/username/password.
Files: ~neo4j-cli/query/connect.go
Decisions: override applied at finishDesktopMatch's two returns + persisted path (not inside builders); flag f.Changed wins even when explicitly empty.
