================================================================================
TASK-001: Add common/skill/print.go leaf command
Date: 2026-05-06T08:35:00Z
================================================================================

Summary:
Created common/skill/print.go defining newPrintCmd that returns a cobra leaf
with Use:"print", cobra.NoArgs, and a RunE that reads SKILL.md from the
embedded bundle via fs.ReadFile and writes raw bytes (no {{VERSION}}
substitution) to cmd.OutOrStdout. Not yet mounted in skill.go (task-002).

Files Changed:
- common/skill/print.go (created — new leaf command)
- .plans/tasks-skill-print-command.yml (status pending -> completed)

Key Decisions:
- Used `_` for unused cfg/skillName params; signature externally matches
  (cfg, bundle, skillName) per acceptance criteria. Idiomatic Go.
- `cmd.Print(string(data))` writes the SKILL.md verbatim to OutOrStdout.
- Long description notes that {{VERSION}} stays literal.

Next Task: task-002 (mount print leaf in NewCmd)

================================================================================
TASK-002: Mount print leaf in common/skill/skill.go::NewCmd
Date: 2026-05-06T08:50:00Z
================================================================================

Summary:
Added single line `cmd.AddCommand(newPrintCmd(cfg, bundle, skillName))` to
common/skill/skill.go::NewCmd. Both binaries now expose `skill print` as a
subcommand. skill.go remains 41 lines (well under the 80-line cap).

Files Changed:
- common/skill/skill.go (modified — added AddCommand for print leaf)
- .plans/tasks-skill-print-command.yml (status pending -> completed)

Key Decisions:
- TestGenerator_RoundTrip in neo4j-cli/internal/skill/gen and neo4j-cli/aura/
  internal/skill/gen now correctly fails because the embedded bundles have
  not yet been regenerated. This is the explicit responsibility of task-004
  (`make generate`) and confirms the round-trip gate works as intended.
- make build and both `skill --help` outputs verified locally; print appears
  alongside install/remove/list/check.

Next Task: task-003 (add common/skill/print_test.go colocated tests)
