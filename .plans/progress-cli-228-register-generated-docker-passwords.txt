# Progress: cli-228-register-generated-docker-passwords

## TASK-001 — 2026-08-05T12:10:41Z
Extracted docker container-password minting into a new password.go owning randSource,
generatedPasswordBytes and generatePassword, which now calls clievents.RegisterSecretValue on the
minted value. Deleted the duplicate generatePassword from load.go; docker load already routes
through the helper, docker create still uses its inline block until task-002.
Files: +neo4j-cli/internal/subcommands/docker/password.go, ~neo4j-cli/internal/subcommands/docker/create.go, ~neo4j-cli/internal/subcommands/docker/load.go
Decisions: error message unified on `docker: generate password:` (matches the package's shared-helper convention `docker: parse ps output:` / `docker: bolt probe:`; no test or doc asserted either old string). create.go also had to drop `crypto/rand` — rand.Reader moved with randSource — while keeping base64/io for the still-inline block.

## TASK-002 — 2026-08-05T12:15:29Z
Routed docker create's password resolution through generatePassword(), completing the CLI-228 fix —
both leaves now register generated passwords. Dropped the newly-unused encoding/base64 and io imports.
Files: ~neo4j-cli/internal/subcommands/docker/create.go
Decisions: crypto/rand was already removed by task-001, so only base64+io remained despite the task text listing three imports. Reassigning the outer closure `err` (declared line 250) is safe — every later err is a fresh `:=` in a narrower scope, so no stale-err read. Review was done inline rather than via the hone-reviewer subagent (fork cannot spawn subagents).

## TASK-003 — 2026-08-05T12:24:27Z
Added password_test.go unit-testing generatePassword directly: one test pins that the minted value is
registered (RedactText scrubs it standalone AND inside a synthetic box-drawing cell), one injects an
always-failing randSource and asserts the wrapped sentinel. Verified both fail with the
RegisterSecretValue call removed, then restored password.go byte-for-byte.
Files: +neo4j-cli/internal/subcommands/docker/password_test.go
Decisions: with the call simply deleted the package won't compile (clievents becomes an unused import), so both tests fail via build failure; isolating the behavioural failure needed the import artificially retained, and then only the registration test fails — the randSource-failure test never touches registration. AC-5's "both" holds only in the compile-failure sense. Reviewer flagged the first draft's cell-assertion comment as false (a 22-char base64 value matches none of RedactText's four regex passes, so the standalone assertion already pins the registry exclusively); rewrote it to claim only what is true. Tightened the error assertion to the full `docker: generate password` prefix.

## TASK-004 — 2026-08-05T12:39:10Z
Added the primary CLI-228 pin to create_test.go: a table-driven test over --format table/toon/json
asserting raw stdout still renders the generated password while clievents.RedactText(stdout) scrubs
it, plus a companion test for the --ephemeral .env blob path.
Files: ~neo4j-cli/internal/subcommands/docker/create_test.go
Decisions: constantReader bytes CONSUMED — 0x02 (table), 0x03 (toon), 0x04 (json), 0x05 (ephemeral).
Full taken set is now 0x01-0x05, 0x10, 0x42, 0x55, 0x66, 0x77, 0x88, 0xAB, 0xCD, 0xEF; task-006 must
pick outside it (0x06 is free). Verified no registered literal is a substring of another, so no
subtest can be carried by a sibling's registration in the additive process-global registry.
--format is passed explicitly per subtest because default resolution is harness/TTY-dependent.
Mutation-verified twice (before and after review edits): with RegisterSecretValue neutralised while
keeping the clievents import live, table and toon FAIL on both the NotContains and the
Contains-"***" assertions while json and ephemeral PASS — confirming json is carried by
textJSONFieldRe and the ephemeral blob by the unanchored textAssignmentRe, exactly as their honesty
comments claim. Reviewer verified all three mandated honesty comments against redact.go / tee.go /
main.go and found none overstated; applied its three low-priority polish notes (dropped the
duplicated CLI-218 sentence and the restated regex-mechanism clause, dropped an unexplained
--no-store-credential, reworded the "survive" phrasing).

## TASK-005 — 2026-08-05T12:51:47Z
Added TestCreate_ExplicitPassword_NotRegisteredAsSecret to create_test.go, pinning REQ-F-006: an
operator-supplied --password is deliberately NOT registered, so clievents.RedactText returns the
literal unchanged. Passes pre- and post-fix by design; mutation-verified to fail if registration
widens.
Files: ~neo4j-cli/internal/subcommands/docker/create_test.go
Decisions: NO constantReader byte consumed — an explicit --password short-circuits generatePassword,
so this test registers nothing (taken set unchanged: 0x01-0x05, 0x10, 0x42, 0x55, 0x66, 0x77, 0x88,
0xAB, 0xCD, 0xEF; 0x06 free for task-006). Literal is `cli228.operator.supplied`: the DOTS are
outside the base64url alphabet, which makes collision-proofness structural rather than probabilistic
— no substring of it can ever be a generatePassword output, so the negative assertion cannot be
broken by the additive process-global registry under any test order (verified with -shuffle=on).
Asserted the bare literal, not the captured stdout: `"password":"<v>"` in the JSON capture is
scrubbed by textJSONFieldRe regardless of registration, so only the direct assertion isolates the
registry. Mutation check: temporarily added clievents.RegisterSecretValue on create.go's supplied-
password branch → test FAILED (got "***"); reverted, create.go and password.go both byte-clean.
Reviewer flagged the first draft's "only a GENERATED password is registered" as false repo-wide
(desktop/dbms/create.go:221 and desktop/connection/create.go:197 DO register supplied passwords) —
rescoped the claim to docker and named the asymmetry.

## TASK-006 — 2026-08-05T13:01:57Z
Added TestLoad_NewContainer_GeneratedPassword_ScrubbedFromRedactedCapture to load_test.go, the
second-site CLI-228 pin: drives `docker load`'s new-container path with --format table and asserts raw
stdout still renders the generated password while clievents.RedactText(stdout) scrubs it to ***.
Files: ~neo4j-cli/internal/subcommands/docker/load_test.go
Decisions: constantReader byte CONSUMED — 0x06 (the first load test to swap randSource at all; the
whole taken set is now 0x01-0x06, 0x10, 0x42, 0x55, 0x66, 0x77, 0x88, 0xAB, 0xCD, 0xEF). --format
table passed explicitly because default resolution is harness/TTY-dependent. Existing runLoad helper
reused unchanged — it already stubs client/resolve/download/listener/bolt-wait, so no daemon or
network. Mutation-verified: with clievents.RegisterSecretValue neutralised in password.go while
keeping the clievents import live, the test FAILS on both the NotContains and the Contains-"***"
assertions (confirming a box-drawing cell matches none of RedactText's regex passes); password.go then
restored byte-clean. Package also green under -shuffle=on. Reviewer verified the four load-specific
comment claims against source (tee/main.go failure-only path, load.go's flag set, ResolveOutput
precedence + unclamped table style, knownSecrets semantics) and flagged three comment defects, all
applied: (1) HIGH — dropped the inherited "human-facing output byte-identical" claim, which is false
for this leaf because task-001 changed load's randSource-failure stderr prefix from `generate
password:` to `docker: generate password:`; rescoped to "registration is additive — the rendered row
is unchanged". (2) MEDIUM — replaced the duplicated scope-caveat prose with a cross-reference to the
create-side matrix test, matching the convention already used by the ephemeral test. (3) LOW — dropped
"the only load test that swaps randSource" (a non-sequitur, since knownSecrets is process-global, and
self-falsifying) in favour of "0x06 is unused package-wide". NOTE for a future pass: create_test.go
carries the same false byte-identical claim at its line ~1770; left untouched as task-004's committed
code and out of this task's scope.

## TASK-007 — 2026-08-05T13:12:05Z
Documentation-only: extended AGENTS.md's RedactText bullet (TOON array rows are unmatched too, and TOON
is the agent-harness default) plus a new adjacent bullet naming docker/password.go generatePassword() as
the single registering mint point, and extended redact.go's coverage note to name docker create /
docker load's new-container path alongside aura instance create.
Files: ~AGENTS.md, ~common/clievents/redact.go
Decisions: named the desktop asymmetry explicitly (`desktop/{dbms,connection}/create.go` +
`connection/update.go` DO register operator-supplied passwords) so no blanket "only generated passwords
are registered" rule can be inferred either way — verified all three call sites. Scoped `docker load` to
its new-container path in both files: the existing-container path reuses a stored dbms credential and
mints nothing (load.go:215), so generatePassword is reached only at load.go:345. Said
`aura instance load` staging with NO flag — there is no `--stage` flag (staging is unconditional, gated
only on the PsAll daemon probe); this was the accuracy trap called out in the task text. Reviewer
empirically confirmed the TOON-array claim by running toon.Marshal output through RedactText: an array
row `[#1]{name,password}:` survives verbatim (textAssignmentRe hits `}` before a `[=:]`), while
single-object TOON `password: <v>` IS matched — hence the claim is scoped to array ROWS in both files.
Also widened the knownSecrets var comment (redact.go:141) to mention TOON rows so the two comments in
that file agree on completeness. Zero go-generate drift (AGENTS.md is not embedded anywhere).

## TASK-008 — 2026-08-05T13:53:39Z
Verification sweep only, no code change: make test (exit 0, zero FAIL, docker pkg ok), fmt-check (0),
lint (0 issues), license-check (0) all pass; `go generate ./...` + `git diff --exit-code` excluding
.plans shows ZERO skill-bundle drift; nothing added under .changes/ on the branch (REQ-NF-003 holds);
docker package green under -shuffle=on x4 (263 tests).
Files: ~tasks-cli-228-register-generated-docker-passwords.yml, ~progress-cli-228-register-generated-docker-passwords.txt
Decisions: `make generate-check` itself cannot pass on a dirty tree (it runs a bare `git diff
--exit-code`, which sees the in-flight .plans edits), so the gate was run as its two constituent steps
with .plans excluded from the diff. REQ-NF-005 re-confirmed by inspection: client.go:276 returns raw
stdout.String(); redactString touches only the non-zero-exit message (:274) and debugInvocation's argv
+ env-NAMES echo (:178), so parsePsOutput/parseInspectOutput/parseNeo4jPluginsEnv all consume
unredacted text, and classifyInspectError's "no such " / stop.go's inspectExited match substrings a
22-char base64url ReplaceAll cannot alter. Independently re-ran the mutation check (registration
replaced by a no-op, import kept live): table + toon + the load pin + the password_test registration
test FAIL, json + ephemeral + the explicit-password test PASS — exactly what their honesty comments
claim; password.go restored byte-exact, package 263/263 green. Also verified via `go list -deps -test`
that no desktop*/aura/desktopclient package links into the docker test binary, so create_test.go's
"every registration in this binary comes from generatePassword" is true, and that
instance/load.go:265 really does call docker.LoadDumpIntoNewContainer (password.go / AGENTS.md
`aura instance load` claim holds). The hone-reviewer subagent was launched but never returned a
report; an inline self-review of every comment claim was done instead and found no defect.
