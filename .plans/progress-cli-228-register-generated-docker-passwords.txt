# Progress: cli-228-register-generated-docker-passwords

## TASK-001 — 2026-08-05T12:10:41Z
Extracted docker container-password minting into a new password.go owning randSource,
generatedPasswordBytes and generatePassword, which now calls clievents.RegisterSecretValue on the
minted value. Deleted the duplicate generatePassword from load.go; docker load already routes
through the helper, docker create still uses its inline block until task-002.
Files: +neo4j-cli/internal/subcommands/docker/password.go, ~neo4j-cli/internal/subcommands/docker/create.go, ~neo4j-cli/internal/subcommands/docker/load.go
Decisions: error message unified on `docker: generate password:` (matches the package's shared-helper convention `docker: parse ps output:` / `docker: bolt probe:`; no test or doc asserted either old string). create.go also had to drop `crypto/rand` — rand.Reader moved with randSource — while keeping base64/io for the still-inline block.

## TASK-002 — 2026-08-05T12:15:29Z
Routed docker create's password resolution through generatePassword(), completing the CLI-228 fix —
both leaves now register generated passwords. Dropped the newly-unused encoding/base64 and io imports.
Files: ~neo4j-cli/internal/subcommands/docker/create.go
Decisions: crypto/rand was already removed by task-001, so only base64+io remained despite the task text listing three imports. Reassigning the outer closure `err` (declared line 250) is safe — every later err is a fresh `:=` in a narrower scope, so no stale-err read. Review was done inline rather than via the hone-reviewer subagent (fork cannot spawn subagents).

## TASK-003 — 2026-08-05T12:24:27Z
Added password_test.go unit-testing generatePassword directly: one test pins that the minted value is
registered (RedactText scrubs it standalone AND inside a synthetic box-drawing cell), one injects an
always-failing randSource and asserts the wrapped sentinel. Verified both fail with the
RegisterSecretValue call removed, then restored password.go byte-for-byte.
Files: +neo4j-cli/internal/subcommands/docker/password_test.go
Decisions: with the call simply deleted the package won't compile (clievents becomes an unused import), so both tests fail via build failure; isolating the behavioural failure needed the import artificially retained, and then only the registration test fails — the randSource-failure test never touches registration. AC-5's "both" holds only in the compile-failure sense. Reviewer flagged the first draft's cell-assertion comment as false (a 22-char base64 value matches none of RedactText's four regex passes, so the standalone assertion already pins the registry exclusively); rewrote it to claim only what is true. Tightened the error assertion to the full `docker: generate password` prefix.

## TASK-004 — 2026-08-05T12:39:10Z
Added the primary CLI-228 pin to create_test.go: a table-driven test over --format table/toon/json
asserting raw stdout still renders the generated password while clievents.RedactText(stdout) scrubs
it, plus a companion test for the --ephemeral .env blob path.
Files: ~neo4j-cli/internal/subcommands/docker/create_test.go
Decisions: each subtest installs its own deterministic randSource so it registers a distinct literal;
verified no registered literal is a substring of another, so no subtest can be carried by a sibling's
registration in the additive process-global registry (made structural in task-011).
--format is passed explicitly per subtest because default resolution is harness/TTY-dependent.
Mutation-verified twice (before and after review edits): with RegisterSecretValue neutralised while
keeping the clievents import live, table and toon FAIL on both the NotContains and the
Contains-"***" assertions while json and ephemeral PASS — confirming json is carried by
textJSONFieldRe and the ephemeral blob by the unanchored textAssignmentRe, exactly as their honesty
comments claim. Reviewer verified all three mandated honesty comments against redact.go / tee.go /
main.go and found none overstated; applied its three low-priority polish notes (dropped the
duplicated CLI-218 sentence and the restated regex-mechanism clause, dropped an unexplained
--no-store-credential, reworded the "survive" phrasing).

## TASK-005 — 2026-08-05T12:51:47Z
Added TestCreate_ExplicitPassword_NotRegisteredAsSecret to create_test.go, pinning REQ-F-006: an
operator-supplied --password is deliberately NOT registered, so clievents.RedactText returns the
literal unchanged. Passes pre- and post-fix by design; mutation-verified to fail if registration
widens.
Files: ~neo4j-cli/internal/subcommands/docker/create_test.go
Decisions: this test registers NOTHING — an explicit --password short-circuits generatePassword.
Literal is `cli228.operator.supplied`: the DOTS are outside
the base64url alphabet, which makes collision-proofness structural rather than probabilistic
— no substring of it can ever be a generatePassword output, so the negative assertion cannot be
broken by the additive process-global registry under any test order (verified with -shuffle=on).
Asserted the bare literal, not the captured stdout: `"password":"<v>"` in the JSON capture is
scrubbed by textJSONFieldRe regardless of registration, so only the direct assertion isolates the
registry. Mutation check: temporarily added clievents.RegisterSecretValue on create.go's supplied-
password branch → test FAILED (got "***"); reverted, create.go and password.go both byte-clean.
Reviewer flagged the first draft's "only a GENERATED password is registered" as false repo-wide
(desktop/dbms/create.go:221 and desktop/connection/create.go:197 DO register supplied passwords) —
rescoped the claim to docker and named the asymmetry.

## TASK-006 — 2026-08-05T13:01:57Z
Added TestLoad_NewContainer_GeneratedPassword_ScrubbedFromRedactedCapture to load_test.go, the
second-site CLI-228 pin: drives `docker load`'s new-container path with --format table and asserts raw
stdout still renders the generated password while clievents.RedactText(stdout) scrubs it to ***.
Files: ~neo4j-cli/internal/subcommands/docker/load_test.go
Decisions: first load test to swap randSource at all, with its own distinct deterministic value.
--format table passed explicitly because default resolution is harness/TTY-dependent. Existing runLoad
helper
reused unchanged — it already stubs client/resolve/download/listener/bolt-wait, so no daemon or
network. Mutation-verified: with clievents.RegisterSecretValue neutralised in password.go while
keeping the clievents import live, the test FAILS on both the NotContains and the Contains-"***"
assertions (confirming a box-drawing cell matches none of RedactText's regex passes); password.go then
restored byte-clean. Package also green under -shuffle=on. Reviewer verified the four load-specific
comment claims against source (tee/main.go failure-only path, load.go's flag set, ResolveOutput
precedence + unclamped table style, knownSecrets semantics) and flagged three comment defects, all
applied: (1) HIGH — dropped the inherited "human-facing output byte-identical" claim, which is false
for this leaf because task-001 changed load's randSource-failure stderr prefix from `generate
password:` to `docker: generate password:`; rescoped to "registration is additive — the rendered row
is unchanged". (2) MEDIUM — replaced the duplicated scope-caveat prose with a cross-reference to the
create-side matrix test, matching the convention already used by the ephemeral test. (3) LOW — dropped
"the only load test that swaps randSource" (a non-sequitur, since knownSecrets is process-global, and
self-falsifying) in favour of "0x06 is unused package-wide". NOTE for a future pass: create_test.go
carries the same false byte-identical claim at its line ~1770; left untouched as task-004's committed
code and out of this task's scope.

## TASK-007 — 2026-08-05T13:12:05Z
Documentation-only: extended AGENTS.md's RedactText bullet (TOON array rows are unmatched too, and TOON
is the agent-harness default) plus a new adjacent bullet naming docker/password.go generatePassword() as
the single registering mint point, and extended redact.go's coverage note to name docker create /
docker load's new-container path alongside aura instance create.
Files: ~AGENTS.md, ~common/clievents/redact.go
Decisions: named the desktop asymmetry explicitly (`desktop/{dbms,connection}/create.go` +
`connection/update.go` DO register operator-supplied passwords) so no blanket "only generated passwords
are registered" rule can be inferred either way — verified all three call sites. Scoped `docker load` to
its new-container path in both files: the existing-container path reuses a stored dbms credential and
mints nothing (load.go:215), so generatePassword is reached only at load.go:345. Said
`aura instance load` staging with NO flag — there is no `--stage` flag (staging is unconditional, gated
only on the PsAll daemon probe); this was the accuracy trap called out in the task text. Reviewer
empirically confirmed the TOON-array claim by running toon.Marshal output through RedactText: an array
row `[#1]{name,password}:` survives verbatim (textAssignmentRe hits `}` before a `[=:]`), while
single-object TOON `password: <v>` IS matched — hence the claim is scoped to array ROWS in both files.
Also widened the knownSecrets var comment (redact.go:141) to mention TOON rows so the two comments in
that file agree on completeness. Zero go-generate drift (AGENTS.md is not embedded anywhere).

## TASK-008 — 2026-08-05T13:53:39Z
Verification sweep only, no code change: make test (exit 0, zero FAIL, docker pkg ok), fmt-check (0),
lint (0 issues), license-check (0) all pass; `go generate ./...` + `git diff --exit-code` excluding
.plans shows ZERO skill-bundle drift; nothing added under .changes/ on the branch (REQ-NF-003 holds);
docker package green under -shuffle=on x4 (263 tests).
Files: ~tasks-cli-228-register-generated-docker-passwords.yml, ~progress-cli-228-register-generated-docker-passwords.txt
Decisions: `make generate-check` itself cannot pass on a dirty tree (it runs a bare `git diff
--exit-code`, which sees the in-flight .plans edits), so the gate was run as its two constituent steps
with .plans excluded from the diff. REQ-NF-005 re-confirmed by inspection: client.go:276 returns raw
stdout.String(); redactString touches only the non-zero-exit message (:274) and debugInvocation's argv
+ env-NAMES echo (:178), so parsePsOutput/parseInspectOutput/parseNeo4jPluginsEnv all consume
unredacted text, and classifyInspectError's "no such " / stop.go's inspectExited match substrings a
22-char base64url ReplaceAll cannot alter. Independently re-ran the mutation check (registration
replaced by a no-op, import kept live): table + toon + the load pin + the password_test registration
test FAIL, json + ephemeral + the explicit-password test PASS — exactly what their honesty comments
claim; password.go restored byte-exact, package 263/263 green. Also verified via `go list -deps -test`
that no desktop*/aura/desktopclient package links into the docker test binary, so create_test.go's
"every registration in this binary comes from generatePassword" is true, and that
instance/load.go:265 really does call docker.LoadDumpIntoNewContainer (password.go / AGENTS.md
`aura instance load` claim holds). The hone-reviewer subagent was launched but never returned a
report; an inline self-review of every comment claim was done instead and found no defect.

## REVIEW — 2026-08-05T14:19:16Z
Added 4 review tasks (task-009..task-012).

## TASK-009 — 2026-08-05T14:33:16Z
Moved `constantReader` verbatim (doc comment included) from create_test.go into helpers_test.go, the
convention home for helpers shared across leaf test files — load_test.go and password_test.go both
depend on it now. Pure code motion: +14/-14, no test body, assertion or byte value touched.
Files: ~neo4j-cli/internal/subcommands/docker/create_test.go, ~neo4j-cli/internal/subcommands/docker/helpers_test.go
Decisions: errReader/errRandSource LEFT in password_test.go — grep confirms they are referenced only
there, so hoisting would be premature. No import edits needed in either file: constantReader names no
imported package (io.Reader is satisfied structurally). Placed AFTER the closing
`var _ dockerClient = (*fakeDockerClient)(nil)` assertion per reviewer note, so the fakeDockerClient
unit stays contiguous and task-011's stubRandSource gets a rand-seam section at the file's end.

## TASK-010 — 2026-08-05T14:40:59Z
Moved all four CLI-228 redaction pins into a new redaction_test.go — the three create-side tests out
of create_test.go (1894 -> 1750 lines, below the 1763 pre-branch baseline, so the branch now SHRINKS
the package's largest test file) and the load pin out of load_test.go (451 -> 402). Verified
byte-identical motion programmatically (extracted blocks compared against HEAD: identical), and that
the package's test-function set is unchanged (171 funcs, 263 subtests, no add/drop/rename).
Files: +neo4j-cli/internal/subcommands/docker/redaction_test.go, ~neo4j-cli/internal/subcommands/docker/create_test.go, ~neo4j-cli/internal/subcommands/docker/load_test.go
Decisions: chose a NEW redaction_test.go over password_test.go — password_test.go is the unit-level
pair for password.go (it calls generatePassword directly and owns errReader/errRandSource), whereas
these four are cobra-driven output-surface pins; a subject-named file keeps both units coherent and
matches the package's existing non-leaf test files (parsers_test.go, debug_test.go, helpers_test.go).
MOVED the load pin too: it makes every randSource-byte consumer this branch added (0x02-0x06) sit in
one file, which is where task-011's stubRandSource helper and byte bookkeeping collapse, and turns
task-012's cross-file comment references into plain adjacency — the ephemeral test's "format-matrix
test above" and the load pin's "see that test's comment" now both resolve in-file. Cost accepted: the
load pin no longer sits with its sibling load tests, but it is a redaction pin, not load behaviour.
Import fallout: dropped clievents from create_test.go, clievents + encoding/base64 from load_test.go
(bytes stays, still used by the buffer helpers). No helper travelled — every helper the moved tests
use (runCreate, runCreateForEphemeral, runLoad, newFakeDockerClient, loadDeps, moviesSpec,
constantReader) is package-scope already. Zero comment/assertion/byte edits: the false byte-identical
claim, the duplicated rationale and the byte ledger are all left intact for tasks 011/012 to rewrite.
Gates: make test (exit 0), fmt-check, lint (0 issues), license-check all pass; all four moved tests
confirmed RUN + PASS by name under -v -run; docker package green 3x under -shuffle=on. Reviewer
returned LGTM with no blocking findings: independently re-derived the 189 moved lines as byte-identical,
confirmed 171 test funcs before and after with an empty symmetric difference, confirmed both flagged
cross-references now resolve in-file (the load pin's was cross-file before, so the move strengthened
it), found zero references to the four moved names from anywhere outside redaction_test.go, and
endorsed the load-pin move — noting that had it stayed, 0x06's "unused package-wide" claim would remain
a cross-file assertion nobody can check locally. Its one factual catch, applied here: the line counts
above were off by one/two because they were measured BEFORE the unused-import removals — create_test.go
is 1750, load_test.go 402.

## TASK-011 — 2026-08-05T14:58:31Z
Replaced the hand-picked-byte constantReader dance with helpers_test.go's stubRandSource(t), which
derives the 16 password bytes from sha256(t.Name()), installs the seam via t.Cleanup and RETURNS the
exact password generatePassword will mint; converted all 13 call sites, deleted constantReader, and
collapsed the three copy-pasted assertion triples into redaction_test.go's assertRedactionScrubs.
Files: ~neo4j-cli/internal/subcommands/docker/helpers_test.go, ~neo4j-cli/internal/subcommands/docker/redaction_test.go, ~neo4j-cli/internal/subcommands/docker/create_test.go, ~neo4j-cli/internal/subcommands/docker/password_test.go
Decisions: derivation is sha256(t.Name())[:16] (the FULL 16-byte buffer, not one repeated byte — one
byte gives only 256 values), keyed on t.Name() because the testing package uniquifies it per test AND
subtest, so the matrix's three subtests stay distinct. Property guaranteed: distinct names → distinct
values, and every value is exactly 22 base64url chars, so equal length ⇒ none can be a substring of
another (redactKnownSecrets is a literal ReplaceAll, so a registered value inside another test's
asserted literal would rewrite part of it). Verified out-of-band: all 15 derived values distinct,
22 chars, substring-free. Converted ALL 13 sites, including the 9 pre-existing ones — none asserts a
hard-coded password string, every one compares against the helper's return value, so
TestCreate_GeneratedPassword_UsesRandSourceAndBase64URLEncoding still independently re-derives
RawURLEncoding in test code and remains a real pin against production switching to padding. Second
helper setRandSource(t, io.Reader) carries the t.Cleanup restore and lets password_test's errReader
site drop its manual defer too; documented that callers must not t.Parallel() (the seam is a package
global). Byte bookkeeping deleted: the "distinct randSource byte" and "byte 0x06 is unused
package-wide" comments in redaction_test.go, plus the authoritative ledger lines in the task-004/005/006
entries above (consumed bytes, full taken set, next-free byte). The three surviving `0x…` mentions in
this file (task-006 and task-010 entries) are historical narrative about comments that no longer
exist, not a table anyone must maintain — left as history rather than rewriting it. Mutation-verified
AFTER the review edits: with RegisterSecretValue neutralised while keeping the clievents import live,
the table + toon subtests, the load pin and TestGeneratePassword_RegistersSecretValueForRedaction FAIL
while json, ephemeral and the explicit-password test PASS; password.go then restored byte-exact (empty
diff). make test / fmt-check / lint / license-check pass; docker package green under -shuffle=on x8.
Reviewer returned no blocking findings; applied all three of its substantive notes: (1) HIGH — the
ephemeral test had silently lost its key-anchored raw assertion (a blob emitting the password under a
DIFFERENT key would still satisfy the generic triple), so require.Contains(stdout,
"NEO4J_PASSWORD="+pw) is back as an explicit line before the helper call; (2) MEDIUM — the doc
comment's "longest-first" justification was inverted (longest-first is what makes nesting SAFE per
redact.go), rewritten to name the actual hazard; (3) MEDIUM — dropped the mintedPasswords collision
map + mutex as unreachable defensive code (~2^-128) that re-introduced a global allocation ledger,
keeping sha256(t.Name()) as the structural guarantee.

## TASK-012 — 2026-08-05T15:13:44Z
Collapsed the five duplicated redaction facts onto generatePassword's doc comment as the single
authority; every other site is now one line plus a pointer. Net -16 prose lines.
Files: ~neo4j-cli/internal/subcommands/docker/password.go, ~neo4j-cli/internal/subcommands/docker/create.go, ~neo4j-cli/internal/subcommands/docker/redaction_test.go, ~common/clievents/redact.go, ~AGENTS.md
Decisions: password.go para 3 (the CLI-218 block) trimmed to one sentence — it described another
branch's state and was the most stale-prone text in the file; the --debug "stderr plus env NAMES only"
clause was dropped as surplus to the point being made. Deleted create-side supplied-password rationale
block wholesale (password.go states it); AGENTS.md bullet cut from five sentences to one + pointer.
Kept ONLY the informative half of the collision-proofness note (dots are outside the base64url
alphabet) and dropped the "no other registering package is linked in" paragraph — a build-graph claim
that rots silently, and the dots argument already makes the exclusion structural. Re-scoped that
sentence from "no registered value" to "no minted password": the universal form silently smuggled the
dropped build-graph claim back in. Stale (REQ-F-015) cites removed from password.go + create.go (that
ID is from an older docker-create PRD; CLI-228's governing IDs are REQ-F-001/002) — the same dangling
convention elsewhere in the package is pre-existing and deliberately left alone. Verified before
asserting: rendered real TOON output ([#1]{name,password}: / dev,abc123) rather than trusting the
shape from memory, and re-confirmed all three desktop leaves DO register operator-supplied passwords,
so password.go's "In the docker leaves only GENERATED passwords are registered" keeps its qualifier.
Also verified aura instance load's stageViaDocker sets no NewContainerLoad.Password, so that path
really does mint via generatePassword (target.Password at instance/load.go:291 is the AURA instance
password for PushToAura, not the container password). Reviewer returned one MEDIUM: my redact.go TOON
clause cited a `{name,password}` field list that matches NO real command (aura instance create emits
ten fields; docker create eight) — replaced with `{…,password,…}`. Fixed before commit; no other
findings. All gates + zero bundle drift.
