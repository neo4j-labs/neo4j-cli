# Progress: cli-228-register-generated-docker-passwords

## TASK-001 — 2026-08-05T12:10:41Z
Extracted docker container-password minting into a new password.go owning randSource,
generatedPasswordBytes and generatePassword, which now calls clievents.RegisterSecretValue on the
minted value. Deleted the duplicate generatePassword from load.go; docker load already routes
through the helper, docker create still uses its inline block until task-002.
Files: +neo4j-cli/internal/subcommands/docker/password.go, ~neo4j-cli/internal/subcommands/docker/create.go, ~neo4j-cli/internal/subcommands/docker/load.go
Decisions: error message unified on `docker: generate password:` (matches the package's shared-helper convention `docker: parse ps output:` / `docker: bolt probe:`; no test or doc asserted either old string). create.go also had to drop `crypto/rand` — rand.Reader moved with randSource — while keeping base64/io for the still-inline block.
