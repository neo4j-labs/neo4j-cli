## TASK-001 — 2026-05-27T12:45:00Z
Scaffold common/skill/catalog pkg: SkillEntry/Catalog types, Load (afero plugin.json), Stale (fetched-at + ttl), URL constants, userCacheDirFn/nowFn seams.
Files: +common/skill/catalog/source.go, +common/skill/catalog/cache.go, +common/skill/catalog/catalog.go, +common/skill/catalog/catalog_test.go

## TASK-002 — 2026-05-27T13:00:00Z
Secure tar+gzip extractor: streams .tar.gz, strips top-level dir, allowlists first-segment skill names, rejects traversal/absolute/symlink/hardlink/device/fifo, 20 MiB total decompressed cap via afero.Fs (0600/0755).
Files: +common/skill/catalog/extract.go, +common/skill/catalog/extract_test.go
Decisions: cap is total-decompressed across all entries (not per-file); NUL-in-name guard is defense-in-depth (Go's tar.Writer already rejects), tested via classifyEntry unit table.

## TASK-004 — 2026-05-27T12:50:10Z
Introduce Source{FS, Version} in common/skill; Install now takes (fs, src, name, agentFilter). injectVersion rewrites or inserts the frontmatter version: line; placeholder substitution retired. update.go + skillrefresh.go wrap commonskill.Install to preserve their (bundle, version) seam shape.
Files: ~common/skill/installer.go, ~common/skill/installer_test.go, ~common/skill/install.go, ~neo4j-cli/internal/skill/install_e2e_test.go, ~neo4j-cli/internal/skillrefresh/skillrefresh.go, ~neo4j-cli/internal/subcommands/update/update.go

## TASK-003 — 2026-05-27T12:59:07Z
Catalog.Refresh (ctx, fs) + Lookup (fs, name, binaryName) with httpDoer seam on Catalog (Doer + BinaryVersion fields). Tarball fetch+extract happens BEFORE plugin.json write so mid-refresh failures leave prior cache intact. SelfCanonicalName + ReservedNames/IsReserved exported for task-005 alias map. httptest routing via per-test Doer; PluginJSONURL/TarballURL stay frozen const.
Files: +common/skill/catalog/refresh.go, +common/skill/catalog/refresh_test.go, ~common/skill/catalog/source.go, ~common/skill/catalog/catalog.go, ~common/skill/catalog/cache.go
Decisions: write plugin.json AFTER tarball extract so cache stays consistent on partial failure

## TASK-005 — 2026-05-27T13:15:00Z
ResolveSelf(bundle, version, binaryName, name) maps `self` + binary-name alias to the embedded Source via catalog.IsReserved; any other name yields ErrNotSelfSkill so install/remove/print leaves can fall through to catalog.Lookup. Reserved set stays owned by catalog (single source of truth); a contract test re-asserts that every catalog.ReservedNames entry is accepted by the resolver.
Files: +common/skill/resolve.go, +common/skill/resolve_test.go

## TASK-006 — 2026-05-27T13:30:00Z
install/remove/print swap positional [agent] for [skill-name] + per-leaf --agent flag. Hard-break helper (didYouMeanAgentErr / unknownSkillErr in helpers.go) gates positional resolution: self+binary-alias pass, agent-name collisions hard-break with `unknown skill: <name>; did you mean '--agent <name>'?`, unknown names emit plain unknown-skill (catalog Lookup plugs in via task-007/008). remove now requires positional; print defaults to self. Bundle regenerated.
Files: ~common/skill/install.go, ~common/skill/remove.go, ~common/skill/print.go, ~common/skill/helpers.go, ~common/skill/install_test.go, ~common/skill/remove_test.go, ~common/skill/print_test.go, ~common/skill/check_test.go, ~common/skill/list_test.go, ~neo4j-cli/internal/skill/bundle/references/skill.md

## TASK-007 — 2026-05-27T13:21:08Z
install leaf gains --all/--refresh + catalog wiring. loadOrRefreshCatalog (catalog_helpers.go) centralises auto-refresh policy: cold cache+net fail → fatal w/ refresh hint when content needed, warm cache+net fail → stderr warn + cached fallback, stale (>24h) → auto-refresh. resolveCatalogSkillSource consults ResolveSelf then catalog.Lookup, agent-name hard-break fires pre-catalog-load to avoid wasted network. --all iterates plugin.json.skills (skipping reserved) and aggregates per-skill failures. HTTPDoer exported on catalog pkg for cross-package test injection; SetCacheRoot/CacheRoot exposed. Test seams via export_test.go (SetCatalogCacheRootForTest, SetCatalogHTTPDoerForTest). Bundle regenerated.
Files: ~common/skill/install.go, ~common/skill/install_test.go, ~common/skill/helpers.go, +common/skill/catalog_helpers.go, +common/skill/catalog_helpers_test.go, +common/skill/export_test.go, ~common/skill/catalog/catalog.go, ~common/skill/catalog/refresh.go, ~common/skill/catalog/source.go, ~neo4j-cli/internal/skill/bundle/references/skill.md
Decisions: --refresh without content need (no positional, no --all) treats cache as optional (warn-only on net fail); --all + positional is rejected up front; agent-name hard-break runs before catalog load to keep error fast/offline.

## TASK-008 — 2026-05-27T14:00:00Z
remove leaf gains --all + offline-only catalog wiring. positional becomes optional (cobra.MaximumNArgs(1)); --all and a positional are mutually exclusive, no-positional && !--all errors. runRemove dispatches: self/binary-alias → Remove + stderr reinstall hint ("Run '<bin> skill install' to reinstall."), known catalog skill → Remove, unknown name → unknown-skill err (catalogSkillKnown gates via cached plugin.json — cold cache treats non-self names as unknown). runRemoveAll pre-validates --agent, no-ops on cold cache (stderr note), iterates cat.Skills skipping reserved entries, surfaces a stderr omission note + per-skill failure aggregation. renderInstallRows extended with action param so empty-row message reads "No agents to remove." for the remove path. Reinstall hint goes to stderr so JSON stdout stays valid. Bundle regenerated.
Files: ~common/skill/remove.go, ~common/skill/remove_test.go, ~common/skill/install.go, ~neo4j-cli/internal/skill/bundle/references/skill.md
Decisions: reinstall hint → stderr (keeps stdout JSON parseable); --all unknown-agent pre-validated to avoid one-error-per-skill noise; cold-cache --all is a no-op (no network access — list-only consumer).

## TASK-009 — 2026-05-27T14:30:00Z
list leaf rewritten: one row per (skill × agent), self-first then plugin.json order, reserved-name skip. Columns: skill, source (embedded|catalog), agent, detected, installed, installed_version, available_version, status (installed|not-installed|drift|unknown-version). Cold cache → self-only rows + stderr hint "Run '<bin> skill refresh'"; warm cache fallback after net failure warns but keeps cached catalog rows. Auto-refresh on staleness via loadOrRefreshCatalog(requireUsableCache:false). --refresh forces fetch. installed_version reused via existing parseVersion helper (installer.go); available_version = cfg.Version for self / cat.Version for catalog. PrintBodyMap drives all three formats from one column set. Bundle regenerated.
Files: ~common/skill/list.go, ~common/skill/list_test.go, ~neo4j-cli/internal/skill/bundle/references/skill.md
Decisions: list all agents (not just detected) — `detected` column carries that signal and matches the pre-existing list shape; suppress the "using cached content" warn when load.Cat==nil so cold-cache + net-fail only emits the refresh hint; JSON shape break (removed display_name, added skill/source/available_version/status) is acceptable since the feature ships behind a Major changelog (task-014).

## TASK-010 — 2026-05-27T15:00:00Z
check leaf rewritten: iterates self + catalog skills × AGENTS, emits one row per *installed* (skill × agent) only. New columns: skill, agent, installed_version, current_version, status (ok|drift|unknown-version). Loads catalog via loadOrRefreshCatalog(requireUsableCache:false) — cold cache reports only self rows. Non-zero exit via clierr.NewValidationError ("drift detected in N skill(s)") whenever any row is drift or unknown-version. New --refresh flag mirrors list. Legacy installer.Check (CheckRow) preserved for installer_test.go callers; CheckRowView is the new leaf-only view. Extracted readInstalledSkill(fs, agent, skillName) to helpers.go and dedup'd list.go's per-row install probe. Bundle regenerated.
Files: ~common/skill/check.go, ~common/skill/check_test.go, ~common/skill/helpers.go, ~common/skill/list.go, ~neo4j-cli/internal/skill/bundle/references/skill.md
Decisions: keep legacy installer.Check API so installer_test.go (and any future internal caller) stays binary-compatible — leaf gets its own CheckRowView; JSON adds `skill` column (breaking) — acceptable under the feature's Major changelog (task-014); use ValidationError (exit 6) for drift since the installed payload no longer matches the source-of-truth version (validation semantics fit better than fatal).

## TASK-011 — 2026-05-27T15:30:00Z
print leaf gains catalog-aware positional resolution while staying strictly offline (REQ-NF-008): empty/self/binary-alias → embedded bundle, catalog name → catalog.Load + Lookup (no Refresh, no loadOrRefreshCatalog), unknown name with agent collision → didYouMeanAgentErr, unknown name + cold cache → clierr.NewUsageError with refresh hint, unknown name + warm cache → plain unknownSkillErr. resolvePrintSource returns fs.FS (not Source) since print never injects version. Stale helpers.resolveSkillSource (only print called it) deleted. Bundle regenerated.
Files: ~common/skill/print.go, ~common/skill/print_test.go, ~common/skill/helpers.go, ~neo4j-cli/internal/skill/bundle/references/skill.md
Decisions: no --agent flag on print (print emits canonical source, not installed copy — REQ-F-004 scopes install/remove only); print never calls catalog.Refresh even with --refresh (it has no --refresh flag) so cold-cache test asserts zero network hits; cold-cache error reuses "unknown skill: <name>" prefix so callers parsing for that token still work.

## TASK-012 — 2026-05-27T16:00:00Z
refresh leaf added: newRefreshCmd registers on parent, forces loadOrRefreshCatalog(forceRefresh:true, requireUsableCache:false) and renders a single-row (version, skill_count, source) result via PrintBodyMap. Cold cache + net fail → UsageError with connectivity hint (own message, not the helper's "run skill refresh" tautology); warm cache + net fail → stderr warn + cached row. `Annotations: write=true` matches install/remove. JSON marshals the single row as an object (not an array) so machine consumers can read `.version` directly. countAddressableSkills uses catalog.IsReserved to mirror list/check's reserved-name skip. Bundle regenerated.
Files: +common/skill/refresh.go, +common/skill/refresh_test.go, ~common/skill/skill.go, ~neo4j-cli/internal/skill/bundle/references/skill.md
Decisions: requireUsableCache:false + explicit Cat==nil check (cleaner error message than helper's "run skill refresh" hint, which is silly when the user already ran refresh); MarshalJSON returns an object not array since refresh always emits exactly one row.

## TASK-013 — 2026-05-27T13:59:22Z
Skill bundle refreshed: description.txt rewritten to mention "installs the embedded self-skill or any curated catalog skill from neo4j-contrib/neo4j-skills into supported AI agents" (1007 chars, under 1024 ceiling); additions.md gained three bullets — catalog source + self/binary-alias reservation + hard-break agent-name positional, --all on install/remove (self-skill preservation note) + --refresh on install/list/check + new refresh leaf, cache location/24h TTL/network-failure policy/print offline-only. Updated the existing "version: line" bullet to clarify self vs catalog source. go generate ./neo4j-cli/internal/skill/... rewrote bundle/SKILL.md so the new tips render at install time. All gates clean.
Files: ~neo4j-cli/internal/skill/additions.md, ~neo4j-cli/internal/skill/description.txt, ~neo4j-cli/internal/skill/bundle/SKILL.md
Decisions: trimmed "from the terminal via the neo4j-cli CLI" / "questions" / "Cypher parameter" → just "parameter" / ".env file consumable by" → ".env for" to make 23+ char headroom for the catalog clause; kept the catalog mention single-clause ("self-skill or any curated catalog skill from neo4j-contrib/neo4j-skills") rather than per-leaf to preserve the ≤1024 ceiling.

## TASK-014 — 2026-05-27T16:05:00Z
Major changelog entry filed via `changie new --projects neo4j-cli --kind Major` capturing both the positional-arg hard break (`skill install <agent>` removed — `--agent <name>` required) and the new catalog surface (catalog install by name, --all, --refresh, new `skill refresh` leaf). All final gates clean: make test / fmt-check / lint / license-check; generate-check shows only the task-yml in_progress flip (no bundle drift), per AGENTS.md Makefile Notes.
Files: +.changes/unreleased/neo4j-cli-Major-20260527-160133.yaml

## REVIEW — 2026-05-27T14:45:48Z
Added 6 review tasks (task-015..task-020).
