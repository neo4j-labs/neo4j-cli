================================================================================
TASK-001: Set release.prerelease to false in .goreleaser.yaml
Date: 2026-05-11T13:00:00Z
================================================================================

Summary:
Changed `release.prerelease` from `true` to `false` in `.goreleaser.yaml` so
all future GoReleaser GitHub Releases are marked as GA (Latest) rather than
pre-release.

Files Changed:
- .goreleaser.yaml (modified: release.prerelease true → false)

Key Decisions:
- Single field change only; no other release block fields touched.

Next Task: task-002

================================================================================
TASK-002: Refactor changie.yml to use batch auto instead of hardcoded alpha version
Date: 2026-05-11T14:00:00Z
================================================================================

Summary:
Removed the 'Compute next pre-release versions' step from `.github/workflows/changie.yml`
entirely (it parsed `alpha.[0-9]*` filenames and emitted `steps.prerelease` output no
longer needed). Changed the `changie batch` invocation from the hardcoded
`batch v0.1.0 --project neo4j-cli --prerelease <alpha.N>` form to
`batch auto --project neo4j-cli` so changie infers the next semver from the highest-kind
change fragment present.

Files Changed:
- .github/workflows/changie.yml (modified: removed pre-release compute step, updated batch args)

Key Decisions:
- Dropped the step entirely rather than no-op-ing it; no downstream steps referenced steps.prerelease.
- `batch auto` is sufficient — changie resolves the next version from existing version files and the kind of unreleased entries.

Next Task: task-003

================================================================================
TASK-003: Author Major-kind changie entry for the v1.0.0 GA release
Date: 2026-05-11T15:00:00Z
================================================================================

Summary:
Created `.changes/unreleased/neo4j-cli-Major-20260511-150000.yaml` with
`project: neo4j-cli`, `kind: Major`, and `body: GA release of neo4j-cli`.
This entry causes `changie batch auto --project neo4j-cli` to bump the major
version from `v0.1.0-alpha.12` → `v1.0.0`.

Files Changed:
- .changes/unreleased/neo4j-cli-Major-20260511-150000.yaml (created)

Key Decisions:
- Used the standard changie YAML fragment format matching existing entries.
- No existing unreleased files were removed or modified.

Next Task: All tasks complete
