## TASK-001 — 2026-05-18T09:50:00Z
Scaffold `desktopclient` package: DbmsInfo/EnvJSON/Credentials types, env JSON loader, port probe (44222..44232 on /fastify/api-docs, ≤200ms per probe), data-dir resolver (NEO4J_DESKTOP_DATA_PATH → env JSON relateDataPath → per-OS default), salt loader. Hermetic tests via afero.MemMapFs + httptest.
Files: +neo4j-cli/internal/desktopclient/types.go, +neo4j-cli/internal/desktopclient/types_test.go, +neo4j-cli/internal/desktopclient/envconfig.go, +neo4j-cli/internal/desktopclient/envconfig_test.go, +neo4j-cli/internal/desktopclient/discovery.go, +neo4j-cli/internal/desktopclient/discovery_test.go
Decisions: test seams as package vars (userConfigDirFn/homeDirFn/goosFn/probeHostFn/httpClientFn) following dotenv pkg pattern; salt loader trims trailing whitespace; LoadEnvs returns []EnvJSON{} when dir missing (no error)

## TASK-002 — 2026-05-18T10:30:00Z
desktopclient HTTP client + JWT auth: HS256 token keyed on `<salt>-<origin>-<clientID>` with 7d iat/exp claims; fresh UUID v4 per Client sent as X-Client-Id + X-API-Token on every call; 30s per-request context.WithTimeout. ListDbmss/GetDbms/CreateDbms (omits `edition`)/DeleteDbms/StartDbms/StopDbms. Probe miss / mid-request EOF / context deadline → canonical REQ-F-008 unreachable text (exported UnreachableError); 401 → REQ-F-010 auth-error w/ restart hint; 5xx → REQ-F-024 body truncated to 200 chars; other 4xx surfaces body. golang-jwt/jwt/v5 added as direct dep. Hermetic tests via httptest.NewServer that verify the JWT with the composite key.
Files: +neo4j-cli/internal/desktopclient/client.go, +neo4j-cli/internal/desktopclient/client_test.go, ~go.mod, ~go.sum
Decisions: CreateDbmsRequest struct has no Edition field by design (callers physically cannot pass one); httpDoFn/uuidNewFn/nowFn package vars for test seams matching existing pattern

## TASK-003 — 2026-05-18T10:55:00Z
desktopclient.GetCredentials(ctx, dbmsID) → GET /credentials/dbms:<id>. (*Credentials, nil) on populated 200; (nil, nil) on JSON `null` body (legacy DBMS / safeStorage unavailable); 401/404/5xx reuse the shared do() error mapping. Tests cover all four shapes hermetically (populated, null+trailing-newline, 404 body surfacing, 401 auth-error mapping, 5xx truncation).
Files: ~neo4j-cli/internal/desktopclient/client.go, ~neo4j-cli/internal/desktopclient/client_test.go
Decisions: tolerate trailing whitespace around `null` (bytes.TrimSpace) — Desktop's JSON encoder may add a newline; path uses literal `dbms:` colon (key namespace, not URL separator) per impl.md

## TASK-004 — 2026-05-18T11:30:00Z
Scaffold `desktop` cobra parent + `env` subtree + `env list` leaf; mount in app.go. Parent registers persistent `--port` flag (default 0 = scan range). `env list` reads env JSONs via desktopclient.LoadEnvs, counts `dbms-*` subdirs under each env's `<relateDataPath>/dbmss/`, best-effort probes for `(live)` suffix on matching `httpOrigin`. Probe failures don't propagate — env list runs without Desktop. `active` rendered as string (`"true"`/`"false"`/`"true (live)"`) so the live tag rides on the same column for table+JSON. Hermetic tests cover empty dir, JSON columns, live suffix, --port propagation, missing dataPath, table headers, no-args.
Files: +neo4j-cli/internal/subcommands/desktop/desktop.go, +neo4j-cli/internal/subcommands/desktop/env.go, +neo4j-cli/internal/subcommands/desktop/env_list.go, +neo4j-cli/internal/subcommands/desktop/env_list_test.go, ~neo4j-cli/app/app.go
Decisions: probe wrapped in `envListProbeFn` package var seam so tests neither hit the network nor depend on a Desktop instance; `originsMatch` falls back to `:<port>` tail-comparison so loopback aliases (`localhost` vs `127.0.0.1`) still match. TestGenerator_RoundTrip drift is expected here — task-011 regenerates the bundle.

## TASK-005 — 2026-05-18T12:00:00Z
Add `desktop list` leaf and shared `newDesktopClient` helper (resolve data dir → load salt → probe port → sign JWT). List renders default table columns (id, name, version, status, connectionUri) via PrintBodyMap; MarshalJSON emits the full DbmsInfo array so `--format json` returns every wire field. On empty result, walks env JSONs and writes REQ-F-001 stderr hint if any dormant env has on-disk `dbms-*` dirs (stderr-only — JSON stdout stays valid). Constructor failures (missing salt, probe miss) map to canonical REQ-F-008 unreachable text. Tests cover JSON full payload, table columns, empty+dormant hint, empty+no-dormant no-hint, dormant-but-empty no-hint, non-empty no-hint, JSON not polluted, unreachable error mapping, --port propagation, table-header-on-empty.
Files: +neo4j-cli/internal/subcommands/desktop/helpers.go, +neo4j-cli/internal/subcommands/desktop/list.go, +neo4j-cli/internal/subcommands/desktop/list_test.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go
Decisions: `newDesktopClientFn` exported as a package-level seam so future create/delete/start/stop leaves share the same construction path + same test override surface; missing salt maps to REQ-F-008 (Desktop-not-running class) rather than a distinct error so users see one canonical hint. TestGenerator_RoundTrip drift remains expected — deferred to task-011.

## TASK-006 — 2026-05-18T13:00:00Z
Add `desktop create` leaf with `--name`/`--version`/`--password`/`--no-wait` flags (no `--edition`). RunE: shared newDesktopClientFn → CreateDbms (body omits `edition`) → StartDbms → poll GetDbms every 1s up to 30s for `status=online` (skipped under `--no-wait`). Annotations write=true. Renders final DbmsInfo via PrintBodyMap (table+toon get default columns id/name/version/status/connectionUri; JSON emits full DbmsInfo via custom MarshalJSON). Required-flag validation surfaces clierr.NewUsageError messages. Tests: post-body-omits-edition, default polls until online (3 GETs), --no-wait skips polling, poll timeout via clock seam returns timed-out error with last-seen status, no credentials.json write on success, annotated write=true, no --edition flag, --port propagates, unreachable canonical error, required-flag triplet.
Files: +neo4j-cli/internal/subcommands/desktop/create.go, +neo4j-cli/internal/subcommands/desktop/create_test.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go
Decisions: createNowFn + createPollSleepFn package-var seams make the 30s deadline path testable in <1ms; ctxSleep spawns a goroutine per poll so cancelled contexts return immediately (time.Sleep is uninterruptible). TestGenerator_RoundTrip drift remains expected — deferred to task-011.

## TASK-007 — 2026-05-18T13:30:00Z
Add `desktop start <id>` and `desktop stop <id>` leaves with optional `--wait` (poll until online/offline, 1s cadence, 30s ceiling, exits non-zero with last-seen status on timeout). Both write=true (require --rw); positional id required (cobra.ExactArgs(1)); default no-wait returns immediately with a minimal {id} envelope, `--wait` upgrades the render to the full DbmsInfo (same column set as list/create). Pulled the poll loop + cancel-aware sleep out of create.go into a shared `pollUntilStatus(ctx, client, id, target)` + `ctxSleep` in helpers.go so create / start / stop share one cadence + timeout message shape; create.go's pollUntilOnline shrinks to a one-line shim. Tests cover: missing id, no-wait skips poll (1 POST, 0 GETs), --wait polls until target on 3rd GET, --wait timeout via createNowFn seam returns "timed out" w/ target+last-seen status, no credentials.json mutation, write annotation, --port propagation, unreachable canonical error.
Files: +neo4j-cli/internal/subcommands/desktop/start.go, +neo4j-cli/internal/subcommands/desktop/start_test.go, +neo4j-cli/internal/subcommands/desktop/stop.go, +neo4j-cli/internal/subcommands/desktop/stop_test.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go, ~neo4j-cli/internal/subcommands/desktop/create.go, ~neo4j-cli/internal/subcommands/desktop/helpers.go
Decisions: shared `statusPoller` interface in helpers.go lets future helper-only unit tests stub GetDbms without an httptest server; timeout message is generic ("re-run without waiting") so it reads cleanly for both create's --no-wait and start/stop's drop-the---wait paths. TestGenerator_RoundTrip drift remains expected — deferred to task-011.

## TASK-008 — 2026-05-18T14:00:00Z
Add `desktop delete <id>` leaf with `--yes` flag. TTY+no-yes: GetDbms then prompt `Delete DBMS "<name>" (<id>)? [y/N]` via cmd.OutOrStdout/cmd.InOrStdin; only `y`/`yes` (case-insensitive, trimmed) proceeds. Non-TTY+no-yes: clierr.NewUsageError citing --yes. Either-yes path skips the confirmation GET and goes straight to DeleteDbms. write=true (require --rw); never touches credentials.json. New `deleteStdinIsTTYFn` package-var seam (default `term.IsTerminal(os.Stdin.Fd())`) + `SetDeleteStdinIsTTYFnForTest` exported helper, mirroring `update/swap.go`'s stdinIsTTYFn pattern. Tests cover: missing id, non-TTY+no-yes refusal (zero API calls), non-TTY+yes (delete without GET), TTY+yes skips prompt + suppresses prompt stdout, TTY+yes/Y/YES/yes-with-whitespace all proceed, TTY+n/N/no/empty/garbage all abort with zero DELETEs, TTY+EOF aborts, name fallback to id when Desktop returns empty name, no credentials.json mutation on success or 404, write annotation, --port propagation, unreachable canonical error.
Files: +neo4j-cli/internal/subcommands/desktop/delete.go, +neo4j-cli/internal/subcommands/desktop/delete_test.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go
Decisions: confirmation prompt goes through cmd.OutOrStdout/cmd.InOrStdin (not os.Stdin) so tests feed input via the standard cobra `SetIn`/`SetOut` plumbing without touching real fds; deleteHelper defaults the TTY seam to true so any test that forgets to opt INTO non-TTY fails noisy (instead of taking a silent refusal path). TestGenerator_RoundTrip drift remains expected — deferred to task-011.

## TASK-009 — 2026-05-18T14:30:00Z
Add `desktop install` orchestration leaf: detect → manifest → SHA-512 verify → delegate. Flags `--force`/`--target-dir`/`--dry-run` (writes, requires --rw). REQ-F-016 detect runs BEFORE any HTTP: macOS stats `/Applications/Neo4j Desktop 2.app` + `~/Applications/...` then regex-parses CFBundleShortVersionString from Info.plist; Linux globs `~/Applications/neo4j-desktop-*.AppImage` and lifts version from the filename; Windows stats `%LOCALAPPDATA%\Programs\neo4j-desktop` and best-effort lifts version from package.json. Manifest fetch + electron-builder YAML parse (`gopkg.in/yaml.v3` indirect→used directly), URL resolved against manifest dir, artifact picked per OS (.dmg / .AppImage / .exe — NEVER macOS .zip Squirrel vehicle). Base64-decoded SHA-512 vs streamed `crypto/sha512.Sum512` of on-disk tempfile; mismatch deletes tempfile + clierr.NewFatalError. `--dry-run` prints manifest+artifact URLs and exits before download; `--force` skips detection; Linux arm64 hard-errors with deployment-center URL BEFORE network. Per-OS install action delegated through `runInstallActionFn` seam (task-010 fills in DMG/AppImage/NSIS). REQ-F-021 no-license-prompt, REQ-F-022 stderr next-step hint (no `open -a`). Tests cover all acceptance criteria hermetically via httptest+seam overrides.
Files: +neo4j-cli/internal/subcommands/desktop/install.go, +neo4j-cli/internal/subcommands/desktop/install_detect.go, +neo4j-cli/internal/subcommands/desktop/install_test.go, +neo4j-cli/internal/subcommands/desktop/install_detect_test.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go
Decisions: `InstallPlan` + `InstalledHit` exported (type alias for the latter) so tests in `_test` package can construct/inspect plans without re-casting at every seam call; per-OS detection uses a regex over Info.plist + filename + package.json (no plist or registry library) — keeps the dependency tree flat; default `runInstallActionFn` returns a clear "not yet wired" error so a user invoking the leaf today gets actionable feedback rather than silent no-op while task-010 is in flight; macOS detection test on a dev host must pin stat so `/Applications/Neo4j Desktop 2.app` (which may genuinely exist) doesn't shadow the tmp-rooted fixture. TestGenerator_RoundTrip bundle drift remains expected — deferred to task-011.

## TASK-010 — 2026-05-18T15:00:00Z
Per-OS install actions wired behind `realRunInstallAction` dispatcher. macOS: `hdiutil attach -nobrowse -noverify -noautoopen` → glob `/Volumes/Neo4j Desktop 2*` → pure-Go recursive copy of the `.app` bundle → defer `hdiutil detach -quiet`; EACCES on `/Applications` falls back to `~/Applications` (created if missing) only when the user did NOT pass `--target-dir` explicitly. Linux: copies AppImage to `<target>/neo4j-desktop-<version>.AppImage`, `chmod 0755`, writes `~/.local/share/applications/neo4j-desktop-2.desktop` with absolute `Exec=`; XDG dir created if missing. Windows: `<artifact.exe> /S` plus `/D=<target>` when `--target-dir` was passed (LAST argv per NSIS convention). All three actions write the resolved on-disk path into a package-private `lastInstalledTargetDir` cell consulted by the cobra leaf for the REQ-F-021 breadcrumb so macOS users see the actual fallback dir. Default `runInstallActionFn` swapped from the task-009 "not yet wired" stand-in to `realRunInstallAction`. Bundle drift unchanged (still pre-existing from task-004..009; task-011 regenerates).
Files: +neo4j-cli/internal/subcommands/desktop/installer_mac.go, +neo4j-cli/internal/subcommands/desktop/installer_mac_test.go, +neo4j-cli/internal/subcommands/desktop/installer_lin.go, +neo4j-cli/internal/subcommands/desktop/installer_lin_test.go, +neo4j-cli/internal/subcommands/desktop/installer_win.go, +neo4j-cli/internal/subcommands/desktop/installer_win_test.go, ~neo4j-cli/internal/subcommands/desktop/install.go
Decisions: Linux file is `installer_lin.go` (not `_linux.go`) — Go's filename rule `*_GOOS.go` would auto-restrict `installer_linux.go` to GOOS=linux, blocking the seam-mocked cross-platform tests on macOS/Windows dev hosts; `_lin`/`_mac`/`_win` is a deliberate parallel naming that keeps all three buildable on every host. macOS uses a pure-Go recursive copy (not `exec.Command "cp"`) so symlink/perm preservation is uniform across hosts and the seam pattern stays consistent; macOS mount-point lookup is glob-based (not stdout parsing) because Apple has reshaped `hdiutil attach`'s output format historically. NSIS `/D=<path>` MUST be the LAST argv entry — encoded as a literal append at the end of the args slice with a code-comment to prevent a future contributor from inserting more args after it.

## TASK-011 — 2026-05-18T15:30:00Z
Regenerate neo4j-cli skill bundle: `go generate ./neo4j-cli/internal/skill/...` produced only the expected `references/desktop.md` (215 lines, 8 leaves: create/delete/env/env list/install/list/start/stop) plus a one-line addition to SKILL.md TOC. Aura bundle regeneration was a no-op. All gates green: make fmt-check, make test, make lint, make license-check, TestGenerator_RoundTrip, TestAllLeafCommands_HaveExamples.
Files: +neo4j-cli/internal/skill/bundle/references/desktop.md, ~neo4j-cli/internal/skill/bundle/SKILL.md

## TASK-012 — 2026-05-18T16:00:00Z
Wire `neo4j-cli query --credential <name>` Desktop fall-through (REQ-F-026): when `cfg.Credentials.Dbms.Get` misses, call `resolveDesktopCredentialFn` which probes Desktop, lists DBMSes, matches by name OR id, and fetches `GetCredentials`. Hit returns a synthesised *conn via `buildConnFromDesktopMatch` (connectionUri→uri, creds.username/password, defaultDatabase). Persisted-cred lookup wins on name collision (fall-through runs only after Dbms.Get fails). Probe miss / salt miss → (nil, nil) → user sees the existing not-found hint. Desktop-reachable-but-erroring (auth/5xx/decode) → surface the underlying canonical error. Null creds (legacy DBMS) → fall through to not-found error (task-013 layers REQ-F-028 prompt on top). Tests cover name match, id match, persisted-wins-on-collision (seam panics if reached), miss-everywhere, Desktop-error-surface, null-creds-falls-through, and a full-stack httptest run using rewriteToSrv to bypass the 44222..44232 scan range without changing production code.
Files: +neo4j-cli/query/desktop.go, +neo4j-cli/query/desktop_test.go, ~neo4j-cli/query/connect.go
Decisions: `resolveDesktopCredentialFn` package-var seam mirrors the existing `runStatementResponseFn` / `driverOpener` pattern in this package; cmd.Context() guarded with context.Background() fallback because cobra returns nil when neither Execute nor SetContext has run (and unit tests skip both); the production resolver swallows infra errors (data-dir lookup, salt-load, probe miss) and returns (nil, nil) so the user keeps the familiar "credential dbms list" surface — only reachable-but-erroring paths bubble up.

## TASK-013 — 2026-05-18T16:30:00Z
Layer REQ-F-027/028/029 on the task-012 fall-through. (027) New `desktopAutoSelectFn` seam + production `desktopAutoSelect` (probe → list → single-DBMS check → GetCredentials) wired into resolveConn's `!hasStoredCred && explicitCount == 0` branch; emits stderr breadcrumb `Using Desktop DBMS "<name>" (<id>). Pass --credential to override.` 0 or 2+ DBMSes → (nil, nil) → existing built-in defaults. (028) Desktop match with null creds: (1) retry persisted under `match.dbms.Name` when it differs from credName (covers `--credential <id>` registered persisted under name); (2) TTY → `promptPassword(cmd)` + build conn with `neo4j` default username; (3) non-TTY → `clierr.NewFatalError` with --password/credential-dbms-add/Reset-password hint. (029) Persisted miss + Desktop fall-through miss → single `clierr.NewFatalError` naming both fallbacks (credential dbms add, start Neo4j Desktop 2). Refactored persisted-cred → *conn build into shared `buildConnFromPersistedCred` helper. Extracted shared probe-chain into `newDesktopFallthroughClient` so named-lookup and auto-select bootstrap identically. Tests cover: REQ-F-028 non-TTY fatal, TTY prompt, persisted-retry-wins (panics-if-prompt-fires); REQ-F-027 auto-select + breadcrumb, zero/multiple fall-through, blocked by stored default, blocked by explicit param; updated MissEverywhereReturnsCombinedError + unknown-credential test for new error wording.
Files: ~neo4j-cli/query/connect.go, ~neo4j-cli/query/connect_test.go, ~neo4j-cli/query/desktop.go, ~neo4j-cli/query/desktop_test.go
Decisions: REQ-F-029 message uses `clierr.NewFatalError` instead of `fmt.Errorf` to satisfy staticcheck ST1005 (errors-end-with-punctuation) while keeping the PRD's user-facing wording verbatim; `desktopAutoSelectFn` is a separate seam from `resolveDesktopCredentialFn` (different shape — no credName, returns nil when count != 1) so tests can independently pin each branch.

## TASK-014 — 2026-05-18T17:00:00Z
Add changie Minor changelog entry for the CLI-32 desktop subcommand + query Desktop fall-through; final cross-platform gates (fmt-check, lint, test, license-check) green; generate-check shows no bundle drift (only the harness-driven task-yaml status flip, expected per AGENTS.md).
Files: +.changes/unreleased/neo4j-cli-Minor-20260518-113428.yaml

## TASK-015 — 2026-05-18T17:30:00Z
Enrich `desktop list` via concurrent GetDbms fan-out (errgroup, SetLimit=8) to fill status/version/edition that Desktop 2.1.4's GET /dbmss slim shape omits. Per-entry GET failures keep slim row + emit stderr-only warning; output order pinned to ListDbmss order via indexed slot write. Existing list_test.go handlers updated to mirror real Desktop shape (slim list + per-id full payload). New tests: enrichment-failure (slim row preserved + stderr warning), order-preservation (12 entries, fastest-last sleep), concurrency-bound (50 entries, max-in-flight ≤8 via mutex counter). go.mod promotes golang.org/x/sync from indirect → direct; tidy also de-indirects gopkg.in/yaml.v3 (pre-existing direct use in install.go from task-009). No skill-bundle regen needed.
Files: ~neo4j-cli/internal/subcommands/desktop/list.go, ~neo4j-cli/internal/subcommands/desktop/list_test.go, ~go.mod
Decisions: `listEnrichClient` is a tiny interface (not a func seam) so the production path passes `*desktopclient.Client` directly without indirection; per-entry warnings go through a sync.Mutex-guarded Fprintf because errgroup goroutines share the cmd.ErrOrStderr writer; index-based slot writes (`enriched[i] = *info`) preserve ListDbmss order regardless of goroutine completion order.

## TASK-016 — 2026-05-18T18:00:00Z
Enrich `desktop start` / `desktop stop` non-`--wait` output via a single post-call GetDbms. Desktop's POST /dbmss/:id/{start,stop} return stringified shell output, not DbmsInfo, leaving the previous no-wait render with only `id`. New shared `fetchForRender` in helpers.go (single GET via `statusPoller` interface, stderr-only warning + `(nil, false)` on failure) keeps start/stop wording identical. On enrichment failure the slim `{id}` envelope is preserved so exit stays 0 (lifecycle call already succeeded). `--wait` path untouched: pollUntilStatus still owns the render. Renamed `TestStart_NoWait_DoesNotPoll`/`TestStop_NoWait_DoesNotPoll` to `…_EnrichedSingleGet` asserting exactly 1 POST + 1 GET + populated DbmsInfo on stdout. New `…_EnrichmentFail_FallsBackToSlimEnvelope` tests cover the 500-on-GET path. No cobra-tree change → no skill-bundle regen.
Files: ~neo4j-cli/internal/subcommands/desktop/helpers.go, ~neo4j-cli/internal/subcommands/desktop/start.go, ~neo4j-cli/internal/subcommands/desktop/stop.go, ~neo4j-cli/internal/subcommands/desktop/start_test.go, ~neo4j-cli/internal/subcommands/desktop/stop_test.go
Decisions: reuse the existing `statusPoller` interface (already exposes GetDbms) instead of introducing a parallel single-GET seam — keeps the helper surface minimal; warning is non-`Error` prefixed (`Warning:`) so users distinguish enrichment-only failures from lifecycle-call failures.

## TASK-017 — 2026-05-18T18:30:00Z
Pre-flight one-DBMS-at-a-time conflict check in `desktop start` + `desktop create`, with `--force` escape hatch. New `assertNoOtherRunning(ctx, client, selfID, selfName, actionVerb, warnOut)` helper in helpers.go: ListDbmss → enrichDbmsList fan-out (reuses task-015 pattern, 8-in-flight bound) → scan for any `Status=="started"` entry whose `ID != selfID` → clierr.NewFatalError naming the running DBMS and suggesting `desktop stop <id>` or `--force`. `start` passes `id` as selfID so re-starting an already-running DBMS is idempotent; `create` passes selfID="" + selfName=name and runs BEFORE the POST /dbmss call so a blocked request leaves no orphan on disk. New `preflightClient` interface = `listEnrichClient` + `ListDbmss`; new `dbmsStatusStarted` constant ("started", distinct from existing "online" used by post-start poll). Skill bundle regenerated — `--force` rendered into references/desktop.md for both leaves. Existing tests opt-into `--force` to bypass the now-mandatory list call; new tests cover blocks-when-another-running, same-id-noop, no-others-proceeds, --force-bypasses (no GET /dbmss) for both leaves; create-blocks test verifies POST /dbmss never fires.
Files: ~neo4j-cli/internal/subcommands/desktop/helpers.go, ~neo4j-cli/internal/subcommands/desktop/start.go, ~neo4j-cli/internal/subcommands/desktop/create.go, ~neo4j-cli/internal/subcommands/desktop/start_test.go, ~neo4j-cli/internal/subcommands/desktop/create_test.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: `dbmsStatusStarted` constant is `"started"` (live-smoked against Desktop 2.1.4), intentionally distinct from `dbmsStatusOnline = "online"` used during post-start poll — Desktop transitions online→started; documented at the constant. selfDescriptor/formatDbmsRef helpers keep the error wording stable across `start <id>` (no name yet) and `create --name X` (no id yet) entry shapes; `%q` quoting keeps the rendered output paste-safe.

## TASK-018 — 2026-05-18T19:00:00Z
Redefine `--force` on `desktop start` / `desktop create` from "skip the pre-flight check" to "stop the conflicting DBMS first, then proceed". Previously --force just bypassed assertNoOtherRunning; Desktop then silently no-op'd the second start (port 7687 collision) and the user saw a UI flicker. New `resolveConflicting(ctx, client, selfID, warnOut)` helper in helpers.go: ListDbmss → enrichDbmsList → find conflicting `Status=="started"` entry with `ID != selfID` → emit stderr breadcrumb `Stopping DBMS <ref> to free port 7687...` → StopDbms → pollUntilStatus(..., dbmsStatusOffline) (30s) → return stopped id. Stop or poll failure → clierr.NewFatalError naming the DBMS, lifecycle call aborted. `start` calls resolveConflicting under --force (selfID=id, idempotent on same-id). `create` calls it under --force before CreateDbms (selfID="" — no orphan if stop fails). --force flag Usage on both leaves now describes the stop-then-start semantics. Skill bundle regenerated (new flag Usage rendered into references/desktop.md). Existing tests that used --force as a bypass updated to either drop --force (default pre-flight path is fine when nothing else runs) or add `GET /fastify/api/dbmss` returning `[]` so the now-mandatory list call resolves cleanly. New tests on both leaves: no-conflict-no-stop, stops-conflicting-then-proceeds (ordering asserted via recording handler), stop-failure-aborts, stop-poll-timeout-aborts (start only).
Files: ~neo4j-cli/internal/subcommands/desktop/helpers.go, ~neo4j-cli/internal/subcommands/desktop/start.go, ~neo4j-cli/internal/subcommands/desktop/create.go, ~neo4j-cli/internal/subcommands/desktop/start_test.go, ~neo4j-cli/internal/subcommands/desktop/create_test.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: reuse `dbmsStatusOffline = "offline"` (already used by `stop --wait` post-stop poll) instead of introducing a new dbmsStatusStopped constant — consistent post-stop polling across leaves; new `resolveClient` interface = `preflightClient` + `StopDbms` keeps the seam minimal.

## TASK-019 — 2026-05-18T19:30:00Z
Fix wire-format mismatch: poll for `started`/`stopped`, not `online`/`offline`. Consolidated status constants to a single pair in helpers.go (`dbmsStatusStarted="started"`, `dbmsStatusStopped="stopped"`); removed duplicate `dbmsStatusOnline="online"` from create.go and `dbmsStatusOffline="offline"` from stop.go. Renamed `pollUntilOnline` → `pollUntilStarted`. Updated Long/Example/flag-Usage text on create/start/stop (status=online→started, status=offline→stopped) and every httptest fixture in desktop/*_test.go (json `"online"`→`"started"`, `"offline"`→`"stopped"`) plus the corresponding test-side assertions/comments. Bundle regenerated to pick up the new help text; resolveConflicting now polls for `dbmsStatusStopped` so --force will actually proceed to StartDbms against live Desktop.
Files: ~neo4j-cli/internal/subcommands/desktop/helpers.go, ~neo4j-cli/internal/subcommands/desktop/create.go, ~neo4j-cli/internal/subcommands/desktop/start.go, ~neo4j-cli/internal/subcommands/desktop/stop.go, ~neo4j-cli/internal/subcommands/desktop/create_test.go, ~neo4j-cli/internal/subcommands/desktop/start_test.go, ~neo4j-cli/internal/subcommands/desktop/stop_test.go, ~neo4j-cli/internal/subcommands/desktop/list_test.go, ~neo4j-cli/internal/subcommands/desktop/delete_test.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: chose the consolidating refactor over the minimum-fix path (two literal flips) so future readers can't reintroduce `online`/`offline` aliases — added a no-aliases note to the const doc; help text touched so bundle regen IS needed (task description said no regen, but the Long/flag Usage references status=X surfaced in references/desktop.md).

## TASK-020 — 2026-05-18T20:00:00Z
Make `desktop create --version` optional. Added `DbmsVersion` type + `ListDbmsVersions(ctx)` to desktopclient (GET /dbmss/versions, reuses shared do() error mapping). Added `pickLatestStableEnterprise` in create.go: filter enterprise + stable (no prerelease), sort by semver desc with cached>online tie-break. When `--version` is omitted the leaf calls versions endpoint, picks the highest stable, sets version, emits `Using Neo4j enterprise <v> (<origin>)` to stderr. Empty/all-prerelease catalog → fatal error pointing at `--version <vX>`. Skill bundle regenerated. golang.org/x/mod was already a direct dep so no go.mod bump needed.
Files: ~neo4j-cli/internal/desktopclient/types.go, ~neo4j-cli/internal/desktopclient/client.go, ~neo4j-cli/internal/desktopclient/client_test.go, ~neo4j-cli/internal/subcommands/desktop/create.go, ~neo4j-cli/internal/subcommands/desktop/create_test.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: `golang.org/x/mod/semver` rejects calendar versions like `2026.04.0` (leading zero in `04` is invalid per strict semver), so introduced `normaliseSemver` + `stripLeadingZeros` to canonicalise components before passing to semver — both 5.x and YYYY.MM series sort correctly afterwards; exposed `PickLatestStableEnterpriseForTest` test-only shim so tie-break / semver-ordering edge cases run as focused unit tests without an httptest server.

## TASK-021 — 2026-05-18T20:30:00Z
Replace `desktop delete` post-delete table render (which showed the pre-delete `status=stopped` snapshot — confusing for a row that no longer exists) with a confirmation. Default/table/toon: one-line `Deleted DBMS "<name>" (<id>).` on stdout; `--format json`: `{"id","name","deleted":true}` (drops the misleading `status` field, fixed 3-key shape for scripts). Dropped `dbmsDeleteResult` + `dbmsDeleteFields` + `desktopclient` import from delete.go. TTY prompt + --yes + non-TTY usage semantics intact. Bundle regenerated for the Example-line wording update.
Files: ~neo4j-cli/internal/subcommands/desktop/delete.go, ~neo4j-cli/internal/subcommands/desktop/delete_test.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: route by `output.ResolveOutput(cmd, cfg)` directly (no ResponseData wrapper) — delete is a single fact, not a resource worth feeding through PrintBodyMap; toon shares the table one-liner because a structured doc adds nothing over the line.

## TASK-022 — 2026-05-21T14:00:00Z
Hard subcommand restructure: `desktop create/delete/start/stop` move under new `desktop dbms` parent; new `desktop dbms list` (DBMS-only) + `desktop connection list` (connection-only) single-resource leaves; composed `desktop list` retained. No backwards-compat aliases. New `dbms` package owns lifecycle helpers (newDesktopClient, pollUntilStatus, status constants, conflict helpers, enrichDbmsList) so the `desktop` parent imports downward without cycle; `desktop/helpers.go` keeps a parent-local newDesktopClient seam for the composed `list`. Exported `dbms.RenderDbmsTable` + `connection.RenderConnectionsTable` so composed `desktop list` shares one renderer per section. Error/help text updated to reference `desktop dbms stop`; query/desktop.go REQ-F-101 hint follows. Bundle regenerated: new `## neo4j-cli desktop dbms` and `### neo4j-cli desktop connection list` sections in `references/desktop.md`; old `## neo4j-cli desktop {create,delete,start,stop}` sections gone (single-file bundle, not per-leaf — task PRD wording about `references/desktop_dbms*.md` aspirational). e2e tests + fixture comments updated to `desktop dbms <verb>` tuples. All gates green.
Files: +neo4j-cli/internal/subcommands/desktop/dbms/dbms.go, +neo4j-cli/internal/subcommands/desktop/dbms/helpers.go, +neo4j-cli/internal/subcommands/desktop/dbms/list.go, +neo4j-cli/internal/subcommands/desktop/dbms/create.go, +neo4j-cli/internal/subcommands/desktop/dbms/delete.go, +neo4j-cli/internal/subcommands/desktop/dbms/start.go, +neo4j-cli/internal/subcommands/desktop/dbms/stop.go, >neo4j-cli/internal/subcommands/desktop/create_test.go=>neo4j-cli/internal/subcommands/desktop/dbms/create_test.go, >neo4j-cli/internal/subcommands/desktop/delete_test.go=>neo4j-cli/internal/subcommands/desktop/dbms/delete_test.go, >neo4j-cli/internal/subcommands/desktop/start_test.go=>neo4j-cli/internal/subcommands/desktop/dbms/start_test.go, >neo4j-cli/internal/subcommands/desktop/stop_test.go=>neo4j-cli/internal/subcommands/desktop/dbms/stop_test.go, +neo4j-cli/internal/subcommands/desktop/connection/list.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go, ~neo4j-cli/internal/subcommands/desktop/list.go, ~neo4j-cli/internal/subcommands/desktop/helpers.go, ~neo4j-cli/internal/subcommands/desktop/connection/connection.go, ~neo4j-cli/internal/desktopclient/client.go, ~neo4j-cli/query/desktop.go, ~neo4j-cli/query/desktop_test.go, ~test/e2e/desktop/desktop_test.go, ~test/e2e/desktop_fixture/handlers.go, ~test/e2e/desktop_fixture/scenario.go, ~test/e2e/desktop_fixture/state.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md, -neo4j-cli/internal/subcommands/desktop/create.go, -neo4j-cli/internal/subcommands/desktop/delete.go, -neo4j-cli/internal/subcommands/desktop/start.go, -neo4j-cli/internal/subcommands/desktop/stop.go
Decisions: dbms package owns the shared lifecycle helpers (newDesktopClient seam, enrichDbmsList, pollUntilStatus, status constants, assertNoOtherRunning, resolveConflicting) so `desktop` (parent) imports downward into `dbms` without an import cycle — `desktop/helpers.go` keeps its own newDesktopClient seam for the composed `desktop list` leaf so each test surface has an isolated seam; connection subpackage stays self-contained with its existing seam pattern, plus a new exported RenderConnectionsTable that the composed view delegates to.

## TASK-023 — 2026-05-21T15:30:00Z
Plugin wire surface on `desktopclient`: `DbmsPlugin` type + 4 methods (ListInstalledPlugins / ListAvailablePlugins / InstallPlugin / UninstallPlugin). Install + Uninstall apply 120s `pluginWriteTimeout` (REQ-F-040) via a refactored `doRaw` low-level + `doPlugin` status-code-mapper that disambiguates 404s into `ErrDbmsNotFound` / `ErrPluginNotFound` sentinels (REQ-F-041 / REQ-F-042) by lowercase substring match on the body. Existing `do` retained for the non-plugin endpoints; status mapping logic inlined back to one place after collapsing the unused `doWithTimeout` helper. Empty plugin arrays return `[]DbmsPlugin{}` (not nil) so JSON renders `[]`. Timeout assertions via `httpDoFn` seam (`captureDeadlineSeam`) inspecting the outbound request context deadline — server-side `r.Context().Deadline()` is the fresh httptest context and can't observe the client-side budget. 91/91 desktopclient tests pass; all repo gates green.
Files: ~neo4j-cli/internal/desktopclient/types.go, ~neo4j-cli/internal/desktopclient/client.go, ~neo4j-cli/internal/desktopclient/client_test.go
Decisions: `doRaw` returns `(status, body, transportErr)` and is the only place that owns transport-level concerns (timeout, probe miss, EOF, deadline) — `do` and `doPlugin` are thin status-code-mapper wrappers on top, so the 120s plugin window AND the 404 sentinel disambiguation get the same canonical unreachable text for free; sentinel approach (`errors.Is`) chosen over body-string inspection at the leaf layer because leaves later (task-026/027) need to map BOTH error classes to distinct REQ-F-041/042 hints and substring matching from outside the package is brittle.

## TASK-024 — 2026-05-21T15:07:25Z
Add `neo4j-cli desktop dbms plugin` subtree (parent) + `plugin list <dbms-id>` leaf. Parent non-runnable, mounted on `desktop dbms` via `dbms.AddCommand(plugin.NewCmd(cfg))`; Long describes Plugin lifecycle / auto-restart UX. Leaf positional `<dbms-id>`, read-only (no `--rw`), reads via `client.ListInstalledPlugins`, renders via PrintBodyMap with columns `name, version, pendingRestart, filePath`; empty list yields `(none)` placeholder table row + `[]` for JSON; `ErrDbmsNotFound` → REQ-F-042 canonical error pointing at `desktop dbms list`. Per-leaf-subtree `newDesktopClientFn` seam mirrors the connection-package pattern (own seam, not shared). Tests: required-id, happy json, happy table, empty table, empty json, dbms-not-found, port-flag-propagates. TestGenerator_RoundTrip drift expected — deferred to task-030.
Files: +neo4j-cli/internal/subcommands/desktop/dbms/plugin/plugin.go, +neo4j-cli/internal/subcommands/desktop/dbms/plugin/list.go, +neo4j-cli/internal/subcommands/desktop/dbms/plugin/helpers.go, +neo4j-cli/internal/subcommands/desktop/dbms/plugin/list_test.go, ~neo4j-cli/internal/subcommands/desktop/dbms/dbms.go
Decisions: plugin subtree gets its own `newDesktopClientFn` seam (duplicates the data-dir→salt→probe→JWT chain that lives in dbms/helpers.go) rather than reusing dbms' seam, because per-subtree isolation prevents one test from accidentally pinning another subtree's seam — same convention as the existing connection subtree.

## TASK-025 — 2026-05-21T15:10:42Z
Add `desktop dbms plugin available <dbms-id>` leaf — read-only browse of the installable plugin catalog Desktop exposes via `GET /dbmss/:id/plugins/available`. Mirrors `plugin list` shape: positional `<dbms-id>`, no `--rw`, reuses `pluginListResult` + `renderPluginList` + `renderPluginTable` + `pluginListFields` from list.go (relate returns the same `DbmsPlugin` struct from both endpoints). `ErrDbmsNotFound` → REQ-F-042 canonical hint. Hermetic tests parallel list_test.go: required-id, happy json (3 plugins), happy table (column headers + row content), empty table (`(none)` placeholder), empty json (`[]`), dbms-not-found, --port propagation. TestGenerator_RoundTrip drift remains expected — deferred to task-030.
Files: +neo4j-cli/internal/subcommands/desktop/dbms/plugin/available.go, +neo4j-cli/internal/subcommands/desktop/dbms/plugin/available_test.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/plugin.go

## TASK-026 — 2026-05-21T17:30:00Z
Add `desktop dbms plugin install <dbms-id> <plugin>` write leaf with `--no-restart` opt-out + auto-restart (Stop → poll(stopped) → Start → poll(started)) helper. RunE: pre-op `GetDbms` captures running status (tolerated on failure — surfaced as status-unknown branch), `InstallPlugin` renders DbmsPlugin via PrintBodyMap (full wire shape on JSON, default columns name/version/pendingRestart/filePath on table+toon). Auto-restart decision matrix: started+!no-restart=restart, started+no-restart=manual hint, stopped=next-start hint, unknown=conservative status-unknown hint (no speculative restart). Restart failures wrap `restartErr` sentinel + downgrade to stderr warning + exit 0 per REQ-F-039 (plugin op not rolled back). `ErrPluginNotFound` → REQ-F-041, `ErrDbmsNotFound` → REQ-F-042. Pulled the dbms-not-found wording into a shared `dbmsNotFoundError` helper used by list/available/install. Plugin-local `dbmsStatusStarted`/`dbmsStatusStopped`/`pollUntilStatus`/`ctxSleep`/`pollNowFn`/`pollSleepFn` constants & seams duplicated from sibling `dbms` package to avoid import cycle (`dbms` mounts `plugin.NewCmd`). Tests: required-positionals, write-annotation, auto-restart-sequence (recording handler asserts GetDbms→Install→Stop→pollGET→Start→pollGET ordering + both breadcrumbs), stopped-skips-restart, --no-restart-skips-restart, plugin-not-found→REQ-F-041, dbms-not-found→REQ-F-042, stop-failure-warns-exit-0, start-failure-warns-exit-0, --port propagation. TestGenerator_RoundTrip drift remains expected — deferred to task-030.
Files: +neo4j-cli/internal/subcommands/desktop/dbms/plugin/install.go, +neo4j-cli/internal/subcommands/desktop/dbms/plugin/install_test.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/helpers.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/plugin.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/list.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/available.go
Decisions: pre-op GetDbms tolerated on failure (status-unknown branch emits a hint instead of failing the install) because `GetDbms` uses generic `do()` mapping — a 404 there surfaces as a generic 4xx, not `ErrDbmsNotFound`. The install path's `doPlugin` IS sentinel-aware, so a not-found DBMS surfaces cleanly through the install POST anyway. Conservative refusal to speculative-restart on unknown status — no auto Stop/Start unless we know status=started.

## TASK-027 — 2026-05-21T18:00:00Z
Add `desktop dbms plugin uninstall <dbms-id> <plugin>` write leaf — mirrors `install` shape: pre-op GetDbms → UninstallPlugin → optional auto-restart (Stop → poll(stopped) → Start → poll(started)) with `--no-restart` opt-out. Output is the dedicated `{name, uninstalled: true}` shape across formats (JSON exact + table/toon single-line `Uninstalled plugin "<name>".`) — no DbmsPlugin payload to render because relate's uninstall response carries only the plugin name. Idempotent at relate layer (REQ-F-038): uninstalling an already-removed plugin returns 200 with `{name}`, surfaced as normal success. Refactored shared `autoRestartIfRunning` to take an `effectVerb` (install passes "active", uninstall passes "removed") so the post-restart breadcrumb reads honestly for both callers. Tests cover required-positionals, write-annotation, auto-restart-sequence (recording handler asserts GetDbms→Uninstall→Stop→pollGET→Start→pollGET + breadcrumbs), stopped-no-restart-hint, --no-restart-skips-restart, idempotent-already-removed, plugin-not-found→REQ-F-041, dbms-not-found→REQ-F-042, stop-failure-warns-exit-0, --port propagation, default-format one-line confirmation. TestGenerator_RoundTrip drift remains expected — deferred to task-030.
Files: +neo4j-cli/internal/subcommands/desktop/dbms/plugin/uninstall.go, +neo4j-cli/internal/subcommands/desktop/dbms/plugin/uninstall_test.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/plugin.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/helpers.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/install.go
Decisions: dropped `id` from uninstall JSON shape — relate's response carries only the plugin name, the DBMS id is positional argv (not wire payload), so `{name, uninstalled: true}` is honest (delete keeps `id` because relate's delete response is a full DbmsInfo snapshot; uninstall is a single-field confirmation). Added `effectVerb` parameter to `autoRestartIfRunning` rather than duplicating the helper — single call site for each verb at compile time keeps the breadcrumb wording typed.

## TASK-028 — 2026-05-21T18:30:00Z
Wire 4 plugin endpoints into desktop e2e fixture: GET /plugins/installed, GET /plugins/available, POST /plugins/install, POST /plugins/uninstall. Added per-dbms `availablePlugins` + `installedPlugins` state (unexported, never leak into JSON), `clearPendingRestart` helper called on every start/stop cycle (REQ-F-043 JAR-vs-PID mtime simulation). Install path: idempotent (re-install returns existing entry), looks up by name OR FilePath match in available catalog, sets `pendingRestart=true` iff DBMS currently `started` (false on stopped). Uninstall path: always returns `{name}`, idempotent on missing plugin (REQ-F-038). 404 disambiguation routed via relate-shaped message bodies (`Could not find DBMS %q` vs `Could not find plugin %q`) so the production client's `classifyPluginNotFound` lands on the right sentinel. New `_scenario/plugin` admin endpoint with optional `available`/`installed` slices (nil keeps existing, empty slice clears). 9 new fixture self-tests cover seeded list, empty list, install pendingRestart-by-status, install on stopped DBMS, idempotent install, idempotent uninstall, start/stop pendingRestart flip, 404 disambiguation. Empty arrays serialise as `[]` (non-nil slice path). All 22 fixture tests pass; make fmt-check/lint/license-check all green; TestGenerator_RoundTrip drift remains expected — deferred to task-030.
Files: ~test/e2e/desktop_fixture/handlers.go, ~test/e2e/desktop_fixture/state.go, ~test/e2e/desktop_fixture/scenario.go, ~test/e2e/desktop_fixture/main_test.go
Decisions: kept plugin state unexported on the `dbms` struct so encoding/json automatically skips it (no need for a separate wire-shape projection); fixture's install supports BOTH name-match and FilePath-match against `availablePlugins` so the verbatim-forwarding path REQ-F-037 (tested in desktopclient unit tests) also works against e2e — keeps the fixture as simple as possible while staying compatible with downstream tests that exercise local-path installs.

## TASK-029 — 2026-05-21T17:45:00Z
Add desktop e2e plugin scenarios covering all 4 leaves end-to-end against the relate-shaped fixture (build tag e2e_desktop). New `scenarioPlug = "/_scenario/plugin"` constant + `seedPlugins` helper drives the fixture's `_scenario/plugin` injector to seed `available`/`installed` per DBMS. 15 new scenarios: list happy/empty/dbms-not-found (with --format table opt-in for the (none) placeholder since non-TTY default is JSON), available happy/empty, install happy-auto-restart (pendingRestart→false verified via follow-up list)/stopped-dbms-next-start-hint/--no-restart/plugin-not-found/dbms-not-found/desktop-unreachable, uninstall happy-auto-restart (plugin gone verified via follow-up list)/idempotent/--no-restart/dbms-not-found. Auto-restart paths assert ordered POST trace [install|uninstall, stop, start] via fixtureTrace + both stderr breadcrumbs (`Plugin change pending`, `restarting DBMS`, `DBMS restarted`, `plugin "apoc" is now active/removed`); no-restart paths assert NO stop/start in the trace. Auto-progress flag converges the production poll loop without arming per-id transitions. 51/51 e2e tests pass; make fmt-check/lint/license-check green; TestGenerator_RoundTrip drift remains expected — deferred to task-030.
Files: ~test/e2e/desktop/desktop_test.go
Decisions: empty-table assertion opts into --format table explicitly because non-TTY runs default to JSON (common/output.ResolveOutput); go-pretty uppercases column headers so the header check matches `NAME` (not `name`); breadcrumb substring assertions deliberately AVOID pinning the relative order of pre-op GET vs install/uninstall — the production code's ordering of those two is an implementation detail, only the lifecycle trio order matters for the auto-restart contract.

## TASK-030 — 2026-05-21T17:45:00Z
Closing task for the restructure + plugin work: regenerated neo4j-cli skill bundle via `go generate ./neo4j-cli/internal/skill/...` (single-file bundle adds 5 TOC entries + 5 plugin sections to `references/desktop.md`; no other drift). Added changie Minor changelog entry covering the desktop dbms restructure + the new plugin subtree (list/available/install/uninstall, auto-restart UX). All final gates green on Linux/macOS dev host: `make fmt-check && make lint && make test && make license-check`; second-run `go generate ./...` produced zero drift (generate-check would pass on a clean tree).
Files: ~neo4j-cli/internal/skill/bundle/references/desktop.md, +.changes/unreleased/neo4j-cli-Minor-20260521-174058.yaml
Decisions: bundle stayed single-file (`references/desktop.md`) per the task-022 decision — PRD aspirational `references/desktop_dbms*.md` per-leaf split would require splitting the generator's output strategy across the entire neo4j-cli command tree, out of scope; changelog body covers both the restructure (already noted in earlier task-022 progress) and the plugin subtree in a single Minor entry so the released changelog reads as one CLI-32 narrative.

## TASK-031 — 2026-05-21T15:00:00Z
Strip user-facing `desktop env` traces. PRD: drop Overview env mention, REQ-F-001 dormant-env stderr hint, REQ-F-006, env/ from file tree, env_list ref in desktopclient block, Lifecycle re-implements-walk sentence, two Acceptance Criteria env bullets, Out-of-Scope env-switch bullet, trim REQ-F-011 shared-with-env_list. impl.md: rephrase `(live)` suffix sentence to active-env discovery. Changelog: rewrite older "add" entry to no longer claim env list; delete redundant "removed env subtree" entry (never publicly shipped, consolidation per task description). `dbms.go:27` comment drops `desktop env` reference.
Files: ~.plans/prd-cli-32-local-desktop-management.md, ~.plans/cli-32-local-desktop-management-impl.md, ~.changes/unreleased/neo4j-cli-Minor-20260518-113428.yaml, -.changes/unreleased/neo4j-cli-Minor-20260521-121541.yaml, ~neo4j-cli/internal/subcommands/desktop/dbms/dbms.go
Decisions: consolidated the two unreleased Minor entries by deleting the explicit removal entry — the older add entry now accurately describes what shipped, so a separate "removed env subtree" note would mislead users about a leaf they never had.
