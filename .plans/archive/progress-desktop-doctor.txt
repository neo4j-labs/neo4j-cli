# desktop-doctor progress log

## task-001 — 2026-05-22T11:35:00Z
Scaffolded `neo4j-cli desktop doctor` cobra leaf with non-empty Long, 3 flush-left examples, stub RunE printing `(not implemented)`. Registered in desktop.go between newList and newInstall. Smoke test covers help build, parent-listing, and stub RunE returning nil. Bundle regenerated.
Files: +doctor.go, +doctor_test.go, ~desktop.go, ~skill/bundle/references/desktop.md

## task-002 — 2026-05-22T12:15:00Z
Added `checkResult` / `doctorReport` / `doctorSummary` types and the five health-check helpers (install / data_dir / auth_data / standard_probe / auth_probe). New `doctorGoosFn` seam + 5 exported `Check*ForTest` wrappers expose the unexported helpers to `desktop_test` without leaking the unexported type. Auth probe disambiguates 2xx + 404 (PASS) from 401 (FAIL) via `errors.As(*clierr.CLIError)`. 14 colocated unit tests cover happy + error paths and all three install branches via the doctor goos seam.
Files: ~doctor.go, +doctor_checks_test.go
Decisions: doctorReport/doctorSummary land now with //nolint:unused — locks the JSON wire contract in task-002 so task-003 only writes the orchestrator that fills them.

## task-003 — 2026-05-22T13:05:00Z
Wired `runChecks(ctx, cfg, pinnedPort) DoctorReport` orchestrator with skip-on-dependency-fail semantics; added 6 fn-var seams (checkInstallPresentFn, checkDataDirFn, checkAuthDataReadableFn, checkStandardProbeFn, buildAuthClientFn, checkAuthenticatedProbeFn) with matching SetXxxFnForTest overrides, plus `RunChecksForTest`. Summary builder surfaces port (when probe PASSes), standard_port_range (false outside 44222..44232 when --port pinned), reachable (probe AND auth_probe PASS), and next_step (first FAILing check's hint). Leaf RunE now calls runChecks but still emits placeholder stdout pending task-004 rendering. 8 new orchestrator tests cover happy path, probe-fail skip, auth-data-fail skip, auth-probe fail, client-build fail, --port in/out of range, install-fail independence, and check ordering.
Files: ~doctor.go, ~doctor_test.go, +doctor_orchestrator_test.go
Decisions: Exported CheckResult / DoctorReport / DoctorSummary so test-package stubs can construct fixture values via struct literals — keeps the seam API hermetic without a Make* factory. Auth probe skips when auth_data_readable FAILs (not only when probe FAILs) — extends REQ-F-003's "depends on probe" with the practical "needs salt to build a client" prerequisite, documented inline.

## task-004 — 2026-05-22T13:50:00Z
Wired `renderReport(cmd, cfg, report)` dispatch + three renderers (`renderDoctorJSON` via `json.MarshalIndent` with tab indent, `renderDoctorToon` via JSON→any→`toon.Marshal(WithLengthMarkers(true))`, `renderDoctorTable` via direct `fmt.Fprintf` with two-pass column sizing for PASS/FAIL/SKIP/INFO alignment). `statusKeyword` maps wire status → uppercase keyword. Replaced placeholder stdout in RunE with `renderReport(cmd, cfg, report)`. 7 new render tests cover: JSON shape (`DoctorReport` unmarshal, all five check names ordered, summary fields), Label elision from JSON (REQ-F-008), toon document keys, table all-PASS columns + forbidden-word sweep (`secret`/`JWT`/` salt`/`private key`), probe-FAIL skip + Next step + port elision, byte-identical TTY-vs-piped for explicit `--format`, TTY auto-detect dispatch (table on TTY, JSON on pipe). `newDoctorCfgDefaultFormat` builds a fresh cfg with `{"format":"default"}` so auto-detect actually fires (orchestrator suite's `newDoctorCfg` pins `json` which would short-circuit).
Files: ~doctor.go, +doctor_render_test.go
Decisions: `MarshalIndent("", "\t")` to match `common/output/output.go:97`. Table uses two-pass column sizing (max label / max keyword width) over `tabwriter` so output stays deterministic across Go versions and trivial to assert in tests. Renderer error return wires through RunE — render failures (well-known formats, well-formed structs) are pathological and not part of REQ-F-009's "check FAIL" spectrum, so non-zero exit on programming error is acceptable. Table summary line emits `reachable=true, [port=N, ]standard_port_range=true` unconditionally for any check-outcome combination; `Next step:` is a separate second line only when a FAILing check carries a hint. No bundle drift — Long unchanged.

## task-005 — 2026-05-22T14:30:00Z
Appended `" or run 'neo4j-cli desktop doctor' to scan."` to `canonicalUnreachable` in `desktopclient/client.go:80` so every leaf that surfaces REQ-F-008's unreachable hint points users at the new diagnostic. Extended `TestUnreachableError_MatchesCanonicalMessage` with a third substring assertion for the doctor hint. All 19 existing `strings.Contains("doesn't appear to be running")` assertions across client_test, leaf tests (connection/create+delete, dbms/start+stop+create+delete, list), and e2e/desktop_test continue to pass — substring match is forward-compatible with a longer canonical string. Gate sweep clean: `make test` 360+ passes, `make fmt-check` ok, `make lint` 0 issues, `make license-check` ok, `make generate-check` shows no bundle drift (only the in-progress source diff per the AGENTS.md known-gotcha; no `internal/skill/bundle/**` paths).
Files: ~client.go, ~client_test.go
Decisions: `, or run '...' to scan.` (comma-separated additional clause) keeps the same single-sentence structure as the existing two clauses; no separate sentence. Lower-case `doctor` (cobra leaf name), single-quoted around the full command per CLI-help convention. No update needed to `test/e2e/desktop_fixture/auth.go:44` — that's a comment string, not an assertion.

## task-006 — 2026-05-22T15:30:00Z
Smoke-tested `bin/neo4j-cli desktop doctor` against live Desktop 2.1.4 on macOS. Scenario 1 (default port 44222, Desktop up): all 5 checks PASS, summary.reachable=true, port=44222, standard_port_range=true. Scenario 2 substituted via `--port 44230` (unused port in 44222..44232 scan range — exercises identical code path to "Desktop stopped on default port" without disrupting live Desktop): install/data_dir/auth_data PASS, standard_probe FAIL ("No relate server answered on port 44230."), auth_probe SKIP ("(depends on standard_probe)"), summary.reachable=false, summary.next_step="Start Neo4j Desktop 2 from your applications menu, or pass a different --port.", standard_port_range=true. Scenario 3 (`--port 12345`): probe FAIL detail names 12345, standard_port_range=false. JSON schema verified against REQ-F-006 — all fields present, omitempty correct (no port/next_step when not applicable). REQ-F-008 forbidden-word sweep clean across all three scenarios (no `secret`, `key`, `JWT`, ` salt`, `private key`). Table + toon renderers also smoke-checked on scenario 1, output aligns + parses. Binary size: baseline (pre-task-001) 15,552,338 B (14.832 MiB) → current 15,588,306 B (14.866 MiB) → delta +35,968 B (+0.034 MiB), well within ±0.5 MiB.
Bug found + fixed during smoke: original auth probe used a synthetic non-UUID id (`neo4j-cli-doctor-probe`) against `GetDbms`, which Desktop's fastify route-validator rejected with a 400 ("must match format uuid") BEFORE reaching the auth hook. With a valid nil-UUID Desktop's handler returned 500 ("not found"), still misclassified as auth FAIL. Switched probe to `ListDbmss` (`GET /dbmss`, no UUID param) — a 2xx response unambiguously proves auth succeeded, a 401 unambiguously proves auth failed. `checkAuthenticatedProbe` now ~25 LOC (down from ~40), drops the 404 disambiguation branch and the `authProbeDbmsID` constant. Removed dead `net/http` import. Updated `doctor_checks_test.go` (removed `_404_Pass`, switched `_2xx_Pass` to expect `/dbmss` path) and `doctor_orchestrator_test.go` (4 fixture detail strings updated to "Authenticated call against Desktop relate API succeeded.").
Files: ~doctor.go, ~doctor_checks_test.go, ~doctor_orchestrator_test.go
Gates: `go test ./...` 2647/2647 pass, `make fmt-check` ok, `make lint` 0 issues, `make license-check` ok, `make generate-check` no bundle drift.
