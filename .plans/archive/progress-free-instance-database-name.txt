================================================================================
TASK-001: Add databaseName helper to instance create
Date: 2026-05-08T00:00:00Z
================================================================================

Summary:
Added a private `databaseName(instanceType, username string) string` helper function
to `credential_helpers.go` in the instance subcommand package. The function returns
the username when the instance type is "free-db" and the username is not "neo4j",
and returns "neo4j" in all other cases. Added comprehensive table-driven tests in
`credential_helpers_test.go` covering all acceptance criteria cases.

Files Changed:
- neo4j-cli/aura/internal/subcommands/instance/credential_helpers.go (modified — added databaseName helper)
- neo4j-cli/aura/internal/subcommands/instance/credential_helpers_test.go (modified — added TestDatabaseName table-driven tests)

Key Decisions:
- Added helper to credential_helpers.go (not a new file) since it is closely related to the other credential helper functions there
- Documented the free-db username convention in the function comment for future maintainers
- Included edge cases in tests: empty instance type and "neo4j" username for free-db

Next Task: task-002

================================================================================
TASK-002: Use databaseName helper in create.go credential storage
Date: 2026-05-08T00:05:00Z
================================================================================

Summary:
Replaced the hardcoded "neo4j" database name string in the `cfg.Credentials.Dbms.Add`
call in `create.go` with a call to the `databaseName(string(_type), username)` helper.
Both `_type` (the CLI flag) and `username` (extracted from the API response) were already
available at the call site, so no new variables or API calls were needed.

Files Changed:
- neo4j-cli/aura/internal/subcommands/instance/create.go (modified — replaced hardcoded "neo4j" with databaseName(...) call)

Key Decisions:
- Used `string(_type)` to convert `flags.InstanceType` (a string alias) to a plain string for the helper
- No other credential storage logic was touched; the change is exactly one argument substitution
- make build, make test, make fmt-check, and make lint all pass

Next Task: task-003

================================================================================
TASK-003: Update and extend tests for database name logic
Date: 2026-05-08T00:15:00Z
================================================================================

Summary:
Updated `TestCreateFreeInstance` in `create_test.go` to use a non-"neo4j" username
("alice123") in the mocked API response and added an `AssertCredentialsValue` assertion
for `database-name`. Added a new table-driven test `TestCreateDatabaseNameStorage` covering
all three scenarios: free-db + non-"neo4j" username (database-name = username), free-db +
"neo4j" username (database-name = "neo4j"), and professional-db + non-"neo4j" username
(database-name always = "neo4j").

Files Changed:
- neo4j-cli/aura/internal/subcommands/instance/create_test.go (modified — updated TestCreateFreeInstance to use non-"neo4j" username and added TestCreateDatabaseNameStorage table-driven test)

Key Decisions:
- Updated existing TestCreateFreeInstance to demonstrate the non-"neo4j" free-db path rather than adding a duplicate test
- Used fmt.Sprintf with %q formatting in the table-driven test to safely embed usernames and types in JSON
- Included vector_optimized field in the professional-db mock response to keep the JSON consistent with existing professional-db fixtures

Next Task: task-004

================================================================================
TASK-004: Run final quality gates
Date: 2026-05-08T00:20:00Z
================================================================================

Summary:
Ran `make test`, `make fmt-check`, and `make lint` as final quality gates.
All three passed with no failures, no unformatted files, and 0 lint issues.
The feature is complete: free-db instances now store the tenant-scoped username
as the database name in the DBMS credential rather than always storing "neo4j".

Files Changed:
- .plans/tasks-free-instance-database-name.yml (modified — task-004 marked completed)
- .plans/progress-free-instance-database-name.txt (modified — task-004 entry appended)

Key Decisions:
- No code changes were needed; this task was purely a quality gate verification
- All existing and new tests pass on the first run

Next Task: All tasks complete
