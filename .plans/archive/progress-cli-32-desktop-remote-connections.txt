## TASK-001 — 2026-05-21T00:00:00Z
Added Connection wire types + CRUD endpoints to desktopclient; renamed GetCredentials→GetCredentialsByKey so callers pass full keyspace ("dbms:<id>" / "connection:<id>"); updated single call site in query/desktop.go.
Files: ~neo4j-cli/internal/desktopclient/types.go, ~neo4j-cli/internal/desktopclient/client.go, ~neo4j-cli/internal/desktopclient/client_test.go, ~neo4j-cli/query/desktop.go
Decisions: ConnectionUpdateArgs uses *string pointers so empty-string description is distinguishable from "not set" — PATCH body is a map[string]any populated only for non-nil pointers.

## TASK-002 — 2026-05-21T01:00:00Z
Replaced implicit Desktop fallthrough + zero-config auto-select with explicit `-c desktop` (single started DBMS) and `-c desktop-connection:<uuid>` (saved connection) prefix dispatch in resolveConn; unprefixed values now never touch the Desktop client. Shared the probe/client bootstrap + REQ-F-028 null-creds branch via `newDesktopFallthroughClient` and `finishDesktopMatch`; updated --credential flag help and skill bundle.
Files: ~neo4j-cli/query/connect.go, ~neo4j-cli/query/desktop.go, ~neo4j-cli/query/desktop_test.go, ~neo4j-cli/query/query.go, ~neo4j-cli/internal/skill/bundle/references/query.md
Decisions: desktopMatch grew a `connection` field alongside `dbms` so finishDesktopMatch can serve both prefix paths with one REQ-F-028 branch; desktopclient.UnreachableError() is returned directly for probe/salt misses on the explicit prefix paths (vs the old silent (nil,nil) swallow) — the user opted in to Desktop by typing the prefix, so a canonical hint beats a misleading "no persisted credential" miss.

## TASK-003 — 2026-05-21T02:00:00Z
Unified `desktop list` to render Local DBMSes + Remote connections in one shot. JSON envelope is now `{dbmss, connections}` (BREAKING vs prior `[]DbmsInfo`); table mode emits two labelled sections with `(none)` placeholder rows when empty; toon mirrors JSON. Both endpoint calls run in parallel via errgroup.
Files: ~neo4j-cli/internal/subcommands/desktop/list.go, ~neo4j-cli/internal/subcommands/desktop/list_test.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: listResult.MarshalJSON force-initialises both slices to `[]` (never `null`) so JSON / toon consumers always see arrays; custom two-section table renderer mirrors query/schema.go's pattern (route table to a custom printer, delegate JSON/toon to PrintBodyMap via the ResponseData contract with a no-op AsArray).

## TASK-004 — 2026-05-21T03:00:00Z
Added `neo4j-cli desktop connection` parent + `create` leaf in a new `connection` subpackage (own client/TTY/password-reader seams since Go forbids the cyclic import back into `desktop`). `create` requires --name/--uri/--username via Cobra MarkFlagRequired; --password is optional on the flag surface but mandatory at runtime — TTY users get a no-echo stderr prompt, non-TTY users get a usage error before any HTTP call. POST body omits the description key unless --description was set on the command line. Bundle regenerated to expose the new subtree.
Files: +neo4j-cli/internal/subcommands/desktop/connection/connection.go, +neo4j-cli/internal/subcommands/desktop/connection/create.go, +neo4j-cli/internal/subcommands/desktop/connection/create_test.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: connection lives in its own package so future leaves (update, delete) get their seams colocated; cmd.Flag("port").Changed-style threading wasn't necessary because cobra inherits the parent persistent --port flag onto child Flags() automatically.

## TASK-005 — 2026-05-21T04:00:00Z
Added `desktop connection update <id>` leaf. Positional id validated via uuid.Parse with REQ-F-106 usage error pointing at `desktop list`; at-least-one mutating flag gate (--name/--uri/--username/--password/--description) fires before any HTTP call; PATCH body built via the desktopclient pointer-field map so each key appears only if user explicitly set the flag (empty-string description survives the wire). `--password ""` triggers the shared TTY prompt seam, non-TTY errors. Bundle regenerated.
Files: +neo4j-cli/internal/subcommands/desktop/connection/update.go, +neo4j-cli/internal/subcommands/desktop/connection/update_test.go, ~neo4j-cli/internal/subcommands/desktop/connection/connection.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: reused create_test.go helpers (mustTestFs, readPostBody) via package-level functions instead of duplicating; `--password ""` is the prompt-me sentinel (matches create) — there is no path to actually set an empty password, which relate would reject anyway.

## TASK-006 — 2026-05-21T05:00:00Z
Added `desktop connection delete <id>` leaf. UUID gate via uuid.Parse with REQ-F-107 usage error pointing at `desktop list`. Confirmation policy mirrors parent `desktop delete`: --yes skips prompt; without --yes on TTY we ListConnections to resolve the name for the y/N prompt (falls back to nameless prompt if id is missing from the list or the list call errors); without --yes on non-TTY hard usage error before any relate call. Confirmation shape mirrors task-021 (one-line table/toon, `{id,name,deleted:true}` JSON). Bundle regenerated.
Files: +neo4j-cli/internal/subcommands/desktop/connection/delete.go, +neo4j-cli/internal/subcommands/desktop/connection/delete_test.go, ~neo4j-cli/internal/subcommands/desktop/connection/connection.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: prompt-label list lookup is best-effort — a failed/empty list does NOT block the delete (race: user typed the id, they want it gone). The list call only happens on the TTY+no-yes branch, never on --yes, so the --yes-without-prompt fast path remains a single round-trip.

## TASK-007 — 2026-05-21T06:00:00Z
Final consolidation: added a worked example block in query.go covering --credential desktop / desktop-connection:<uuid> / persisted-name forms, extended the credential parent Long to note that desktop-prefix forms are runtime-resolved against Desktop and NOT stored, regenerated the neo4j-cli skill bundle, and landed three Minor changie entries (connection subtree, --credential prefix dispatch + removal of implicit fallthrough, BREAKING desktop list JSON shape).
Files: ~neo4j-cli/query/query.go, ~neo4j-cli/internal/subcommands/credential/credential.go, ~neo4j-cli/internal/skill/bundle/references/query.md, ~neo4j-cli/internal/skill/bundle/references/credential.md, +.changes/unreleased/neo4j-cli-Minor-20260521-130444.yaml, +.changes/unreleased/neo4j-cli-Minor-20260521-130449.yaml, +.changes/unreleased/neo4j-cli-Minor-20260521-130454.yaml

## TASK-008 — 2026-05-21T07:00:00Z
Dropped `project` from default table columns in both `desktop list` Remote connections section and `desktop connection create/update` single-row render. Wire field Connection.Project preserved; `--format json` still surfaces project. Long text + skill bundle regen aligned.
Files: ~neo4j-cli/internal/subcommands/desktop/list.go, ~neo4j-cli/internal/subcommands/desktop/list_test.go, ~neo4j-cli/internal/subcommands/desktop/connection/create.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md

## TASK-009 — 2026-05-21T08:00:00Z
Fixed `query -c desktop` always reporting "no running DBMS": `/dbmss` returns lightweight shape without status, so filter never matched. Added `ListDbmssInfo` hitting `/dbmss/info` and switched resolveDesktopActiveDbmsCredential to it. Updated RealClient test to mock both endpoints (regression gate: if resolver regresses to `/dbmss` the status filter sees empty strings and fails).
Files: ~neo4j-cli/internal/desktopclient/client.go, ~neo4j-cli/internal/desktopclient/client_test.go, ~neo4j-cli/query/desktop.go, ~neo4j-cli/query/desktop_test.go
Decisions: kept `desktop list` on `ListDbmss`+per-id enrichment unchanged (out of scope per task description); manual live-Desktop verify deferred (sandboxed agent).

## TASK-011 — 2026-05-21T12:20:00Z
Removed the user-facing `desktop env` subtree (parent + `list` leaf + colocated test) and dropped the dormant-env stderr hint from `desktop list`. Internal env resolution via `desktopclient/envconfig.go` is preserved — NEO4J_DESKTOP_ENV still influences HTTP discovery / salt loading, just no longer exposed as a CLI surface. Cleaned up stale doc-comments referencing `desktop env list` in desktop.go (Long + package doc), envconfig.go, types.go. Bundle regenerated; one changie Minor entry added.
Files: -neo4j-cli/internal/subcommands/desktop/env.go, -neo4j-cli/internal/subcommands/desktop/env_list.go, -neo4j-cli/internal/subcommands/desktop/env_list_test.go, ~neo4j-cli/internal/subcommands/desktop/desktop.go, ~neo4j-cli/internal/subcommands/desktop/list.go, ~neo4j-cli/internal/subcommands/desktop/list_test.go, ~neo4j-cli/internal/desktopclient/envconfig.go, ~neo4j-cli/internal/desktopclient/types.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md, +.changes/unreleased/neo4j-cli-Minor-20260521-121541.yaml
Decisions: also dropped the `dormantEnvsWithDbmss` + writeEnvJSON/writeDbmsDir test helpers since their only callers were the env-hint tests; task-010's `desktop` Long tightening still pending (its dep on task-011 satisfied — left for a future iteration).

## TASK-010 — 2026-05-21T13:00:00Z
Tightened `desktop` parent Long help: dropped the HTTP API URL mention (`http://localhost:<port>/fastify/api`) from both Long and the package doc-comment; kept the purpose statement and `--rw` reminder. env/NEO4J_DESKTOP_ENV mentions already gone (task-011). Bundle regenerated.
Files: ~neo4j-cli/internal/subcommands/desktop/desktop.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md

## TASK-012 — 2026-05-21T14:00:00Z
Built relate-shaped fixture HTTP server at test/e2e/desktop_fixture/ for upcoming desktop e2e suite: mimics Desktop 2's `/fastify/api/*` surface (dbmss lite + info + CRUD + start/stop, connections CRUD, credentials, versions, api-docs probe) with HS256 JWT auth using the same composite-key derivation as production. Scenario admin endpoints (`/_scenario/*`) let tests reset state, seed dbmss/connections/creds, flip auth modes (accept/reject/500/close), arm status transitions, swap version catalog, and dump the request log without restarting the process. Prints `listening on http://127.0.0.1:<port>` as first stdout line and exits cleanly on SIGTERM within 1s. 13 tests cover scenario state mutations + JWT validation.
Files: +test/e2e/desktop_fixture/main.go, +test/e2e/desktop_fixture/state.go, +test/e2e/desktop_fixture/auth.go, +test/e2e/desktop_fixture/handlers.go, +test/e2e/desktop_fixture/scenario.go, +test/e2e/desktop_fixture/main_test.go
Decisions: `/dbmss` is intentionally a distinct lite shape (no status) from `/dbmss/info` so task-009's regression gate carries forward into e2e; `--client-id` flag dropped from spec since the auth middleware just checks non-empty + JWT signature, not a fixed value (production sends a fresh UUID per invocation); `close` auth-mode uses http.Hijacker to slam the socket mid-stream so REQ-F-008 transport-EOF path is exercisable.

## TASK-013 — 2026-05-21T15:00:00Z
Added `e2e_desktop_seams` build-tag-gated overrides to desktopclient: ProbePort / origin / LoadSalt / ResolveDataDir each check a package-level override var (e2ePortOverride / e2eOriginOverride / e2eSaltOverride / e2eDataDirOverride) before running production logic. seams_e2e.go init() reads NEO4J_CLI_DESKTOP_E2E_{PORT,HTTP_ORIGIN,SALT,DATA_DIR} env vars; seams_default.go is the no-op doc stub for the production build. seams_default_test.go asserts the env vars are inert without the tag. Manual verify: `go build -tags e2e_desktop_seams` binary connects to fixture and round-trips `desktop list --format json`.
Files: ~neo4j-cli/internal/desktopclient/discovery.go, +neo4j-cli/internal/desktopclient/seams_e2e.go, +neo4j-cli/internal/desktopclient/seams_default.go, +neo4j-cli/internal/desktopclient/seams_default_test.go
Decisions: PORT override SKIPS /api-docs validation entirely (fixture is brought up by the harness before the CLI runs, so re-validating is dead weight); HTTP_ORIGIN override replaces the FULL origin string so URL prefix in client.do AND JWT signing key both flip atomically — keeps the two-tie-together invariant intact in e2e.

## TASK-014 — 2026-05-21T16:00:00Z
Added desktop e2e suite at `test/e2e/desktop/desktop_test.go` (build tag `e2e_desktop`): 36 subtests covering desktop list/create/start/stop/delete, desktop connection create/update/delete, query -c desktop / desktop-connection:<uuid>, full DBMS status matrix (started/starting/stopping/stopped + zero-DBMSes), connection-prefix sad paths (malformed UUID / unknown UUID / null-creds non-TTY), transport sad paths (unreachable / 401 / 500 / mid-stream EOF), and resource sad paths (duplicate names, unknown IDs). TestMain builds CLI with -tags e2e_desktop_seams and the fixture once, then each test spawns its own fixture process for port isolation and t.Parallel safety. Bolt-class query subtests cap their subprocess at 4s via exec.CommandContext so the suite finishes in ~6s wall time (well under the 60s budget). Added `/_scenario/auto_progress` admin endpoint on the fixture so lifecycle tests with `--wait` converge without arming per-id transitions (avoids the preflight-enrichment-GET-eats-the-arm race).
Files: +test/e2e/desktop/desktop_test.go, ~test/e2e/desktop_fixture/state.go, ~test/e2e/desktop_fixture/handlers.go, ~test/e2e/desktop_fixture/scenario.go
Decisions: 4xx-non-401 surfaces as exit 1 (clierr.NewFatalError), not exit 3, because desktopclient.do() maps all non-401 4xx to fatal; the e2e sad-path tests assert exit != 0 + stderr substring rather than exit-code shape so they stay robust to future error-mapping tweaks. Bolt query happy-path tests don't assert exit code at all (subprocess is SIGKILLed by context timeout) — only that resolver-class errors are absent from stderr.

## TASK-015 — 2026-05-21T17:00:00Z
Wired desktop e2e into `.github/workflows/test.yml`: one new step after the tier-2 update e2e block invokes `go test -tags=e2e_desktop -count=1 -timeout=180s ./test/e2e/desktop/...` on the full ubuntu/macos/windows matrix.
Files: ~.github/workflows/test.yml
Decisions: no workflow-side build steps for the CLI seam binary or fixture — task-014's TestMain self-builds both, so a single `go test` invocation covers the spec's three-step build/fixture/run sequence. Task description's `NEO4J_CLI_DESKTOP_E2E_BINARY/FIXTURE` env vars are not used (the harness doesn't honor them).

## TASK-016 — 2026-05-21T18:00:00Z
Fixed Windows path resolution in desktopclient: `defaultDataDir()` Windows branch drops the stray `Application` segment (real path is `<home>\.Neo4jDesktop2\Data`); `EnvConfigDir()` now branches per `goosFn()` to match relate's deprecated env-paths.ts locations (darwin: `com.Neo4j.Relate/Config/environments`, windows: `Neo4j/Relate/Config/environments`, linux: `neo4j-relate/environments`). Added `TestEnvConfigDir_PerOS` (mocked 3-OS sweep) plus `TestDefaultDataDir_NativeOS` + `TestEnvConfigDir_NativeOS` (unmocked native-OS suffix assertions for CI matrix defense-in-depth). `writeEnvJSON` test helper now resolves via `EnvConfigDir()` instead of hardcoding the macOS layout; `newDesktopFallthroughFs` in query/desktop_test.go pins `goosFn` to linux so the env-dir layout is stable on every runner.
Files: ~neo4j-cli/internal/desktopclient/discovery.go, ~neo4j-cli/internal/desktopclient/discovery_test.go, ~neo4j-cli/internal/desktopclient/envconfig.go, ~neo4j-cli/internal/desktopclient/envconfig_test.go, ~neo4j-cli/internal/desktopclient/types.go, ~neo4j-cli/query/desktop_test.go
Decisions: `writeEnvJSON` retains its `configDir` parameter (now unused, marked `_ =`) for caller clarity — removing it would churn 7 unrelated callsites with no payoff; `newDesktopFallthroughFs` installs its own goosFn override rather than asking callers to do it, so the two RealClient tests no longer need a separate `SetUserConfigDirFnForTest` cleanup.
