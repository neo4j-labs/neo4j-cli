## TASK-001 — 2026-05-22T13:30:00Z
Added unauthenticated FetchAppInfo + AppInfo to desktopclient (no X-Client-Id/X-API-Token; reuses httpDoFn seam, requestTimeout, maxResponseBodyBytes; mirrors doRaw transport mapping). Nine table-style tests cover 200 happy/unknown-fields, 401→auth, 5xx→fatal w/ truncation, transport→unreachable, deadline→timeout, decode error, empty body, request-timeout budget.
Files: ~neo4j-cli/internal/desktopclient/discovery.go, ~neo4j-cli/internal/desktopclient/discovery_test.go

## TASK-002 — 2026-05-22T14:30:00Z
ResolveDataDir signature now (ctx, fs, probe) with 4-step precedence: env-var → /info/app → env-JSON → OS default. Empty probe.Origin or any FetchAppInfo error/empty-DataPath falls through silently. Reordered helpers (desktop, dbms, plugin, connection, query/desktop) to ProbePort first → ResolveDataDir → LoadSalt → NewClient. Doctor orchestrator runs probe before data-dir while preserving output row order; checkDataDir + seam now take ProbeResult. Added table-driven precedence tests (9 cases) including zero-httpDoFn-call assertions for env-var short-circuit and empty-probe skip.
Files: ~neo4j-cli/internal/desktopclient/discovery.go, ~neo4j-cli/internal/desktopclient/discovery_test.go, ~neo4j-cli/internal/subcommands/desktop/{helpers,doctor,doctor_test,doctor_checks_test,doctor_render_test,doctor_orchestrator_test}.go, ~neo4j-cli/internal/subcommands/desktop/dbms/helpers.go, ~neo4j-cli/internal/subcommands/desktop/dbms/plugin/helpers.go, ~neo4j-cli/internal/subcommands/desktop/connection/create.go, ~neo4j-cli/query/desktop.go
Decisions: kept doctor checks output order (install, data_dir, auth_data, probe, auth_probe) by appending probeR at its original slot even though probe is invoked earlier; tests use findCheck-by-name so reorder is transparent.

## TASK-003 — 2026-05-22T15:30:00Z
Added info_app diagnostic row to desktop doctor — sits between standard_probe and data_dir (REQ-F-207). checkInfoApp calls desktopclient.FetchAppInfo and either PASSes with structured Version/AppPath/DataPath fields + table-friendly Detail "version=X appPath=Y dataPath=Z", or INFOs with "unavailable (older Desktop)" on any error (401/5xx/transport/decode/empty-DataPath/empty-probe-origin). Never FAILs. Probe FAIL cascades into SKIP via skipResult. Row order reshuffled to install→probe→info_app→data_dir→auth_data→auth_probe (matches REQ-F-207; task-002's deferred-append no longer needed). CheckResult gained omitempty Version/AppPath/DataPath; existing JSON consumers see additive-only changes. Bundle regenerated (Long says "six health checks"). Tests: 7-case checkInfoApp table + 418-catch-all neverFails + orchestrator SkipsInfoApp + render asserts on version=/appPath= substrings.
Files: ~neo4j-cli/internal/subcommands/desktop/{doctor,doctor_test,doctor_checks_test,doctor_orchestrator_test,doctor_render_test}.go, ~neo4j-cli/internal/skill/bundle/references/desktop.md
Decisions: info_app Detail is single-line "key=value key=value" (space-separated) to keep the table renderer's per-row layout intact; structured fields carry the canonical data for JSON/toon. info_app SKIPs (not INFOs) on probe FAIL so the dependency chain is visible in the report.

## TASK-004 — 2026-05-22T16:00:00Z
E2E smoke against live Desktop 2.1.4 + changelog filed + final gate sweep all green.
Empirical /info/app payload (version=2.1.4, appPath=/Users/oskarhane/Library/Application Support/neo4j-desktop/Application, dataPath=/Users/oskarhane/Library/Application Support/neo4j-local/Application/relate-data-neo4j-local) observed against the running dev build (AppTranslocation). Scenario 1: desktop list --format json returned 9 dbmss (incl. memory) + 2 connections, exit 0. Scenario 2: doctor JSON showed both branches — info_app PASS row carrying the live version/appPath/dataPath, and (post Desktop restart from /Applications) info_app INFO row "unavailable (older Desktop)" while data_dir / auth_data_readable / authenticated_probe still PASS. Scenario 3: env-JSONs moved aside + restored via trap (verified bak removed); on the production 2.1.4 build that 401s /info/app, fall-through reaches OS default which lacks Application/Data → canonical unreachable surfaces as expected (PRD REQ-F-204 fall-through semantics confirmed). Gates: make test (2676/76pkg), fmt-check, lint (0 issues), license-check, build, generate-check (against clean tree via stash) all PASS.
Files: +.changes/unreleased/neo4j-cli-Minor-20260522-155701.yaml
Decisions: production Desktop 2.1.4 from /Applications does NOT serve unauth /info/app (401); only the AppTranslocation dev build did. Resolves the PRD Open Question — minimum Desktop version that ships unauth /info/app is later than 2.1.4 release; env-JSON fallback remains load-bearing on shipped builds today.
