## TASK-001 — 2026-05-19T14:00:00Z
Added `github.com/zalando/go-keyring v0.2.8` to go.mod/go.sum via `go get`; transitive deps `danieljoos/wincred` (Windows) and `godbus/dbus/v5` (Linux D-Bus) are pure Go on all three target platforms.
Files: ~go.mod, ~go.sum

## TASK-002 — 2026-05-19T14:30:00Z
Created `KeyringProvider` interface with Get/Set/Delete, backed by `goKeyringProvider` delegating to go-keyring package functions; added `SetKeyringProviderForTest` seam to `export_test.go`; defined `ServiceName` constant and `KeyringKey` formatter; updated go.mod to mark go-keyring as a direct dependency.
Files: +common/clicfg/credentials/keyring.go, +common/clicfg/credentials/keyring_test.go, ~common/clicfg/credentials/export_test.go, ~go.mod, ~go.sum

## TASK-003 — 2026-05-19T15:00:00Z
Registered `"credential-storage"` in `GlobalConfig.ValidConfigKeys`; added validation in `GlobalConfig.Set()` accepting only `"keyring"` or `"insecure"`; added `GlobalConfig.CredentialStorage()` defaulting to `"keyring"` when absent; updated list tests in both neo4j-cli and standalone aura config packages to include new key; added unit tests for `CredentialStorage()` helper and `config set credential-storage` cases.
Files: ~common/clicfg/clicfg.go, ~common/clicfg/clicfg_test.go, ~neo4j-cli/internal/subcommands/config/list_test.go, ~neo4j-cli/internal/subcommands/config/set_test.go, ~neo4j-cli/aura/internal/subcommands/config/standalone_config_test.go

## TASK-005 — 2026-05-19T17:00:00Z
Added `MigrateToKeyring() error` to `Credentials`; iterates all three credential types writing required fields (ClientSecret, Password) and optional fields (AccessToken, APIKey) to the OS keyring with atomic rollback on any failure; zeroes in-memory sensitive fields and calls save() to scrub JSON on success.
Files: ~common/clicfg/credentials/credentials.go, ~common/clicfg/credentials/credentials_keyring_test.go
Decisions: Method is designed to run while storageMode is still StorageModeInsecure so save() writes zeroed values to JSON; comment added to document this contract.

## TASK-004 — 2026-05-19T16:00:00Z
Added `storageMode` field and `StorageModeInsecure`/`StorageModeKeyring` constants to `Credentials`; exposed `SetStorageMode(mode)` and `StorageMode()` methods; `save()` dispatches to `saveWithKeyring()` which writes secrets to OS keyring and zeros them in JSON while keeping in-memory values populated; `loadSensitiveFieldsFromKeyring()` populates secrets from keyring after JSON load, with pre-migration JSON fallback for ErrNotFound; `NewConfig` calls `SetStorageMode` only when `credential-storage` is explicitly set in viper (avoids breaking existing tests that don't set the key); comprehensive keyring-mode tests added.
Files: ~common/clicfg/credentials/credentials.go, +common/clicfg/credentials/credentials_keyring_test.go, ~common/clicfg/clicfg.go
Decisions: `NewConfig` guards `SetStorageMode` with `Viper.IsSet("credential-storage")` so existing tests and pre-task-009 invocations are not broken by the `CredentialStorage()` keyring default.
