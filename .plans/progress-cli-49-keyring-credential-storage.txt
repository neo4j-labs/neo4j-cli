## TASK-001 — 2026-05-19T14:00:00Z
Added `github.com/zalando/go-keyring v0.2.8` to go.mod/go.sum via `go get`; transitive deps `danieljoos/wincred` (Windows) and `godbus/dbus/v5` (Linux D-Bus) are pure Go on all three target platforms.
Files: ~go.mod, ~go.sum

## TASK-002 — 2026-05-19T14:30:00Z
Created `KeyringProvider` interface with Get/Set/Delete, backed by `goKeyringProvider` delegating to go-keyring package functions; added `SetKeyringProviderForTest` seam to `export_test.go`; defined `ServiceName` constant and `KeyringKey` formatter; updated go.mod to mark go-keyring as a direct dependency.
Files: +common/clicfg/credentials/keyring.go, +common/clicfg/credentials/keyring_test.go, ~common/clicfg/credentials/export_test.go, ~go.mod, ~go.sum

## TASK-003 — 2026-05-19T15:00:00Z
Registered `"credential-storage"` in `GlobalConfig.ValidConfigKeys`; added validation in `GlobalConfig.Set()` accepting only `"keyring"` or `"insecure"`; added `GlobalConfig.CredentialStorage()` defaulting to `"keyring"` when absent; updated list tests in both neo4j-cli and standalone aura config packages to include new key; added unit tests for `CredentialStorage()` helper and `config set credential-storage` cases.
Files: ~common/clicfg/clicfg.go, ~common/clicfg/clicfg_test.go, ~neo4j-cli/internal/subcommands/config/list_test.go, ~neo4j-cli/internal/subcommands/config/set_test.go, ~neo4j-cli/aura/internal/subcommands/config/standalone_config_test.go

## TASK-005 — 2026-05-19T17:00:00Z
Added `MigrateToKeyring() error` to `Credentials`; iterates all three credential types writing required fields (ClientSecret, Password) and optional fields (AccessToken, APIKey) to the OS keyring with atomic rollback on any failure; zeroes in-memory sensitive fields and calls save() to scrub JSON on success.
Files: ~common/clicfg/credentials/credentials.go, ~common/clicfg/credentials/credentials_keyring_test.go
Decisions: Method is designed to run while storageMode is still StorageModeInsecure so save() writes zeroed values to JSON; comment added to document this contract.

## TASK-006 — 2026-05-19T18:00:00Z
Added `MigrateToInsecure() error` to `Credentials`; reads required fields (ClientSecret, Password) and optional fields (AccessToken, APIKey) from the OS keyring, aborts with named error on ErrNotFound for required fields, silently skips ErrNotFound for optional fields, and hard-errors on any non-ErrNotFound keyring failure; on success temporarily switches storageMode to insecure to flush secrets to JSON, then deletes all read keyring entries (best-effort).
Files: ~common/clicfg/credentials/credentials.go, ~common/clicfg/credentials/credentials_keyring_test.go
Decisions: Phase 2 uses a temporary storageMode flip (keyring→insecure→keyring) rather than a new save variant, keeping the save() dispatch clean and the method self-contained; caller (task-007) calls SetStorageMode(insecure) after persisting config key.

## TASK-007 — 2026-05-19T19:00:00Z
Updated `config set` RunE to detect `credential-storage` key changes, call `MigrateToKeyring()`/`MigrateToInsecure()` before writing config (migration failure leaves config unchanged), and update in-memory `StorageMode` after successful persistence. Extended test helper with `executeCommandWithCredentials` and added `TestConfigSet_CredentialStorageMigration` using `gokeyring.MockInit`/`MockInitWithError` to exercise the migration path and failure path from the config_test package.
Files: ~neo4j-cli/internal/subcommands/config/set.go, ~neo4j-cli/internal/subcommands/config/set_test.go, ~neo4j-cli/internal/subcommands/config/helpers_test.go
Decisions: Used `gokeyring.MockInit()` directly (not `credentials.SetKeyringProviderForTest` which is inaccessible from external test packages) to inject mock keyring state into the full command stack; guarded the post-save `SetStorageMode` call with `credentialStorageModeChanged` flag to avoid redundant keyring reload on no-op.

## TASK-008 — 2026-05-19T20:00:00Z
Added `DeleteKeyringEntries(credType, name)` to `Credentials`; updated `credential aura-client remove`, `credential dbms remove`, and `credential embed remove` (both neo4j-cli and standalone aura paths) to call it best-effort in keyring mode, printing a warning on non-ErrNotFound errors; added `_KeyringMode` test functions for all three credential types covering the delete-entries, ErrNotFound-is-ignored, and insecure-mode-no-op cases; also added unit tests for `DeleteKeyringEntries` directly in the credentials package.
Files: ~common/clicfg/credentials/credentials.go, ~common/clicfg/credentials/credentials_keyring_test.go, ~neo4j-cli/aura/internal/subcommands/credential/remove.go, ~neo4j-cli/aura/internal/subcommands/credential/remove_test.go, ~neo4j-cli/internal/subcommands/credential/credential.go, ~neo4j-cli/internal/subcommands/credential/credential_test.go, ~neo4j-cli/internal/subcommands/credential/dbms/remove.go, ~neo4j-cli/internal/subcommands/credential/dbms/remove_test.go, ~neo4j-cli/internal/subcommands/credential/dbms/add_test.go, ~neo4j-cli/internal/subcommands/credential/embed/remove.go, ~neo4j-cli/internal/subcommands/credential/embed/remove_test.go, ~neo4j-cli/internal/subcommands/credential/embed/helpers_test.go

## TASK-004 — 2026-05-19T16:00:00Z
Added `storageMode` field and `StorageModeInsecure`/`StorageModeKeyring` constants to `Credentials`; exposed `SetStorageMode(mode)` and `StorageMode()` methods; `save()` dispatches to `saveWithKeyring()` which writes secrets to OS keyring and zeros them in JSON while keeping in-memory values populated; `loadSensitiveFieldsFromKeyring()` populates secrets from keyring after JSON load, with pre-migration JSON fallback for ErrNotFound; `NewConfig` calls `SetStorageMode` only when `credential-storage` is explicitly set in viper (avoids breaking existing tests that don't set the key); comprehensive keyring-mode tests added.
Files: ~common/clicfg/credentials/credentials.go, +common/clicfg/credentials/credentials_keyring_test.go, ~common/clicfg/clicfg.go
Decisions: `NewConfig` guards `SetStorageMode` with `Viper.IsSet("credential-storage")` so existing tests and pre-task-009 invocations are not broken by the `CredentialStorage()` keyring default.

## TASK-009 — 2026-05-19T21:00:00Z
Implemented `initCredentialStorageDefault` in `neo4j-cli/app/app.go`, called from `PersistentPreRunE`; writes `"insecure"` plus the one-time upgrade notice to stderr only when `GlobalConfig.Set` succeeds, and writes `"keyring"` silently for fresh installs; added `CredentialStorageIsSet` and fixed `GlobalConfig.Set` viper-sync comment to remove the parenthetical caller example.
Files: ~neo4j-cli/app/app.go, ~common/clicfg/clicfg.go, +neo4j-cli/app/credential_storage_default_test.go
