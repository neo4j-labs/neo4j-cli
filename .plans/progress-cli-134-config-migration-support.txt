## task-001 — 2026-05-19T00:00:00Z
Scaffolded configmigrate package: Migration struct, empty registry, init validator that panics on gap/duplicate/non-1-start.
Files: +neo4j-cli/internal/configmigrate/migrations.go

## task-002 — 2026-05-19T00:00:00Z
Implemented Run/runWith engine: empty-registry/missing-file no-op, gjson-read _schema_version, per-migration Apply + sjson stamp, single fileutils.WriteFile, stderr warning on read/apply errors.
Files: +neo4j-cli/internal/configmigrate/configmigrate.go
Decisions: stamp _schema_version via sjson.SetBytes after each Apply (not just at loop end) so a downstream Apply failure leaves the file untouched but the in-memory chain stays valid; matches "write once at end" acceptance.

## task-003 — 2026-05-19T00:00:00Z
Added table-driven engine tests (fresh/partial/up-to-date/idempotency/per-migration error/empty registry/missing file) and validator tests (gap/duplicate/non-1 start). Extracted init() body into validateMigrations for direct testing without recover().
Files: +neo4j-cli/internal/configmigrate/configmigrate_test.go, ~neo4j-cli/internal/configmigrate/migrations.go

## task-004 — 2026-05-19T00:00:00Z
Wired configmigrate.Run into clicfg.NewConfig (after first ReadInConfig, followed by unconditional re-read). Removed commented-out migration block. Moved package out of neo4j-cli/internal/ to common/ because Go's internal-package rule blocked common/clicfg from importing neo4j-cli/internal/configmigrate. Dropped clicfg/testfs imports from test file to avoid test-time import cycle; replaced with local seedTestFs helper using a hard-coded relative path.
Files: ~common/clicfg/clicfg.go, >neo4j-cli/internal/configmigrate/configmigrate.go=>common/configmigrate/configmigrate.go, >neo4j-cli/internal/configmigrate/migrations.go=>common/configmigrate/migrations.go, >neo4j-cli/internal/configmigrate/configmigrate_test.go=>common/configmigrate/configmigrate_test.go
Decisions: package moved from neo4j-cli/internal/configmigrate to common/configmigrate — internal-rule forbids common pkgs from importing neo4j-cli/internal/*; clicfg.NewConfig is the integration point so the engine had to be reachable from common.

## task-005 — 2026-05-19T00:00:00Z
Final gates + live no-op smoke. config.json sha256 byte-identical before/after `config list` (cfd30a3e…3704). make build/fmt-check/lint/test/license-check all clean; generate-check diff is only the harness YAML status flip (no bundle drift).
Files: ~.plans/tasks-cli-134-config-migration-support.yml

## task-006 — 2026-05-19T00:00:00Z
Added .agents/config-migrations.md (marker, registry shape, error policy, test seam, internal-package gotcha). Linked from AGENTS.md "Config Migration Notes" and tightened .agents/feature-flags.md CLI-134 cross-refs.
Files: +.agents/config-migrations.md, ~AGENTS.md, ~.agents/feature-flags.md
