## TASK-001 — 2026-06-04T00:00:00Z
Removed --instance-username and --instance-password flags from graphql create. Added --service-account flag (optional, default read_write) with enum validation in RunE (fires before SilenceUsage=true) rejecting values other than read_only or read_write. Updated request body to send aura_instance.service_account. Updated all Example strings. Also fixed a pre-existing gofmt issue in update.go (whitespace only).
Files: ~create.go, ~update.go (fmt only)

## TASK-002 — 2026-06-04T01:00:00Z
Replaced --instance-username and --instance-password with --service-account (optional, empty default) on `graphql update`. Validation rejects values other than read_only or read_write before any API call. When provided, body includes aura_instance.service_account; when omitted, aura_instance is absent entirely. Updated Example strings and regenerated skill bundle. Also updated create_test.go and update_test.go to remove broken old-flag references and add service-account test coverage so make test passes.
Files: ~update.go, ~update_test.go, ~create_test.go, ~internal/skill/bundle/references/aura.md
