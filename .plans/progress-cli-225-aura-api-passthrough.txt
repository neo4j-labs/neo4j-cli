# Progress: cli-225-aura-api-passthrough

## TASK-001 — 2026-07-30T13:42:00Z
Extracted MakeRequest's prologue (base-url SSRF gate, URL/version/path join, query params, request build, credential resolution, getHeaders, --debug request emit) into unexported `prepareRequest`; MakeRequest delegates. Pure refactor, existing suite green untouched.
Files: ~neo4j-cli/aura/internal/api/api.go
Decisions: `prepareRequest` takes versionPath as a literal string (empty = no version segment) and query params as `url.Values` (new `queryValues` converts MakeRequest's map) — both shapes exist for task-006's RawRequestConfig; task-006 MUST assert the empty-versionPath and repeated-query-key paths or they stay uncovered. http.NewRequest failure now returns a usage error instead of panicking (unreachable for MakeRequest; keeps the future raw entrypoint panic-free). getVersionPath is now evaluated before the base-url validation — divergence only for an invalid Version constant, a programmer error that panics under both orderings.

## TASK-002 — 2026-07-30T14:05:00Z
Extracted the four-branch --rw precedence (rw set / agent / TTY / else) from `EnforceWriteGate` into exported `flags.RequireWriteAccess(cmd)`; `EnforceWriteGate` keeps its `write:true` annotation short-circuit and delegates. Added a table-driven `TestRequireWriteAccess` (all four branches + exit-2 assertion) and a nil-`--rw`-flag case; deduped the seam save/restore into `setGateSeams`.
Files: ~common/flags/flags.go, ~common/flags/flags_test.go
Decisions: Left `TestEnforceWriteGate_GateMatrix` untouched per the acceptance criteria, so the precedence matrix is now asserted through both entrypoints; collapsing it into the new test is optional follow-up.

## TASK-003 — 2026-07-30T14:35:00Z
Split `confirm.Require` into a thin parent-name-deriving wrapper over a new exported `RequireTyped(cmd, resourceType, resourceID)` that owns the whole --yes/--force gate; the gate contract doc moved onto RequireTyped. Added a table-driven `TestRequireTyped` (custom noun, parentless cmd, empty ID/type, both-flags, TTY accept/decline) plus a byte-identity delegation test covering both the TTY prompt and the non-TTY error copy.
Files: ~common/confirm/confirm.go, ~common/confirm/confirm_test.go, ~common/confirm/confirmtest/confirmtest.go
Decisions: The empty-resourceType -> "resource" fallback lives in `RequireTyped` (not the wrapper), so a parentless or unnamed-parent mount degrades identically through either entrypoint. Kept the `RequireTyped` name from the task YAML despite the mild GitHub-style "type the name to confirm" misread risk.

## TASK-004 — 2026-07-30T15:05:00Z
Added exported `utils.ResolveOrgProject(cmd, cfg)` — `resolveIDs` precedence + `ValidateResourceID` on both IDs, zero HTTP. `ResolveAndValidateOrgProject` now delegates to it and only adds the `validateProjectInOrg` membership call. Table-driven `TestResolveOrgProject` (flags, default-workspace, flag precedence, missing org/project, legacy default-tenant hint, traversal org/project IDs, over-segmented workspace) asserts exit codes and zero requests against a counting httptest server.
Files: ~neo4j-cli/aura/internal/subcommands/utils/resolve.go, ~neo4j-cli/aura/internal/subcommands/utils/resolve_test.go
Decisions: Inverted the delegation (old -> new) rather than duplicating the two `ValidateResourceID` calls; order is byte-identical (resolveIDs -> org -> project -> membership) so malformed-ID errors still beat a membership 404. Amended the task's "ResolveAndValidateOrgProject is unchanged" criterion to say observable behaviour + callers unchanged. Traversal IDs are exit 6 (`NewValidationError`), resolution failures exit 2 — task-009's placeholder substitution depends on that split, now pinned. Counting server returns `{"data": []}` so a regression that issues a request hits the assertion instead of panicking in `api.ParseBody`.

## TASK-006 — 2026-07-30T15:55:00Z
Added `api.MakeRawRequest` + `RawRequestConfig`/`RawResponse` on top of task-001's `prepareRequest`: literal VersionPath (no enum, `""` = no segment), verbatim body (top-level arrays OK), url.Values query, header overlay, body kept on every status, no handleResponseError, no 2xx-errors trap, no panics (transport/read failures -> NewUpstreamError with scrubbed text). 401 still runs `formatAuthorizationError` so the stale token is cleared. 15 tests: status matrix incl. 413/415/422, version paths, embedded-errors success, repeated query keys, verbatim/array bodies, header overlay (case-insensitive, replace-not-append), transport failure, truncated-body read failure (hijacked conn), 401 stored + ephemeral credential, missing method, inherited SSRF gate, debug on/off.
Files: ~neo4j-cli/aura/internal/api/api.go, +neo4j-cli/aura/internal/api/raw.go, +neo4j-cli/aura/internal/api/raw_test.go
Decisions: `prepareRequest` now takes `*RawRequestConfig` (was 7 positional params) — MakeRequest fills one in after marshalling its body and resolving its version enum; the warnW->os.Stderr default moved inside it. Non-2xx is NOT an error from MakeRawRequest (task-007's RawStatusError maps it); 401 is the sole exception and returns both the response and the auth error, so task-011 must check `err != nil` before rendering. Header overlay replaces per name and assumes one spelling per header (map order would otherwise pick the winner) — task-010's -H parser must build via http.Header.Add. No Host-header special case: `-H 'Host: x'` is a silent no-op. A malformed header value currently exits 8 from net/http, so task-010's usage-error validation is the only guard.

## TASK-005 — 2026-07-30T16:35:00Z
Redaction hardening for the new `aura api` flags: `header`/`H` added to `secretFlags` (whole value scrubbed — the header NAME can be the secret), `isParamFlag` generalised into `isKeyValueFlag` over a new `keyValueFlags` list (`param`, `field`, `raw-field`, `F`, `f`) so `redactParamValue`'s selective key=value scrubbing is single-sourced. Added a 4th argv shape to `RedactArgs` — attached shorthand (`-HAuthorization: …`, `-Fpassword=x`) — plus 21 table cases.
Files: ~common/clievents/redact.go, ~common/clievents/redact_test.go
Decisions: Shorthands `H`/`F`/`f` are registered here BEFORE task-010 defines them; if task-010 picks different shorthands this fails OPEN with no test failure — task-011 should add a gate walking the api command's `Flags()` asserting every secret-bearing long name and shorthand is matched by `RedactArgs`. `secretParamKeyParts` no longer aliases `secretWords`: it appends `auth`/`credential`/`passphrase`, since the regex hazard that excluded `auth` is regex-only and `--field` carries arbitrary API keys (also widens `--param`, no existing test affected). Attached-shorthand matching runs LAST so exact name matches win (`-password s3cret` must not split into `-p` + `assword`); `-f`/`update --force` collision is benign because `redactParamValue` passes through any value with no `=`.
