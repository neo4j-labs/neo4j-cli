## TASK-001 — 2026-05-18T12:05:00Z
Bootstrap task verified: branch oskar/cli-138-fix-claude-review-workflows checked out with PRD/tasks commit only ahead of main, working tree clean.
Files: ~.plans/tasks-cli-138-fix-claude-review-workflows.yml

## TASK-002 — 2026-05-18T12:15:00Z
Added "Force HTTPS for github.com git clones" step between Install govulncheck and Load security prompt into env. Sets git insteadOf so the Claude plugin installer's git clone uses HTTPS (no SSH key on runner). YAML parses; diff scoped to the new step.
Files: .github/workflows/claude-review-security.yml

## TASK-003 — 2026-05-18T12:25:00Z
Renamed conventions workflow job key security-review -> conventions-review and step "Run Claude Code (security review)" -> "Run Claude Code (conventions review)". Disambiguates the GitHub check name from the security workflow's check. YAML parses; diff is exactly the two rename lines.
Files: .github/workflows/claude-review-conventions.yml

## TASK-004 — 2026-05-18T12:35:00Z
Pushed oskar/cli-138-fix-claude-review-workflows to origin and opened draft PR https://github.com/neo4j-labs/neo4j-cli/pull/130 (title "ci: fix Claude Review SSH plugin clone + dedupe job names (CLI-138)"). Body spells out the two defects (SSH plugin clone Permission denied; duplicate security-review job id) and the corresponding fixes (insteadOf git config; rename to conventions-review), links CLI-138, notes no changie required. Verified: isDraft=true, head=oskar/cli-138-fix-claude-review-workflows, base=main, remote sha e8095de.
Files: (no code diff — PR metadata only)

## TASK-005 — 2026-05-18T13:00:00Z
Marked PR #130 ready-for-review; both workflows fired. Security run 26028637076 conclusion=failure; conventions run 26028637056 conclusion=failure. BUT both progressed past the original blockers: "Force HTTPS for github.com git clones" step ran clean (no Permission denied (publickey) anywhere in either log), Run Claude Code step started executing (bun install 161 packages, prompt loaded, plugin_marketplaces ./ + plugins cc-skills-golang@neo4j-cli-review accepted). Both runs then failed on a separate pre-existing issue: "App token exchange failed: 401 Unauthorized - Workflow validation failed. The workflow file must exist and have identical content to the version on the repository's default branch. If you're seeing this on a PR when you first add a code review workflow file to your repository, this is normal and you should ignore this error." This is anthropic claude-code-action's standard first-PR-with-new-workflow gate and self-resolves once these workflows land on main. Check names verified distinct: security-review (workflow "Claude Review — Security") + conventions-review (workflow "Claude Review — Conventions"). Both CLI-138 defects fixed. Task-006 (marketplace.json fallback) NOT needed — insteadOf rewrite worked; SSH gate fully unblocked.
Files: (no code diff — verification only)
