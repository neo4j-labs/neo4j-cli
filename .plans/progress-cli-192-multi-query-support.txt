## TASK-001 — 2026-05-29T09:30:00Z
Added splitStatements (line-walk) splitting Cypher on line-end semicolons only; strips terminating ;, trims, drops empties, handles CRLF. Table-driven tests cover single/multi/trailing-;/mid-line-;/blank-lines/CRLF/whitespace fragments.
Files: +neo4j-cli/query/split.go, +neo4j-cli/query/split_test.go, ~.plans tasks
Decisions: mid-line ; (incl. inside string literals ending a line) splits per PRD documented rule — no lexer.

## TASK-002 — 2026-05-29T10:05:00Z
Added batch single-transaction execution path in connect.go: runStatementsResponseFn seam + runStatementsResponse dispatcher (categorizeBoltError at boundary) + runStatementsResponseImpl (one session, one ExecuteRead/ExecuteWrite looping tx.Run->Collect->Consume per statement) + runStatementsWithMode unwrapper. Tests cover ordering, read/write routing, and error-surfacing (client+transient).
Files: +neo4j-cli/query/connect_batch_test.go, ~neo4j-cli/query/connect.go, ~.plans tasks

## TASK-003 — 2026-05-29T10:40:00Z
Added commonoutput.PrintBodyMaps (json array / stacked tables / toon array, reusing ResolveOutput/printTable; printToon refactored over a slice-capable printToonValue) and query.renderResults (len==1 -> existing PrintBodyMap byte-identical path, len>1 -> PrintBodyMaps with per-result columns). Not yet wired into runQuery (task-004).
Files: ~common/output/output.go, ~common/output/output_test.go, ~neo4j-cli/query/output.go, ~neo4j-cli/query/output_test.go, ~.plans tasks+progress

## TASK-004 — 2026-05-29T11:20:00Z
Registered persistent --atomic flag on query cmd and refactored runQuery to split the resolved cypher via splitStatements, keeping one-time setup (params/embeds/conn/password/openDriver) once, then branching: default mode loops per-statement (per-stmt EXPLAIN preflight when !--rw, fail-fast) while --atomic runs the preflight loop first then the whole batch via runStatementsWithMode. Truncation/warnings extracted to truncateResult, prefixing "statement N: " only when >1 statement ran; render via renderResults.
Files: ~neo4j-cli/query/run.go, ~neo4j-cli/query/query.go, ~neo4j-cli/query/run_test.go, ~neo4j-cli/internal/skill/bundle/references/query.md, ~.plans tasks+progress
Decisions: regenerated skill bundle (only the --atomic flag row in references/query.md changed) to keep make test green per task guidance — Long/Example untouched, that remains task-005.

## TASK-005 — 2026-05-29T12:05:00Z
Documented multi-statement support: query Long/Example, README "Multiple statements" section, query-additions.md + additions.md gotcha; regenerated skill bundle.
Files: ~7 mod (query.go, README.md, additions.md, query-additions.md, bundle/SKILL.md, bundle/query-additions.md, bundle/references/query.md)
Decisions: substantive query guidance went in query-additions.md (dedicated query doc) plus a one-line additions.md gotcha.

## Manual verification (live DB)
Requires a live Neo4j (e.g. `make run-neo4j` or `neo4j-cli docker create`); NOT run in CI/here.
1. Multi-read JSON array: `neo4j-cli query 'RETURN 1 AS a;
RETURN 2 AS b' --format json` -> expect a JSON array of two result envelopes in order.
2. Stacked table/toon: same two-statement read with `--format table` -> two table blocks separated by a blank line; `--format toon` -> array form.
3. --atomic rollback vs default persistence on a failing 2nd write:
   - Default: `neo4j-cli query --rw 'CREATE (:A);
THIS IS INVALID' ` -> first CREATE persists (verify `MATCH (:A) RETURN count(*)` > 0), error surfaces.
   - Atomic: clear, then `neo4j-cli query --rw --atomic 'CREATE (:A);
THIS IS INVALID'` -> whole tx rolls back (verify count == 0), error surfaces.
4. Single-statement parity: `neo4j-cli query 'RETURN 1 AS a'` and `... 'RETURN 1 AS a;'` -> output byte-identical to pre-feature single-statement output (single JSON object / single table), no `statement N:` prefixes.

## TASK-006 — 2026-05-29T09:31:37Z
Added changie Minor changelog entry (multi-statement + --atomic); all gates (test/fmt-check/lint/license-check) clean and generate-check shows no bundle drift; updated PRD acceptance checkboxes and documented live-DB manual verification steps.
Files: +.changes/unreleased/neo4j-cli-Minor-20260529-113013.yaml, ~prd, ~tasks, ~progress

## REVIEW — 2026-05-29T09:40:13Z
Added 2 review tasks (task-007..task-008).

## TASK-007 — 2026-05-29T13:05:00Z
Collapsed runStatementResponseImpl into a thin batch-of-one delegate over runStatementsResponseImpl; deleted ~65 lines of duplicated session/work/dispatch logic. Both seam vars kept independent; existing query tests pass unmodified (471).
Files: ~neo4j-cli/query/connect.go, ~.plans tasks+progress
Decisions: delegate to the impl not the categorizing wrapper, so categorizeBoltError still runs exactly once at the runStatementResponse boundary. nil-conn/nil-driver guards now sourced from the batch impl (same errors).
