feature: cli-104-help-explain-sentence-leak
prd: ./prd-cli-104-help-explain-sentence-leak.md
created_at: 2026-05-13T12:31:06Z
updated_at: 2026-05-13T12:31:06Z

tasks:
  - id: task-001
    title: "Strip query/EXPLAIN clause from root and aura `Long:` fields"
    description: |
      Edit `neo4j-cli/app/app.go:36` and `neo4j-cli/aura/aura.go:29` to remove the
      trailing sentence about `query` / EXPLAIN preflight / `--rw`-gating. Keep the
      `Write operations require --rw.` clause as the global gate hint. After the
      edits, run the binary directly to confirm the leak is gone before regenerating
      bundles. See PRD REQ-F-001, REQ-F-002.
    status: completed
    dependencies: []
    acceptance_criteria:
      - "`neo4j-cli/app/app.go:36` Long equals: `\"Allows you to manage Neo4j resources. Write operations require --rw.\"`"
      - "`neo4j-cli/aura/aura.go:29` Long equals: `\"Allows you to programmatically provision and manage your Aura resources. Write operations require --rw.\"`"
      - "`go run ./neo4j-cli --help` output does not contain the substring `EXPLAIN`"
      - "`go run ./neo4j-cli aura --help` output does not contain the substring `EXPLAIN`"
      - "`go run ./neo4j-cli aura tenant --help` output is unchanged (was already clean)"
    completed_at: 2026-05-13T12:45:00Z

  - id: task-002
    title: "Append EXPLAIN/`--rw`-gate sentence to `query` `Long:`"
    description: |
      Edit `neo4j-cli/query/query.go` to append the sentence
      `"Write operations require `--rw`; without `--rw`, an EXPLAIN preflight runs first and statements classified as writes are blocked."`
      to the end of the existing string-concatenated `Long:` (after the
      `query :embed` sentence at line 28-29). Use backticks around `--rw` to match
      the surrounding style. Verify the rendered paragraph reads naturally.
      See PRD REQ-F-003, REQ-F-004.
    status: completed
    dependencies:
      - task-001
    acceptance_criteria:
      - "`neo4j-cli/query/query.go` Long ends with the new gate sentence verbatim"
      - "New sentence is appended via string concatenation (`+` operator), not a separate field"
      - "Backticks wrap `--rw` (matching the existing `--param NAME:embed=<text>` style)"
      - "`go run ./neo4j-cli query --help` shows the new sentence as the final clause of the Long paragraph"
      - "`go run ./neo4j-cli query --help` output contains the substring `EXPLAIN preflight`"
    completed_at: 2026-05-13T12:50:00Z

  - id: task-003
    title: "Regenerate skill bundles to reflect new help text"
    description: |
      Run `go generate ./neo4j-cli/internal/skill/... ./neo4j-cli/aura/internal/skill/...`
      to refresh the committed bundle files. Without this step the
      `TestGenerator_RoundTrip` CI gate will fail with a "references/<sub>.md differs"
      error. Inspect the diff and confirm only the three expected `Long:`-derived
      surfaces changed. See PRD REQ-F-005 and AGENTS.md "Cobra Help / Skill Bundle
      Rendering Notes".
    status: completed
    dependencies:
      - task-001
      - task-002
    acceptance_criteria:
      - "`go generate ./neo4j-cli/internal/skill/... ./neo4j-cli/aura/internal/skill/...` exits 0"
      - "`neo4j-cli/internal/skill/bundle/SKILL.md` no longer contains `runs EXPLAIN first when --rw`"
      - "`neo4j-cli/internal/skill/bundle/references/aura.md` no longer contains `runs EXPLAIN first when --rw`"
      - "`neo4j-cli/internal/skill/bundle/references/query.md` contains the new EXPLAIN gate sentence"
      - "`neo4j-cli/aura/internal/skill/bundle/SKILL.md` no longer contains `runs EXPLAIN first when --rw`"
      - "`grep -rn \"runs EXPLAIN first when --rw\" .` returns no hits anywhere"
    completed_at: 2026-05-13T12:55:00Z

  - id: task-004
    title: "Add `Patch`-kind changelog entry"
    description: |
      Add a single user-facing changelog entry under `.changes/unreleased/` for the
      `neo4j-cli` project. Prefer `changie new --projects neo4j-cli --kind Patch
      --body "fix(cli): scope query/EXPLAIN --rw note to \`query --help\` only (CLI-104)"`;
      fall back to hand-authoring the YAML file named
      `neo4j-cli-Patch-<YYYYMMDD>-<HHMMSS>.yaml` per `.agents/build.md` if changie
      isn't installed locally. See PRD REQ-F-006.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "Exactly one new file exists matching `.changes/unreleased/neo4j-cli-Patch-*.yaml`"
      - "The YAML file has `project: neo4j-cli`, `kind: Patch`, and a populated `body:` referencing CLI-104"
      - "The body mentions `query --help` and the scope of the change"
      - "The YAML is valid (loads without error)"
    completed_at: null

  - id: task-005
    title: "Run final gates: `make test`, `make fmt-check`, `make lint`"
    description: |
      Run all three CI gates locally as the final check per AGENTS.md "Feedback
      Instructions". `make test` exercises `TestGenerator_RoundTrip` (bundle drift
      detector); `make fmt-check` mirrors CI's gofmt linter; `make lint` runs
      golangci-lint v2. All three must pass before the change is considered done.
      Also run a final `--help` sweep to confirm the user-visible outcome.
    status: pending
    dependencies:
      - task-001
      - task-002
      - task-003
      - task-004
    acceptance_criteria:
      - "`make test` exits 0 with no failing tests"
      - "`make fmt-check` exits 0 (no files need gofmt)"
      - "`make lint` exits 0 (no golangci-lint issues)"
      - "`go run ./neo4j-cli --help`, `… aura --help`, `… aura tenant --help`, `… aura instance --help` all show no `EXPLAIN` substring"
      - "`go run ./neo4j-cli query --help` shows the EXPLAIN gate sentence"
    completed_at: null
