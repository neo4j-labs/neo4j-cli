## task-001 — 2026-05-10T00:00:00Z
Swap install-script sha256 verify to POSIX `-c -` + stdout-redirect so BSD/Darwin sha256sum on macOS Tahoe works. Verified all 3 branches + tamper case on Darwin 25.
Files: ~distribution/installation-scripts/install-neo4j-cli.sh

## task-002 — 2026-05-10T00:00:00Z
Add ShellCheck CI workflow pinned to ludeeus/action-shellcheck v2.0.0 SHA, severity=warning, scandir scoped to distribution/installation-scripts so npm/pypi/.github/scripts/.research scripts are not pulled in. Path-filtered to install script + workflow file. Local shellcheck 0.11.0 -S warning passes clean. make test/fmt-check/lint/license-check all green.
Files: +.github/workflows/shellcheck.yml
Decisions: scandir over additional_files — keeps the action's recursive .sh discovery contained without needing to enumerate every file; future install-scripts/ siblings auto-covered.

## task-003 — 2026-05-10T19:14:36Z
Add changie Patch YAML for install-script macOS Tahoe checksum fix. Hand-authored (changie not installed locally) following AGENTS.md schema. make test/fmt-check/lint/license-check all green.
Files: +.changes/unreleased/neo4j-cli-Patch-20260510-191436.yaml
