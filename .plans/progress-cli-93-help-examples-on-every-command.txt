## task-001 — 2026-05-13T11:10:59Z
Added Example blocks to 9 aura instance leaves (list/get/delete/pause/resume/update/overwrite + snapshot.create/list/get); normalised existing create.go Example flush-left + neo4j-cli prefix. Regenerated bundles.
Files: ~create.go ~list.go ~get.go ~delete.go ~pause.go ~resume.go ~update.go ~overwrite.go ~snapshot/create.go ~snapshot/list.go ~snapshot/get.go ~bundle/references/instance.md ~bundle/references/aura.md

## task-002 — 2026-05-13T11:30:00Z
Added Example blocks to 17 aura leaves: tenant (list/get), credential (add/list/use/remove), config (list/get/set), config/project (list/add/use/remove), customer-managed-key (list/create/delete/get). Regenerated aura skill bundle + aura.md in neo4j-cli bundle.
Files: ~17 leaf .go + ~bundle/references/{config,credential,customer-managed-key,tenant}.md (aura) + ~bundle/references/aura.md (neo4j-cli)

## task-003 — 2026-05-13T11:24:45Z
Added Example blocks to 10 aura deployment leaves: top-level (list/create/delete/get), token (create/update/delete), database (list), server (list), server/database (list). Bundles unchanged because deployment is beta-gated (AuraBetaEnabled=false in gen).
Files: ~10 leaf .go
Decisions: bundle skipped on purpose — deployment subtree is mounted only under AuraBetaEnabled, generator runs with default config so references/deployment.md isn't emitted. Examples still flow to --help.

## task-004 — 2026-05-13T11:45:00Z
Added Example blocks to 13 data-api/graphql leaves: top-level (create/list/get/update/delete/pause/resume), auth-provider (create/list/delete/get), cors-policy/allowed-origin (add/remove). Bundles unchanged because data-api is beta-gated like deployment. Incidentally cleaned trailing tabs on two Long blocks (update.go, resume.go).
Files: ~13 leaf .go
Decisions: bundle skipped — same beta-gate reason as task-003. Cleaned-up trailing whitespace in Long strings is harmless (not referenced elsewhere).

## task-005 — 2026-05-13T11:34:04Z
Added Example blocks to 7 leaves: graph-analytics/session (create/list/delete/get) + import/job (create/get/cancel). Regenerated bundles (graph-analytics.md + aura.md).
Files: ~7 leaf .go + ~bundle/references/graph-analytics.md (aura) + ~bundle/references/aura.md (neo4j-cli)

## task-006 — 2026-05-13T11:37:27Z
Added Example blocks to 9 credential leaves: dbms (add/list/use/remove/set-embed) + embed (add/list/use/remove). dbms add covers both --file and explicit-flag forms. Regenerated neo4j-cli bundle (references/credential.md).
Files: ~9 leaf .go + ~bundle/references/credential.md (neo4j-cli)

## task-007 — 2026-05-13T12:00:00Z
Added Example blocks to 5 top-level leaves: config (list/get/set) + update parent + update check. config set + update use --rw where applicable (config set always; update has no write annotation so plain). Regenerated neo4j-cli bundle (references/config.md, references/update.md).
Files: ~5 leaf .go + ~bundle/references/config.md ~bundle/references/update.md (neo4j-cli)
Decisions: update parent treated as non-read/non-write — no `write: true` annotation but emits structured JSON, so examples include --format json and omit --rw to match the actual gate.
