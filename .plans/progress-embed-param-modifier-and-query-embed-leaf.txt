================================================================================
TASK-001: Add EmbedCredentials storage type and wire into Credentials
Date: 2026-05-07T11:00:00Z
================================================================================

Summary:
Added EmbedCredential / EmbedCredentials types mirroring the dbms equivalents.
Wired into CredentialsFile + Credentials with onUpdate rewire after JSON
unmarshal. Printable types omit api-key from AsArray and MarshalJSON; on-disk
credentials.json preserves api-key. First credential added becomes default.

Files Changed:
- common/clicfg/credentials/embed.go (created)
- common/clicfg/credentials/embed_test.go (created)
- common/clicfg/credentials/credentials.go (modified: added Embed field, init, onUpdate rewire, save block)

Key Decisions:
- Mirror DbmsCredentials API exactly so cobra leaf code in task-002 can be
  copy-paste from dbms.
- Add(name, provider, model, baseURL, apiKey, dimensions) — explicit positional
  signature matches Add for dbms; keeps the struct field order documented.
- Embed re-init guard in load() handles the case where credentials.json exists
  but has no "embed" key (older files written before this change).
- omitempty on CredentialsFile.Embed kept to avoid noisy `"embed":null` writes
  from older code paths; load() always reinstates a non-nil Embed before save.

Next Task: task-002

================================================================================
TASK-002: Add `credential embed {add,list,remove,use}` cobra subtree
Date: 2026-05-07T11:30:00Z
================================================================================

Summary:
Added cobra leaf cluster mirroring `credential dbms`. New package
`neo4j-cli/internal/subcommands/credential/embed/` with parent `embed.go`
plus four leaves (add/list/remove/use). Mounted on the credential parent.
Provider validation against {openai, ollama, huggingface} runs in add.go
RunE before Embed.Add so a bad provider never persists. List columns:
name, provider, model, base-url, dimensions, default — api-key never
included in any output.

Files Changed:
- neo4j-cli/internal/subcommands/credential/embed/embed.go (created)
- neo4j-cli/internal/subcommands/credential/embed/add.go (created)
- neo4j-cli/internal/subcommands/credential/embed/list.go (created)
- neo4j-cli/internal/subcommands/credential/embed/remove.go (created)
- neo4j-cli/internal/subcommands/credential/embed/use.go (created)
- neo4j-cli/internal/subcommands/credential/embed/helpers_test.go (created)
- neo4j-cli/internal/subcommands/credential/embed/add_test.go (created)
- neo4j-cli/internal/subcommands/credential/embed/list_test.go (created)
- neo4j-cli/internal/subcommands/credential/embed/remove_test.go (created)
- neo4j-cli/internal/subcommands/credential/embed/use_test.go (created)
- neo4j-cli/internal/subcommands/credential/credential.go (modified: import + AddCommand)
- neo4j-cli/internal/skill/bundle/references/credential.md (regenerated by go generate)

Key Decisions:
- validProviders kept package-private inside add.go (only validation site at
  storage time; query/embed runtime does its own switch).
- isValidProvider helper uses linear scan (3 elements; map overkill).
- helpers_test.go avoids the credential parent helper — embed.NewCmd is mounted
  directly without the parent's --rw gate, matching how dbms tests work, since
  --rw is registered by the parent only when the credential package's parent
  cmd is the entrypoint.

Next Task: task-003

================================================================================
TASK-003: Add EmbedCredential link field + SetEmbed on DbmsCredential
Date: 2026-05-07T12:00:00Z
================================================================================

Summary:
Added `EmbedCredential string` field (json:"embed-credential,omitempty") to
DbmsCredential. Added SetEmbed(dbmsName, embedName) method on DbmsCredentials
that mutates the named cred and triggers onUpdate; empty embedName clears the
link; missing dbmsName returns a usage error. Updated PrintableDbmsCredentials.
AsArray to always emit "embed-credential" (empty string when unset) so the
column is stable for table rendering and JSON output.

Files Changed:
- common/clicfg/credentials/dbms.go (modified: new field, SetEmbed, AsArray)
- common/clicfg/credentials/dbms_test.go (modified: SetEmbed table tests,
  persistence test, JSON round-trip omitempty test, AsArray/MarshalJSON
  always-includes-key test)

Key Decisions:
- SetEmbed reuses existing iteration pattern; no helper extracted (keeps the
  dbms.go file in the same shape as DbmsCredentials.Get/Remove).
- PrintableDbmsCredentials.AsArray adds the key unconditionally — table
  rendering still filters by dbmsCredentialFields in list.go (task-004 will
  append the column there); JSON output now always includes "embed-credential"
  (existing JSON tests use Contains assertions and remain green).
- omitempty on the struct keeps credentials.json clean for older creds with
  no embed link; on-the-wire `Printable` always emits the key.

Next Task: task-004

================================================================================
TASK-004: Wire dbms→embed link into cobra (`add --embed-credential`, `set-embed`, list column)
Date: 2026-05-07T12:30:00Z
================================================================================

Summary:
Added `--embed-credential` flag to `credential dbms add` (validates target
exists in Embed.Get BEFORE Dbms.Add so a bad name never half-creates a cred).
Added `credential dbms set-embed <dbms-name> [embed-name]` leaf that validates
both dbms cred and embed cred existence then calls SetEmbed; one-arg form
clears the link. Both leaves annotated `write: true`. Appended `embed-credential`
column to `dbmsCredentialFields` in list.go. Regenerated skill bundle so
`references/credential.md` reflects the new flag and leaf.

Files Changed:
- neo4j-cli/internal/subcommands/credential/dbms/add.go (modified: --embed-credential flag + pre-validation + post-Add SetEmbed)
- neo4j-cli/internal/subcommands/credential/dbms/add_test.go (modified: helper now seeds embed section; new cases for valid/invalid --embed-credential; assertNoCredsAdd flag)
- neo4j-cli/internal/subcommands/credential/dbms/dbms.go (modified: AddCommand for set-embed)
- neo4j-cli/internal/subcommands/credential/dbms/list.go (modified: appended embed-credential to dbmsCredentialFields)
- neo4j-cli/internal/subcommands/credential/dbms/list_test.go (modified: assert EMBED-CREDENTIAL column header + value)
- neo4j-cli/internal/subcommands/credential/dbms/set_embed.go (created: cobra leaf with RangeArgs(1,2), validates both creds, calls SetEmbed)
- neo4j-cli/internal/subcommands/credential/dbms/set_embed_test.go (created: write annotation check + table-driven set/clear/missing-dbms/missing-embed/arg-count cases)
- neo4j-cli/internal/skill/bundle/references/credential.md (regenerated)

Key Decisions:
- add.go does Embed.Get pre-check then Dbms.Add then Dbms.SetEmbed — three
  steps but keeps the Add signature unchanged (Add still takes the original
  5 args; embed link is a separate persistence step). Alternative was to add
  a 6th arg to Dbms.Add, but that would force Add to also know how to
  validate embed creds, leaking cobra-level concerns into the storage layer.
- set-embed errors phrase the resolution path in the message (`run \`credential
  embed list\``) rather than relying on cobra's generic Usage output, since
  the offending name comes from positional args and won't appear in help text.
- assertNoCredsAdd boolean on the test struct beats name-string matching for
  the "no half-creation" assertion — clearer + survives test renames.
- Regenerated the skill bundle here even though task-012 plans the full
  regen pass, because TestGenerator_RoundTrip otherwise fails the gate as
  soon as new flags/leaves are added.

Next Task: task-005

================================================================================
TASK-005: Add embed provider package skeleton (Provider, Config, Resolve, New, dbms-link helper)
Date: 2026-05-07T13:00:00Z
================================================================================

Summary:
Created neo4j-cli/query/embed/ package. Defined Provider interface (Embed),
Config struct (Provider, Model, BaseURL, APIKey, Dimensions, UserAgent),
package-private resolveSelectedDbmsCred helper, pickBaseEmbedCred (REQ-F-013
step 4 + REQ-F-014 + REQ-F-027 stale-link error), Resolve (stored cred <
.env < OS env < flags), provider-specific API-key precedence (per-provider
env beats generic env beats stored), New() factory with not-implemented
stubs for openai/ollama/huggingface (fleshed out in tasks 006-008), and a
providerFactory test seam exposed via WithFactory for cross-package test
injection.

Files Changed:
- neo4j-cli/query/embed/embed.go (created)
- neo4j-cli/query/embed/embed_test.go (created)

Key Decisions:
- findDotenv duplicated rather than exported from query/connect.go to keep
  the embed package free of cross-package internals (PRD says either is
  acceptable; duplication is ~15 lines and matches the existing helper
  exactly).
- API-key resolution staged: stages = [dotenv, osEnv]; within each stage
  generic key applies first then per-provider overrides. So provider-
  specific env beats generic env beats provider-specific dotenv beats
  generic dotenv beats stored. Matches the PRD-described chain.
- providerFactory exposed via WithFactory(restoreFn) closure rather than
  t.Cleanup so the helper stays test-framework-agnostic for future cross-
  package tests in tasks 010/011.
- Provider name constants (ProviderOpenAI/Ollama/HuggingFace) exported so
  the cobra add.go validator and the runtime factory share one source of
  truth.
- New() returns not-implemented errors for known providers + an invalid-
  provider usage error for unknown ones; an empty Provider returns the
  "missing embed provider" usage error since that's the most likely case
  during normal use (no --embed-provider set).
- Test commands register the same five persistent flags (plus --credential)
  the real query parent registers, then ParseFlags so cmd.Flag.Changed
  semantics match production exactly.

Next Task: task-006

================================================================================
TASK-006: Implement OpenAI embedding provider
Date: 2026-05-07T11:54:50Z
================================================================================

Summary:
Implemented `openAIProvider` in neo4j-cli/query/embed/openai.go: POST {BaseURL}/embeddings,
bearer auth, dimensions pointer-omitempty so Config.Dimensions=0 drops the field, default
BaseURL=https://api.openai.com/v1 applied at request time so a stored credential overrides
cleanly. Wired New(Config) to construct it instead of returning the not-implemented stub.
Errors: missing APIKey -> clierr usage error before any HTTP; non-2xx wraps "openai: HTTP
<code>: <body-snippet (4KiB cap)>"; auth header value never echoed.

Tests cover happy path with/without dimensions, missing key short-circuit (server fatals if
hit), non-200 + auth-leak assertion, malformed body, empty data array, default-BaseURL via
RoundTripper stub, and ctx cancellation.

ctx-cancellation gotcha: httptest server-side r.Context().Done() did NOT unblock when the
client cancelled — the handler hung until the 10-min go-test timeout. Root cause: server-side
ctx propagation from a closed client conn is best-effort and timing-dependent in this Go
version (1.26.2). Fixed by a 2s safety timeout in the handler's select and a 5s test-side
select on errCh. The test still verifies the client-side err contains "context", which is
what we actually want to assert.

Files Changed:
- neo4j-cli/query/embed/openai.go (new — provider impl)
- neo4j-cli/query/embed/openai_test.go (new — 8 tests, 1 stub RoundTripper)
- neo4j-cli/query/embed/embed.go (New() wires ProviderOpenAI to newOpenAIProvider)
- neo4j-cli/query/embed/embed_test.go (TestNew_NotImplementedStubs minus openai; new TestNew_OpenAIReturnsProvider)

Key Decisions:
- Default BaseURL applied at request time (not in Resolve) so credential.BaseURL="" overrides cleanly.
- Dimensions as *int with omitempty — OpenAI rejects an explicit `0` for models that ignore the field.
- Body snippet capped at 4KiB for non-2xx error wrap to avoid spamming on misconfigured-proxy HTML.
- Always add a server-side handler timeout when testing client-cancellation paths via httptest.

Next Task: task-007 (Ollama provider)

================================================================================
TASK-007: Implement Ollama embedding provider
Date: 2026-05-07T13:30:00Z
================================================================================

Summary:
Implemented `ollamaProvider` in neo4j-cli/query/embed/ollama.go: POST {BaseURL}/api/embed
with no auth header (Config.APIKey ignored intentionally), body `{model, input}` only —
no `dimensions` field regardless of Config.Dimensions. Default BaseURL=http://localhost:11434
applied at request time so a stored credential pointing at a remote Ollama host overrides
cleanly. Wired New(Config) to construct it instead of returning the not-implemented stub.
Errors: non-2xx wraps "ollama: HTTP <code>: <body-snippet (4KiB cap)>".

Tests cover happy path, dimensions ignored, no-auth-header even with APIKey populated,
non-2xx + body propagation, malformed body, empty embeddings array, ctx cancellation,
default-BaseURL via captureRoundTripper stub.

Files Changed:
- neo4j-cli/query/embed/ollama.go (new — provider impl)
- neo4j-cli/query/embed/ollama_test.go (new — 8 tests, reuses captureRoundTripper from openai_test.go)
- neo4j-cli/query/embed/embed.go (New() wires ProviderOllama to newOllamaProvider)
- neo4j-cli/query/embed/embed_test.go (TestNew_NotImplementedStubs minus ollama; new TestNew_OllamaReturnsProvider)

Key Decisions:
- Mirror openAIProvider structure exactly — same field names (cfg, client), same idioms
  (4KiB body snippet cap, defer Close with nolint, default-BaseURL at request time).
- Reuse the package-private captureRoundTripper from openai_test.go for the default-BaseURL
  test rather than defining a parallel one — both files live in the same `package embed`.
- Explicit assertion in TestOllama_Embed_NoAuthHeaderEvenWithAPIKey that Authorization is
  empty even when Config.APIKey is set — guards against a future refactor that mistakenly
  pulls in the OpenAI auth pattern.

Next Task: task-008 (HuggingFace provider)

================================================================================
TASK-008: Implement HuggingFace embedding provider (serverless + dedicated)
Date: 2026-05-07T14:00:00Z
================================================================================

Summary:
Implemented `huggingFaceProvider` in neo4j-cli/query/embed/huggingface.go: POST to
{BaseURL}/{Model} when BaseURL == default (serverless mode) or to {BaseURL} verbatim
when overridden (dedicated endpoint mode). Bearer auth, body `{inputs: text}`. Tolerates
both [[floats]] (typical serverless shape) and [floats] (some dedicated endpoints) by
attempting nested unmarshal first then falling back to flat. Default
BaseURL=https://router.huggingface.co/hf-inference/models. Wired New(Config) to construct
it instead of returning the not-implemented stub.

Tests cover happy paths (serverless+nested, dedicated+flat), cross-shape tolerance
(serverless+flat, dedicated+nested), missing key short-circuit, non-200 + auth-leak
assertion, malformed body, empty arrays (both shapes), ctx cancellation, and default-
BaseURL via a serverlessRedirectTransport that rewrites only the host so the path
construction remains under test.

Files Changed:
- neo4j-cli/query/embed/huggingface.go (new — provider impl, mode selection in Embed)
- neo4j-cli/query/embed/huggingface_test.go (new — 11 tests, custom serverlessRedirectTransport)
- neo4j-cli/query/embed/embed.go (New() wires ProviderHuggingFace; dropped unused fmt import)
- neo4j-cli/query/embed/embed_test.go (removed TestNew_NotImplementedStubs; new TestNew_HuggingFaceReturnsProvider)

Key Decisions:
- Mode selection lives in Embed() (one branch on `base == defaultHuggingFaceBaseURL`)
  rather than at provider construction so a future Config mutation between New and
  Embed (currently impossible but harmless to support) routes correctly.
- Body is read once into []byte then attempted as [[floats]] then [floats] — simpler
  than peeking the first byte of the JSON and lets json.Unmarshal own all whitespace
  handling. The double-decode cost is irrelevant at embedding-vector size.
- serverlessRedirectTransport (test-only) rewrites the default scheme+host to the
  httptest server while preserving the /hf-inference/models/<Model> path, so the
  default-BaseURL test asserts on the path the provider actually constructs rather
  than on the request URL alone.
- Mirror openAIProvider structure exactly — same field names, same idioms (4KiB body
  snippet cap, defer Close with nolint, default-BaseURL at request time, lazy APIKey
  check). Keeps the file diffable against openai.go for future maintainers.

Next Task: task-009

================================================================================
TASK-009: Extend parseParams to recognise the `:embed` modifier
Date: 2026-05-07T14:30:00Z
================================================================================

Summary:
Extended parseParams to return (map[string]any, []EmbedJob, error). Detects
`key:modifier` by splitting on first `:` in the key portion (before `=`).
Only `embed` is recognised; any other modifier returns a usage error. With
`:embed`, JSON-typing is disabled (text passes through verbatim) — except a
JSON array literal is rejected as a guardrail. Empty Text accepted. Multiple
`--param NAME:embed=...` entries are collected in order. Errors now use
clierr.NewUsageError directly, so run.go just returns err.

Files Changed:
- neo4j-cli/query/params.go (rewrote: new signature, helpers, EmbedJob type)
- neo4j-cli/query/params_test.go (added embed/modifier cases; renamed want -> wantParams + wantEmbeds)
- neo4j-cli/query/run.go (caller: 3-return tuple, no longer wraps with clierr)

Key Decisions:
- splitKeyModifier helper kept private — only one call site, but extracted
  for readability and to make the "first `:` wins" rule explicit in code.
- looksLikeJSONArray short-circuits on `[` prefix before attempting full
  unmarshal — keeps the happy-path string check O(1) for non-array text.
- Switched parseParams errors to clierr.NewUsageError (was fmt.Errorf with
  caller wrapping in run.go). One-step is cleaner; usage-error semantics
  are preserved at the entry point regardless.
- EmbedJob exported (capital E) — task-010 in run.go and task-011 in
  embed.go both consume it, so package-private isn't enough.
- `:embed` value is appended verbatim including any `=` — `q:embed=a=b`
  yields Text=`"a=b"`. Matches existing literal-param behaviour for `=`
  in values.

Next Task: task-010

================================================================================
TASK-010: Register `--embed-*` flags on `query` parent and wire embed resolution into runQuery
Date: 2026-05-07T15:00:00Z
================================================================================

Summary:
Registered five new persistent flags on the query parent (--embed-credential,
--embed-provider, --embed-model, --embed-base-url, --embed-dimensions). Added
resolveEmbedJobs in run.go that calls embed.Resolve + embed.Factory()(cfg) +
provider.Embed for each EmbedJob, populating params before resolveConn (so
embed errors short-circuit before any driver is opened). The same params map
then feeds both the EXPLAIN preflight and the real run, so a vector is
computed exactly once per invocation regardless of --rw routing.

Files Changed:
- neo4j-cli/query/query.go (5 new persistent flags)
- neo4j-cli/query/run.go (parseParams 3-tuple, resolveEmbedJobs helper, embed import)
- neo4j-cli/query/run_test.go (5 new tests + stubEmbedProvider helper; embed import)
- neo4j-cli/internal/skill/bundle/references/query.md (regenerated)

Key Decisions:
- resolveEmbedJobs short-circuits when len(embeds) == 0 so the embed package
  is never consulted for queries that don't use :embed. Keeps the cold path
  zero-cost for the 99% case.
- Embed resolution happens BEFORE resolveConn / promptPassword / openDriver
  so a missing provider key (or any embed error) aborts before we touch the
  database — protects users from a 5s connect timeout when the real failure
  was OPENAI_API_KEY.
- Provider error wrap format `query: embed %q: %w` mirrors the existing
  `query: read stdin` / `query: read password` idiom for stderr presentation
  consistency.
- Tests use `embed.WithFactory(...)` (the cross-package seam) rather than
  reaching into the unexported providerFactory directly. The driverOpener
  panic in TestRunQuery_EmbedParam_ProviderErrorAbortsBeforeExplain is the
  hard signal that we never reach driver construction on the embed-error
  path.

Next Task: task-011

================================================================================
TASK-011: Add `query :embed [text]` standalone leaf and shared input helper
Date: 2026-05-07T15:30:00Z
================================================================================

Summary:
Created neo4j-cli/query/input.go with readPositionalOrStdin(cmd, args, label).
Refactored resolveCypher to delegate to it (one-liner now). Created
neo4j-cli/query/embed.go with newEmbedCmd + runEmbed + embedVector type
implementing ResponseData (AsArray) and json.Marshaler. Mounted on query
parent. The leaf does NOT open a Bolt driver and does NOT prompt for
password. Tests cover positional/stdin paths, json/table/toon rendering,
provider error propagation, factory error propagation, AsArray/MarshalJSON
shape locks, and a hard-fail driverOpener+statementSeam to prove no DB
connection is opened. Regenerated skill bundle so references/query.md
reflects the new :embed sub-leaf.

Files Changed:
- neo4j-cli/query/input.go (created)
- neo4j-cli/query/run.go (resolveCypher delegates to readPositionalOrStdin; dropped strings import)
- neo4j-cli/query/embed.go (created — newEmbedCmd, runEmbed, embedVector)
- neo4j-cli/query/embed_test.go (created — 9 tests)
- neo4j-cli/query/query.go (cmd.AddCommand(newEmbedCmd(cfg)))
- neo4j-cli/internal/skill/bundle/references/query.md (regenerated)

Key Decisions:
- readPositionalOrStdin parameterised by label string ("Cypher" / "text") so
  the helper can be reused by future `:foo [bar]` leaves with consistent
  diagnostic wording. Old "cypher statement is empty" message becomes
  "Cypher is empty" — no tests pinned to the old wording.
- embedVector.AsArray returns a single row {"embedding": "<compact JSON>"}
  rather than a multi-row "one float per row" view. Single row keeps the
  table compact for short vectors AND avoids huge tables for 1536-dim
  OpenAI vectors. Header passed to PrintBodyMap is "embedding".
- embedVector.MarshalJSON returns the raw float array (not wrapped in an
  object) so json/toon outputs stay machine-friendly for downstream piping.
- Empty/nil vector marshals to `[]` (test-locked) rather than `null` to
  preserve the array contract for consumers.
- runEmbed wraps provider errors as `query: embed: %w` (mirrors run.go's
  `query: embed %q: %w` for the per-job case). Test asserts contains
  "embed" rather than the exact prefix to stay resilient to future tweaks.
- Test for "no Bolt connection / no password prompt" uses a panicking
  driverOpener AND a t.Fatal-ing passwordReader AND a t.Fatal-ing
  statement seam — three independent guards prove the leaf is fully
  provider-side.
- Regenerated bundle here (rather than waiting for task-012) because
  TestGenerator_RoundTrip otherwise gates `make test` red as soon as the
  new leaf is added.

Next Task: task-012
