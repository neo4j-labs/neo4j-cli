## 2026-05-28 task-001
Dropped --await binding from common/flags and aura delegator + pruned wait_test.go.
- common/flags/wait.go: removed AwaitFlagAlias const, second BoolVar, MarkDeprecated block; updated RegisterWait doc.
- neo4j-cli/aura/internal/flags/wait.go: removed AwaitFlagAlias re-export; updated comments.
- neo4j-cli/aura/internal/flags/wait_test.go: reduced to two table rows (neither + --wait); dropped deprecationMsg, wantDeprecated, stderr capture; HidesAliasFromHelp retained.
Gates: go build OK; go test ./common/flags/... ./neo4j-cli/aura/internal/flags/... OK; make fmt-check OK; make lint OK.
Note: full `make test` fails on TestCreate_Wait_AwaitAlias_StillWorks — that test is deleted in task-002 (expected).

## 2026-05-28 task-002
Stripped --await from docker start/stop Examples and deleted TestCreate_Wait_AwaitAlias_StillWorks.
- docker/start.go: removed trailing --await example pair; existing TCP-fallback line retained for the 3rd invocation.
- docker/stop.go: removed --await example pair; added "staging --wait --rw" as the 3rd invocation to stay ≥3.
- docker/create_test.go: deleted TestCreate_Wait_AwaitAlias_StillWorks (lines 826-860).
Gates: make fmt-check OK; make lint OK; docker + agentcontext tests pass; TestGenerator_RoundTrip fails as expected (bundle regen is task-004).

## 2026-05-28 task-003
Dropped --await deprecation sentence from CONTRIBUTING.md §3.5.
- CONTRIBUTING.md: removed the trailing sentence on line 211; --wait narrative kept unchanged.
Gates: make fmt-check OK; make lint OK; make test OK except TestGenerator_RoundTrip (expected; owned by task-004). No new failures.

## 2026-05-28 task-004
Regenerated neo4j-cli skill bundle.
- ran go generate ./neo4j-cli/internal/skill/...
- bundle diff: only neo4j-cli/internal/skill/bundle/references/docker.md changed — two --await example blocks removed; "docker stop staging --wait --rw" added (from task-002 source edit).
Gates: make fmt-check OK; make lint OK; make test OK (TestGenerator_RoundTrip now green); make generate-check noise limited to the hone in_progress YAML flip + the bundle work product (expected per AGENTS.md).

## 2026-05-28 task-005
Added Minor changelog entry for --await alias removal.
- changie new --projects neo4j-cli --kind Minor --body "Removed deprecated --await alias; --wait is now the only spelling (CLI-111)." → .changes/unreleased/neo4j-cli-Minor-20260528-104616.yaml.
Gates: make fmt-check OK; make lint OK; make test OK; make generate-check noise limited to hone in_progress YAML flip (no Go/bundle drift, expected).

## 2026-05-28 task-006
Final gates + manual CLI smoke verification.
Gates: make fmt-check OK; make lint OK (0 issues); make test OK (no FAILures); make license-check OK; make generate-check noise limited to hone in_progress YAML flip (no Go/bundle drift, expected per AGENTS.md).
Build: make build → bin/neo4j-cli (14.7M).
Smoke checks (all pass):
- 4a `aura instance create --type free-db --await --name x --tenant-id t --rw` → exit 2, stderr `Error: unknown flag: --await`.
- 4b `aura instance create --type free-db --wait --name x --tenant-id t --rw` → exit 3, no `unknown flag`; reaches project-lookup logic and fails on dev-env credential/project — acceptable.
- 4c `docker start dev --await --rw` → exit 2, stderr `Error: unknown flag: --await`.
- 4d `docker stop dev --await --rw` → exit 2, stderr `Error: unknown flag: --await`.
- 4e `aura instance create --help` → stdout contains `--wait`, contains 0 occurrences of `await`.
Grep sweep: production-source `--await` references = 0 across common/, neo4j-cli/, CONTRIBUTING.md, README.md, AGENTS.md. Only remaining `--await` literal in repo source is the negative regression in `neo4j-cli/aura/internal/flags/wait_test.go:61` (`assert.NotContains(t, usage, "--await")`) — expected.

## REVIEW — 2026-05-28T12:00:00Z
Added 2 review tasks (task-007..task-008). Investigation revealed TestAllLeafCommands_HaveExamples enforces only non-empty + flush-left first line — the "≥3 invocations" in AGENTS.md is convention, not gate. task-008 drops docker stop/start example padding and relaxes the doc convention to ≥2.
