feature: cli-172-skill-install-antigravity
prd: ./prd-cli-172-skill-install-antigravity.md
created_at: 2026-05-21T06:28:24Z
updated_at: 2026-05-21T06:28:24Z

tasks:
  - id: task-001
    title: "Add antigravity entry to agent catalog and update order-locking test"
    description: |
      Insert a new `Agent` literal into the `AGENTS` slice in `common/skill/agents.go`
      immediately before the `gemini-cli` entry, with Name="antigravity",
      DisplayName="Antigravity", DetectDir="~/.gemini/antigravity",
      SkillsDir="~/.gemini/antigravity/skills". In the same change, update
      `TestAGENTSCatalog` in `common/skill/agents_test.go` so the `expected`
      slice contains "antigravity" immediately before "gemini-cli" (catalog
      order). Both edits must land together since the test enforces catalog
      ordering and length via `require.Len(t, AGENTS, len(expected))`.
    status: completed
    dependencies: []
    acceptance_criteria:
      - "New `AGENTS` entry exists with the four field values from REQ-F-001 and is positioned immediately before the `gemini-cli` line in `common/skill/agents.go`."
      - "`TestAGENTSCatalog` `expected` slice in `common/skill/agents_test.go` contains \"antigravity\" immediately before \"gemini-cli\" matching catalog order."
      - "`go test ./common/skill/...` passes (TestAGENTSCatalog, TestFindAgent, TestDetectAgents, TestAgentDetectAndSkillsPath all green)."
      - "`make fmt-check` and `make lint` pass."
      - "`make generate-check` stays clean — no bundle regeneration triggered."
    completed_at: 2026-05-21T06:35:00Z

  - id: task-002
    title: "Update README supported-agents list"
    description: |
      Edit the "Supported agents:" sentence on line 41 of `README.md` to insert
      `Antigravity, ` immediately before `Gemini CLI`, so the README order
      matches catalog order after the new entry lands.
    status: pending
    dependencies:
      - task-001
    acceptance_criteria:
      - "README.md `## Agent skills` section lists `Antigravity` immediately before `Gemini CLI` in the comma-separated supported-agents sentence."
      - "No other content in README.md is modified."
      - "Doc order matches the order of entries in `common/skill/agents.go` `AGENTS`."
    completed_at: null

  - id: task-003
    title: "Add changie patch entry for Antigravity support"
    description: |
      Run `changie new --projects neo4j-cli --kind Patch --body "Add skill
      install support for Antigravity CLI."` to create a YAML entry under
      `.changes/unreleased/`. If `changie` is not on PATH, hand-author the
      equivalent file under `.changes/unreleased/` named
      `neo4j-cli-Patch-<YYYYMMDD>-<HHMMSS>.yaml` with fields `project: neo4j-cli`,
      `kind: Patch`, `body: 'Add skill install support for Antigravity CLI.'`,
      and an RFC3339 `time:` (per `.agents/` notes).
    status: pending
    dependencies:
      - task-001
    acceptance_criteria:
      - "One new YAML file exists under `.changes/unreleased/` with `project: neo4j-cli`, `kind: Patch`, body matching the task description, and a valid RFC3339 `time:` field."
      - "No other unreleased changelog entries are added or modified."
      - "`make test` still passes (changelog dir is not gated by tests, but sanity-check there is no regression)."
    completed_at: null
