## TASK-001 — 2026-05-28T00:00:00Z
Promoted golang.org/x/oauth2 to a direct dep ahead of the vertex provider. Added a small placeholder file in neo4j-cli/query/embed/ that imports oauth2/google so `go mod tidy` keeps the require direct; helpers will be wired in by task-006.
Files: ~go.mod, ~go.sum, +neo4j-cli/query/embed/vertex_deps.go
Decisions: go mod tidy strips truly-unused modules, so a tiny `//nolint:unused`-tagged stub (vertexScope + findDefaultTokenSource) keeps oauth2 direct until task-006 lands; transitive footprint stayed at oauth2 + cloud.google.com/go/compute/metadata as predicted.

## TASK-002 — 2026-05-28T00:00:00Z
Added VertexProject/VertexLocation omitempty fields to EmbedCredential; extended EmbedCredentials.Add with two trailing string params; updated the sole production call site (credential/embed/add.go pass-through with empty strings) and all test call sites. PrintableEmbedCredentials.AsArray now adds vertex-* keys only when non-empty.
Files: ~common/clicfg/credentials/embed.go, ~common/clicfg/credentials/embed_test.go, ~neo4j-cli/internal/subcommands/credential/embed/add.go

## TASK-003 — 2026-05-28T00:00:00Z
Added ProviderGemini/ProviderVertex + envGeminiKey/envGoogleKey constants, VertexProject/VertexLocation Config fields, Resolve copy from stored cred, and extended New default-case "must be one of" message. New providers still fall through to the default until tasks 005/006 land.
Files: ~neo4j-cli/query/embed/embed.go, ~neo4j-cli/query/embed/embed_test.go
Decisions: envGoogleKey is unused this commit (wired in task-004); kept it co-located with envGeminiKey for grep-locality.

## TASK-004 — 2026-05-28T00:00:00Z
Extended resolveAPIKey with a ProviderGemini case (GOOGLE_API_KEY then GEMINI_API_KEY within each stage, so gemini-specific wins). Added both new vars to osEnvSnapshot and clearEmbedEnv. Added 7 table-driven cases covering stored-only, no-env, OS env precedence ladder, `.env` vs OS env, and dotenv-only ordering. Vertex deliberately omitted (ADC, not API key).
Files: ~neo4j-cli/query/embed/embed.go, ~neo4j-cli/query/embed/embed_test.go

## TASK-005 — 2026-05-28T00:00:00Z
Added geminiProvider with x-goog-api-key auth, {base}/models/{model}:embedContent URL, taskType RETRIEVAL_QUERY, optional outputDimensionality, L2-normalization when Dimensions > 0 && != 3072 (zero-vec passthrough), and 4KiB body snippet on non-2xx. Wired `case ProviderGemini` into New. Test suite covers happy path, dims-omitted, dims-native (no normalize), dims-other (normalize math), zero-vector, missing key, non-2xx (no key leak), empty values, malformed JSON, blocked-URL SSRF, default base URL, and New factory.
Files: +neo4j-cli/query/embed/gemini.go, +neo4j-cli/query/embed/gemini_test.go, ~neo4j-cli/query/embed/embed.go
Decisions: Inlined a small l2Normalize helper (unexported) in gemini.go rather than a shared math package — it's the only call site and the function is six lines. Native-dim guard set to 3072 to match REQ-F-027.

## TASK-006 — 2026-05-28T00:00:00Z
Added vertexProvider with ADC OAuth2 auth (lazy via sync.Once), location-derived URL ignoring cfg.BaseURL, task_type snake_case body, omitempty parameters when Dimensions==0, no L2-normalization, 4KiB body snippet on non-2xx. Wired `case ProviderVertex` into New. Two package-level test seams: findDefaultTokenSource (oauth2.TokenSource stub) and vertexURLHostPrefix (httptest redirect). Test suite covers missing project/location, missing ADC, happy path, dimensions overlay, non-2xx (no token leak), empty predictions, malformed JSON, default URL, BaseURL-ignored, New factory. Folded vertexScope + findDefaultTokenSource from the task-001 placeholder into vertex.go and deleted vertex_deps.go.
Files: +neo4j-cli/query/embed/vertex.go, +neo4j-cli/query/embed/vertex_test.go, ~neo4j-cli/query/embed/embed.go, -neo4j-cli/query/embed/vertex_deps.go

## TASK-007 — 2026-05-28T00:00:00Z
Extended `credential embed add` with gemini + vertex in validProviders, two new flags (`--vertex-project`, `--vertex-location`), cross-flag validation (vertex requires both; non-vertex rejects strays), updated Long/Example, passes new args into EmbedCredentials.Add. Added 5 new table-driven test cases. Skill bundle is now drifted (expected; task-008 regenerates).
Files: ~neo4j-cli/internal/subcommands/credential/embed/add.go, ~neo4j-cli/internal/subcommands/credential/embed/add_test.go

## TASK-008 — 2026-05-28T00:00:00Z
Regenerated skill bundle via `go generate ./neo4j-cli/internal/skill/...`; only `references/credential.md` drifted (new providers in `--provider` list, two new `--vertex-*` flag rows, gemini + vertex Example invocations). All gates green.
Files: ~neo4j-cli/internal/skill/bundle/references/credential.md

## TASK-009 — 2026-05-28T14:28:21Z
Added Minor changie entry for the new Gemini + Vertex AI embedding providers via `changie new`.
Files: +.changes/unreleased/neo4j-cli-Minor-20260528-142821.yaml

## REVIEW — 2026-05-28T15:00:00Z
Added 2 review tasks (task-010..task-011).

## TASK-010 — 2026-05-28T12:42:14Z
Fixed gemini/vertex examples in credential embed add to use `gemini-embedding-001 --dimensions 3072` (matches DI graph default; PRD acceptance model). Regenerated skill bundle.
Files: ~neo4j-cli/internal/subcommands/credential/embed/add.go, ~neo4j-cli/internal/skill/bundle/references/credential.md
