================================================================================
TASK-001: Add skill-auto-refresh bool config key to GlobalConfig
Date: 2026-05-14T00:00:00Z
================================================================================

Summary:
Added "skill-auto-refresh" as a boolean GlobalConfig key with default true. The key
is visible to `neo4j-cli config set skill-auto-refresh false` and readable via
cfg.Global.Get("skill-auto-refresh"). Two affected config-list tests were updated to
include the new key in their expected JSON output.

Files Changed:
- common/clicfg/clicfg.go (modified — added "skill-auto-refresh" to ValidConfigKeys, SetDefault, and Set validation)
- neo4j-cli/internal/subcommands/config/list_test.go (modified — updated expected JSON output in 2 test cases)
- neo4j-cli/aura/internal/subcommands/config/standalone_config_test.go (modified — updated expected JSON output in 1 test case)

Key Decisions:
- Followed exact same pattern as "telemetry" key: added to ValidConfigKeys slice, SetDefault, and boolean validation in Set()
- The key sorts alphabetically between "format" and "telemetry" in JSON output

Next Task: task-002

================================================================================
TASK-002: Create skillrefresh package — state file I/O (cache.go)
Date: 2026-05-14T00:00:00Z
================================================================================

Summary:
Created neo4j-cli/internal/skillrefresh/cache.go with state file I/O following
the versioncheck pattern. Also created a stub skillrefresh.go to satisfy the
linter (Go requires at least one non-test .go file to declare the package).

Files Changed:
- neo4j-cli/internal/skillrefresh/cache.go (new — Neo4j copyright header, cacheEntry
  struct with LastRefreshedVersion string, cachePath() resolving to
  clicfg.ConfigPrefix/neo4j/cli/skill-refresh.json, readCache/writeCache using
  afero.Fs with silent error swallowing and mode-0600 writes)
- neo4j-cli/internal/skillrefresh/skillrefresh.go (new stub — package declaration only,
  satisfies Go compiler for the package to be importable)

Key Decisions:
- Followed the same afero.Fs injection pattern as versioncheck/cache.go
- All I/O errors silently swallowed: absent/malformed cache returns nil cacheEntry
- writeCache uses a deferred recover() to swallow any panics internally
- Mode 0600 enforced via afero.WriteFile with os.FileMode(0600)

Gates:
- make test: PASS
- make fmt-check: PASS
- make lint: PASS

Next Task: task-003
