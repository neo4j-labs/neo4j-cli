================================================================================
TASK-001: Add SetActiveCredential/ActiveCredential to AuraConfig
Date: 2026-05-06T00:00:00Z
================================================================================

Summary:
Added an unexported `activeCredential *credentials.AuraCredential` field to the
`AuraConfig` struct in `common/clicfg/clicfg.go`, along with `SetActiveCredential`
and `ActiveCredential` methods. The field is a pure per-invocation in-memory
override — never serialized to disk or bound to viper. Unit tests added in
`common/clicfg/clicfg_test.go` covering nil/unset, set, and overwrite cases.

Files Changed:
- common/clicfg/clicfg.go (modified: added activeCredential field and two methods)
- common/clicfg/clicfg_test.go (modified: added TestAuraConfigActiveCredential with 3 table-driven cases)

Key Decisions:
- Placed methods adjacent to the existing SetBetaEnabled/AuraBetaEnabled pattern for consistency
- Used table-driven tests per project conventions in AGENTS.md
- Added the credentials import to the test file (credentials package is already a clicfg dependency)

Next Task: task-002 (Update MakeRequest() to use ActiveCredential with GetDefault() fallback)

================================================================================
TASK-002: Update MakeRequest() to use ActiveCredential with GetDefault() fallback
Date: 2026-05-06T00:00:00Z
================================================================================

Summary:
Replaced `cfg.Credentials.Aura.GetDefault()` in `MakeRequest()` with a branch: if
`cfg.Aura.ActiveCredential()` is non-nil use it directly; otherwise fall back to `GetDefault()`.
Added table-driven unit tests covering all three resolution paths.

Files Changed:
- neo4j-cli/aura/internal/api/api.go (modified: credential resolution branch added, credentials import added)
- neo4j-cli/aura/internal/api/api_test.go (created: 3-case table test for MakeRequest credential resolution)

Key Decisions:
- No error handling in the active-credential branch — validation happens upstream in PersistentPreRunE
- Test includes "active-cred" in the credentials store to mirror real production flow from task-003
- Named result `err` is reused (not shadowed) to avoid subtle bugs

Next Task: task-003

================================================================================
TASK-003: Implement RegisterAuraCredentialFlag() helper in common/flags
Date: 2026-05-06T00:00:00Z
================================================================================

Summary:
Added `RegisterAuraCredentialFlag(cmd, cfg)` to `common/flags/flags.go`. The helper adds a
persistent `--credential`/`-c` flag and wraps any existing `PersistentPreRunE` in a chain that
runs the prior hook first, then resolves the named credential from the store and calls
`cfg.Aura.SetActiveCredential`. Binary-specific error hints use `cmd.Root().Use`.
7 unit tests cover all acceptance criteria paths.

Files Changed:
- common/flags/flags.go (modified: added RegisterAuraCredentialFlag)
- common/flags/flags_test.go (created: 7 unit tests covering all hook paths)

Key Decisions:
- Captures `prior := cmd.PersistentPreRunE` before overwriting to enable correct chaining
- Uses `cmd.Flag("credential").Changed` (not value != "") to detect explicit flag setting
- Error message uses plain ASCII comma instead of em dash for codebase consistency
- Binary hint: root.Use == "neo4j-cli" → "neo4j-cli aura credential list"; else → "aura-cli credential list"

Next Task: task-004
