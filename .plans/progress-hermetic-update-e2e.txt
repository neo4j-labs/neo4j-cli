## TASK-001 — 2026-05-12T08:30:51Z
Authored release_fixture binary + canned releases JSON for both scenarios. Pure stdlib HTTP fixture serving /repos/neo4j-labs/neo4j-cli/releases from go:embed'd testdata; --scenario {stable-head|pre-release-head} required. Smoke-verified e2e-seams CLI returns channel=stable+latest=v9.9.0 for stable-head and channel=pre-release+latest=v9.9.1-alpha.1 for pre-release-head (both with --pre-releases). NEO4J_CLI_UPDATE_E2E_ALLOW_HTTP=1 required at runtime alongside the API_URL seam (HTTPS-pin off + 127.0.0.1 allowlist) — workflow step in task-003 must set both.
Files: +test/e2e/release_fixture/main.go, +test/e2e/release_fixture/testdata/releases-stable-head.json, +test/e2e/release_fixture/testdata/releases-pre-release-head.json
Decisions: Embed via //go:embed (cwd-independent, single binary). Reviewer phase skipped — Task/subagent tool unavailable in this harness.

## TASK-002 — 2026-05-12T08:33:18Z
Added --schema-only mode to check_json: enforces shape + types + post-swap-absence + enum membership (channel∈{stable,pre-release}, install_method∈{binary,homebrew,npm,pipx,uv}) without value coupling. Mutex with --expect-channel/--cross-check checked before stdin read. Updated package comment with new Modes section. Smoke-verified 10 cases (valid stable, valid prerelease, malformed JSON, non-semver latest, bad channel enum, bad install_method enum, missing key, stray updated_skills, two mutex violations, legacy default-mode regression). All four mandatory gates clean.
Files: ~test/e2e/check_json/main.go
Decisions: Map-based enum sets (lookup readability over slice scan). Reviewer phase: self-review (Task/subagent tool unavailable).

## TASK-003 — 2026-05-12T08:36:28Z
Rewrote Tier-1 workflow step "Update e2e (check subcommand)" into hermetic two-scenario block. Build step now compiles bin/neo4j-cli-e2e with -tags e2e_seams (replaces previous untagged build); both scenarios share the binary (live smoke in task-004 will reuse it without seam env vars). Bash loop iterates "stable-head:stable:v9.9.0 pre-release-head:pre-release:v9.9.1-alpha.1": launch release_fixture in background, scrape port from first stdout line (60s poll loop, mirrors Tier-2), export NEO4J_CLI_UPDATE_E2E_API_URL + NEO4J_CLI_UPDATE_E2E_ALLOW_HTTP=1, pipe `update check --pre-releases -f json` to check_json with --expect-channel + --cross-check, kill+wait+`trap - EXIT` between iterations. No gh CLI / GITHUB_TOKEN env block on the step. RUNNER_OS=Windows case adds .exe to fixture+cli paths with fallback to non-.exe for local sim. Local sim against both scenarios: both pass with channel=stable+latest=v9.9.0 and channel=pre-release+latest=v9.9.1-alpha.1. All four mandatory gates clean (make fmt-check, license-check, lint, test).
Files: ~.github/workflows/test.yml
Decisions: Single iteration loop with `for sc in ${scenarios}` (space-delimited triples) — clearer than function abstraction at this size. Trap reset (`trap - EXIT`) between iterations so a stale PID can't be killed by a later trap fire. Build tag added to Tier-1 binary primarily for the seams but harmless for the schema-only live smoke in task-004 (init checks env-var non-empty). Reviewer phase: self-review (Task/subagent unavailable).

## TASK-004 — 2026-05-12T08:38:41Z
Added "Update e2e (schema-only live smoke)" step in .github/workflows/test.yml immediately after the hermetic Tier-1 block, before Tier-2. Reuses bin/neo4j-cli-e2e (built earlier with -tags e2e_seams); does NOT export NEO4J_CLI_UPDATE_E2E_API_URL/ALLOW_HTTP, so seams_e2e.go's apiBaseURL shim falls through to https://api.github.com. Pipes `update check --pre-releases -f json` to `go run ./test/e2e/check_json --schema-only`. RUNNER_OS=Windows .exe path mirror identical to Tier-1. No --expect-channel / --cross-check, so calendar-immune. Local live smoke verified: channel=stable, latest=v1.0.0, install_method=binary → schema-only passes. All four mandatory gates clean (make fmt-check, license-check, lint, test); YAML parses via PyYAML.
Files: ~.github/workflows/test.yml
Decisions: Defensive `set +e` around the cli invocation mirrors Tier-1's rationale (exit-code drift in cobra path should not pre-empt JSON assertion). Reviewer phase: self-review (Task/subagent unavailable).
