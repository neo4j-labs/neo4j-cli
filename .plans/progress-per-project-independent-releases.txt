2026-05-01T00:00:00Z task-001 completed
  Added "Detect unreleased changes per project" step (id: detect) to .github/workflows/changie.yml.
  Step uses grep -rl to check .changes/unreleased/ for 'project: aura-cli' and 'project: neo4j-cli'
  and writes has_aura and has_neo4j boolean outputs to GITHUB_OUTPUT. Placed before both batch steps.
  Reviewer confirmed no blocking issues.

2026-05-01T00:00:00Z task-002 completed
  Gated all batch/merge/version/PR steps on detect outputs in .github/workflows/changie.yml.
  - "Batch aura-cli changes": if: steps.detect.outputs.has_aura == 'true'
  - "Batch neo4j-cli changes": if: steps.detect.outputs.has_neo4j == 'true'
  - "Merge changelogs", "Get aura-cli/neo4j-cli latest version", "Extract version numbers": gated on either project having changes
  - Added "Skip — no unreleased changes" no-op step gated on neither project having changes
  - Added if: guard to "Create Pull Request" step (critical fix from reviewer): prevents malformed PR when no changes detected
  Reviewer critical issue resolved: Create Pull Request step now gated on at least one project having changes.

================================================================================
TASK-003: Compute dynamic PR metadata and update create-pull-request step
Date: 2026-05-01T00:00:00Z
================================================================================

Summary:
Added "Compute PR metadata" step (id: pr-meta) to .github/workflows/changie.yml after
"Extract version numbers". The step branches on has_aura/has_neo4j detect outputs to
produce single-project or dual-project PR title, branch name, and body. Updated
"Create Pull Request" step to reference steps.pr-meta.outputs.pr_title/pr_branch/pr_body
instead of hardcoded strings. Used printf to build multiline PR body to avoid leading
whitespace from shell script indentation within YAML run: | blocks.

Files Changed:
- .github/workflows/changie.yml (modified — added pr-meta step, updated Create Pull Request step)
- .plans/tasks-per-project-independent-releases.yml (modified — task-003 marked completed)
- .plans/progress-per-project-independent-releases.txt (modified — appended task-003 entry)

Key Decisions:
- Used printf with \n escapes instead of multiline string assignment to avoid YAML indentation
  carrying into the PR body as literal leading whitespace
- Multiline pr_body output uses GitHub Actions heredoc syntax (<<PR_BODY_EOF) for correctness
- Dual-project branch name uses release/neo4j-cli-<version> (unchanged from previous hardcoded format)
- Single-project aura-only uses release/aura-cli-<version>; neo4j-only uses release/neo4j-cli-<version>

Next Task: task-004

================================================================================
TASK-004: Update release.yml trigger and per-project release notes detection
Date: 2026-05-01T00:00:00Z
================================================================================

Summary:
Updated .github/workflows/release.yml with two changes:
1. Added CHANGELOG-aura.md to on.push.paths so aura-cli-only releases trigger GoReleaser.
2. Added 'git diff HEAD~1 --name-only' detection in "Generate release notes" step to set
   INCLUDE_NEO4J and INCLUDE_AURA variables; subsections under ## Changes are now gated
   on these variables instead of checking whether the body string is non-empty.
3. Added fetch-depth: 2 to the actions/checkout step (critical fix) so HEAD~1 exists —
   without this, the shallow clone (default depth 1) causes git diff HEAD~1 to fail,
   keeping both INCLUDE_* flags false and the ## Changes section always empty.

Files Changed:
- .github/workflows/release.yml (modified — checkout fetch-depth: 2, paths trigger, INCLUDE_* detection)
- .plans/tasks-per-project-independent-releases.yml (modified — task-004 marked completed)
- .plans/progress-per-project-independent-releases.txt (modified — appended task-004 entry)

Key Decisions:
- fetch-depth: 2 is the minimum depth needed for HEAD~1 to exist on a triggered push commit
- Detection uses git diff HEAD~1 --name-only piped through grep -q for each changelog file
- ## Versions section and both changie latest steps remain unconditional (always run)
