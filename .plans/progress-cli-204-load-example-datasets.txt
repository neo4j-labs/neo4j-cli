## TASK-001 — 2026-06-04T15:19:12Z
Created neo4j-cli/internal/dataset support package with embedded curated suggestion catalog (List()) sourced from demo.neo4jlabs.com README; 10 entries, each verified to carry a relate.project-install.json manifest.
Files: +neo4j-cli/internal/dataset/catalog.go, +neo4j-cli/internal/dataset/catalog_test.go
Decisions: twitter slug maps to twitter-v2 repo; offshoreleaks -> icij-offshoreleaks; network -> network-management. List returns a defensive copy.

## TASK-002 — 2026-06-04T16:08:00Z
Added manifest resolution to internal/dataset: Resolve(ctx, owner/repo, version) fetches relate.project-install.json (branch main then master), selects compatible dbms[] dump entry (newest-compatible lower bound on overlap), verifies dump path via HEAD, returns Spec{Owner,Repo,Branch,DumpPath,Plugins,MatchedVersionRange}.
Files: +neo4j-cli/internal/dataset/resolve.go, +neo4j-cli/internal/dataset/version.go, +resolve_test.go, +version_test.go
Decisions: no semver-range lib in go.mod, so hand-rolled npm-style comparator-set matcher (>=,<=,>,<,= ANDed by whitespace) over golang.org/x/mod/semver compare; scriptFile entries skipped; rawBaseURL is a var seam so assertRawHost validates against the httptest host in tests.

## TASK-003 — 2026-06-04T17:05:00Z
Added Download(ctx, spec, maxBytes) to internal/dataset: builds the media.githubusercontent.com/media/<owner>/<repo>/<branch>/<dumpPath> URL from the Spec, HTTPS-only + host-allowlist (media./raw.githubusercontent.com, github.com, codeload.github.com), streams to a 0600 temp file (no io.ReadAll), caps at maxBytes (DefaultMaxDumpBytes=2GiB when <=0), returns (path, cleanup, err). Sniffs first 256 bytes for the 'version https://git-lfs' pointer prefix and fails loudly. Models swap.go (assertAllowedHost/assertAllowedHostURL/redactURL/requireHTTPS).
Files: +neo4j-cli/internal/dataset/download.go, +neo4j-cli/internal/dataset/download_test.go
Decisions: mediaBaseURL is a var seam; test helper startMediaServer flips requireHTTPS off + temporarily adds the httptest host to allowedDownloadHosts. Sniff bytes are re-prepended via io.MultiReader so streaming loses nothing; cap detection uses LimitReader(maxBytes+1) + written>maxBytes (exact-cap succeeds). Final-URL host re-checked to catch redirects.

## TASK-004 — 2026-06-04T18:05:00Z
Added neo4j-cli/internal/subcommands/dataset cobra package (one-file-per-leaf): dataset.go parent (discovery-only, Long signposts docker load / desktop dbms load / aura instance load; no load leaf) + list.go leaf rendering curated suggestions (slug/title/description/repo) via commonoutput.PrintBodyMap honoring --format. Mounted dataset.NewCmd(cfg) in app.go between config and history.
Files: +dataset/dataset.go, +dataset/list.go, +dataset/list_test.go, M app/app.go, regenerated skill bundle (M SKILL.md, +references/dataset.md).
Decisions: listRows/MarshalJSON mirror docker/list.go ([] not null on empty); --format inherited from root persistent flag (no per-leaf registration). Tests table-driven over json/table/toon + JSON-shape match + no-load-leaf guard, all via testfs (no OsFs). Ran go generate ./neo4j-cli/internal/skill/... — TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green. make test/fmt-check/lint all clean.

## TASK-005 — 2026-06-04T15:41:10Z
Added docker load leaf (neo4j-cli/internal/subcommands/docker/load.go) + registered in docker.go. Two paths: NEW container (no existing --name) downloads dump, runs one-shot `--rm` loader container (bash entrypoint: cp mounted /import dump → /tmp/load/<db>.dump → neo4j-admin database load <db> --from-path=/tmp/load --overwrite-destination=true) on a named volume neo4j-cli-<name>-data, then starts long-lived server container reusing that volume with NEO4J_PLUGINS=<json-array> from manifest, image neo4j:<version>-enterprise; stores dbms credential; --wait via waitForBoltFn. EXISTING managed container (--force required, else usage error naming the db; unmanaged → unknownContainerError; missing manifest plugin → refused): resolves stored dbms cred, downloads dump, CopyTo into scratch dir as <db>.dump, STOP DATABASE → neo4j-admin database load → deferred START (mirrors PushToAura).
Exported reusable LoadDumpIntoNewContainer(ctx,cfg,client,NewContainerLoad)->NewContainerResult for the aura loader (task-008).
Files: +docker/load.go, +docker/load_test.go, M docker/docker.go, M docker/client.go (+CopyTo iface method + execClient impl; +parseNeo4jPluginsEnv; inspect now parses Config.Env), M docker/labels.go (+Container.Plugins), M docker/helpers_test.go (+CopyTo fake + CopyToCall), regenerated bundle (M references/docker.md).
Decisions: dataset.Resolve/Download wrapped behind resolveDatasetFn/downloadDatasetFn package seams (dataset internals are unexported, can't stub cross-package) — mirrors waitForBoltFn/stopStartFn idiom. Existing-container dump staging uses new docker cp verb (CopyTo) since a running container can't bind-mount a new volume; host path is non-secret so argv is fine. Container.Plugins parsed from NEO4J_PLUGINS env (JSON array) and fails-closed (nil) on missing/unparseable so the plugin gate refuses rather than crashes. --version defaults to 5 (resolves manifest + image tag). Plugin compare is case/order-insensitive. Tests swap clientFactory + dataset seams (no httptest needed at leaf level since dataset is mocked); cover new/existing, force gating, missing-plugin, unmanaged, resolve-err, inspect-operational-err, db override, max-size, wait, no-plugins-empty-array. make test/fmt-check/lint all clean; bundle regenerated (TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green).

## TASK-006 — 2026-06-04T19:05:00Z
Added Client.LoadDump(ctx, dbmsID, db, sourceFilePath, overwrite) to neo4j-cli/internal/desktopclient/upload.go: POST /dbmss/:id/databases/:db/load-dump with body {sourceFilePath, overwrite}, url.PathEscape on both id and db. SYNCHRONOUS (relate runs neo4j-admin database load inline; no task to poll, unlike async UploadDatabase) — dedicated loadDumpTimeout=30m (not the 2m upload kick timeout). DBMS must be stopped; overwrite maps to neo4j-admin --overwrite-destination.
Files: M upload.go (+LoadDump +loadDumpTimeout), M upload_test.go (+TestClient_LoadDump_SendsBodyAndAuthHeaders, +TestClient_LoadDump_PropagatesServerError).
Decisions: tests reuse the existing newAuthedServer/pinClientSeams httptest harness (same pattern as UploadDatabase) rather than the e2e desktop_fixture binary; asserts path/Content-Type/body + 500-error propagation. Did NOT add a load-dump route to test/e2e/desktop_fixture/handlers.go — that belongs with task-007's leaf where the e2e flow is exercised. make test/fmt-check/lint all clean.

## TASK-007 — 2026-06-04T20:30:00Z
Added desktop dbms load leaf (neo4j-cli/internal/subcommands/desktop/dbms/load.go) + registered in dbms.go after create. Flags: --dbms-id/--name (mutually exclusive, exactly one required → usage error), --database (default neo4j), --version, --password, --max-size (DefaultMaxDumpBytes), --force, --wait.
EXISTING (--dbms-id, requires --force, gated in RunE BEFORE client creation so a Desktop-down probe doesn't mask the force usage error): GetDbms → resolve dataset against the DBMS's OWN version → download → if status==started StopDbms+pollUntilStatus(stopped) → LoadDump(db,path,overwrite=true) → InstallPlugin per manifest plugin → StartDbms → renderLoaded.
NEW (--name): if --version omitted, ListDbmsVersions + pickLatestStableEnterprise (reused from create.go) and emit stderr breadcrumb; resolve against that version → download → CreateDbms (CreateDbmsRequest has NO dumpOrBackupPath field, so create-then-LoadDump per task fallback) → LoadDump → InstallPlugin → StartDbms → optional --wait pollUntilStarted → renderLoaded. Password via promptCreatePassword (TTY) so it never lands in argv; non-TTY without --password → usage error.
Files: +dbms/load.go, +dbms/load_test.go, M dbms/dbms.go, regenerated bundle (M references/desktop.md).
Decisions: dataset.Resolve/Download wrapped behind resolveDatasetFn/downloadDatasetFn package seams (+ SetResolveDatasetFnForTest/SetDownloadDatasetFnForTest) — mirrors docker load leaf; dataset internals unexported so cross-package stubbing needs a seam. Tests reuse the create_test.go httptest+newDesktopClientFn harness (salt-load), stub dataset seams with a fixed Spec + recording of resolve (ownerRepo@version) calls; stateful handler toggles GetDbms status started→stopped after stop POST so the stop poll settles. dbmsLoadResult mirrors dbmsInfoResult shape (id/name/version/status/connectionUri); MarshalJSON full DbmsInfo. installManifestPlugins aborts on any plugin failure (dataset unusable without its plugins). renderLoaded falls back to slim {id} envelope on post-load GetDbms failure (load already succeeded → exit 0). Covered: exactly-one, mutually-exclusive, existing-needs-force, new-needs-password-nonTTY, existing stop/load/install/start + overwrite=true + sourceFilePath, existing-stopped-skips-stop, new create/load/install/start + create body, new auto-pick-version, --database override. make test/fmt-check/lint all clean; bundle regenerated (TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green).

## TASK-008 — 2026-06-04T21:30:00Z
Added aura instance load leaf (neo4j-cli/aura/internal/subcommands/instance/load.go) + registered NewLoadCmd in instance.go after deploy. Reuses the create flag set (--version/--region/--memory/--name/--type/--cloud-provider/--vector-optimized/--credential-*) via validateInstanceFlags + buildCreateInstanceBody + createAndStoreInstance + utils.ResolveAndValidateOrgProject; adds --database (default neo4j, system rejected in PreRunE) + --max-size (DefaultMaxDumpBytes). Positional <owner/repo> ExactArgs(1).
Order of ops (matches PRD REQ-F-020/021): resolve manifest → (1) GDS hard-error if plugins contain graph-data-science → (2) dockerAvailableFn probe → resolveInstanceName → create instance → PollInstance(running) → download dump → stageViaDocker → render {load_status}. GDS + docker checks BOTH before any instance creation; stage failure leaves the instance in place (load_status=failed, retry/delete hint to stderr, returns the error). Download failure same leave-in-place treatment.
stageViaDocker: docker.LoadDumpIntoNewContainer (fixed name "neo4j-cli-aura-load"; resolveContainerName auto-suffixes collisions, returns chosen name) with Wait:true + NEO4J_PLUGINS from manifest → docker.PushToAura(chosenName,db,target) → deferred teardown (client.RemoveForce + cfg.Credentials.RemoveDbms). Mirrors deploy.go's deployViaDockerFn seam idiom.
Files: +instance/load.go, +instance/load_test.go, M instance/instance.go, regenerated bundle (M references/aura.md).
Decisions: seams = resolveDatasetFn/downloadDatasetFn (dataset internals unexported cross-package) + dockerAvailableFn (PsAll probe — fails when binary missing OR daemon down; no Ping on dockerClient iface, PsAll is the lightest reuse) + stageViaDockerFn. Tests reuse deploy_test.go's deployOrgID/deployProjectID/deployCreateResponse + a parallel loadHarness; cover GDS-hard-error-before-docker-probe-and-create, docker-absent-before-create, resolve-error-before-create, success create→stage→push order, stage-failure-leaves-instance, system-db reject. make test/fmt-check/lint all clean; bundle regenerated (TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green).

## TASK-009 — 2026-06-04T22:10:00Z
Verification-only task: all four new leaves (dataset list / docker load / desktop dbms load / aura instance load) already carried compliant flush-left Example fields from their respective tasks (each ≥2 invocations, # comment per invocation, --rw on writes, ≥1 --format json on the read/JSON-emitting examples). No edits needed. Ran go generate ./neo4j-cli/internal/skill/... — no bundle diff (already in sync). Gates: TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples pass; make test/fmt-check/lint all clean. Only the task-file/progress updates committed.
