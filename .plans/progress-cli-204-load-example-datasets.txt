## TASK-001 — 2026-06-04T15:19:12Z
Created neo4j-cli/internal/dataset support package with embedded curated suggestion catalog (List()) sourced from demo.neo4jlabs.com README; 10 entries, each verified to carry a relate.project-install.json manifest.
Files: +neo4j-cli/internal/dataset/catalog.go, +neo4j-cli/internal/dataset/catalog_test.go
Decisions: twitter slug maps to twitter-v2 repo; offshoreleaks -> icij-offshoreleaks; network -> network-management. List returns a defensive copy.

## TASK-002 — 2026-06-04T16:08:00Z
Added manifest resolution to internal/dataset: Resolve(ctx, owner/repo, version) fetches relate.project-install.json (branch main then master), selects compatible dbms[] dump entry (newest-compatible lower bound on overlap), verifies dump path via HEAD, returns Spec{Owner,Repo,Branch,DumpPath,Plugins,MatchedVersionRange}.
Files: +neo4j-cli/internal/dataset/resolve.go, +neo4j-cli/internal/dataset/version.go, +resolve_test.go, +version_test.go
Decisions: no semver-range lib in go.mod, so hand-rolled npm-style comparator-set matcher (>=,<=,>,<,= ANDed by whitespace) over golang.org/x/mod/semver compare; scriptFile entries skipped; rawBaseURL is a var seam so assertRawHost validates against the httptest host in tests.

## TASK-003 — 2026-06-04T17:05:00Z
Added Download(ctx, spec, maxBytes) to internal/dataset: builds the media.githubusercontent.com/media/<owner>/<repo>/<branch>/<dumpPath> URL from the Spec, HTTPS-only + host-allowlist (media./raw.githubusercontent.com, github.com, codeload.github.com), streams to a 0600 temp file (no io.ReadAll), caps at maxBytes (DefaultMaxDumpBytes=2GiB when <=0), returns (path, cleanup, err). Sniffs first 256 bytes for the 'version https://git-lfs' pointer prefix and fails loudly. Models swap.go (assertAllowedHost/assertAllowedHostURL/redactURL/requireHTTPS).
Files: +neo4j-cli/internal/dataset/download.go, +neo4j-cli/internal/dataset/download_test.go
Decisions: mediaBaseURL is a var seam; test helper startMediaServer flips requireHTTPS off + temporarily adds the httptest host to allowedDownloadHosts. Sniff bytes are re-prepended via io.MultiReader so streaming loses nothing; cap detection uses LimitReader(maxBytes+1) + written>maxBytes (exact-cap succeeds). Final-URL host re-checked to catch redirects.

## TASK-004 — 2026-06-04T18:05:00Z
Added neo4j-cli/internal/subcommands/dataset cobra package (one-file-per-leaf): dataset.go parent (discovery-only, Long signposts docker load / desktop dbms load / aura instance load; no load leaf) + list.go leaf rendering curated suggestions (slug/title/description/repo) via commonoutput.PrintBodyMap honoring --format. Mounted dataset.NewCmd(cfg) in app.go between config and history.
Files: +dataset/dataset.go, +dataset/list.go, +dataset/list_test.go, M app/app.go, regenerated skill bundle (M SKILL.md, +references/dataset.md).
Decisions: listRows/MarshalJSON mirror docker/list.go ([] not null on empty); --format inherited from root persistent flag (no per-leaf registration). Tests table-driven over json/table/toon + JSON-shape match + no-load-leaf guard, all via testfs (no OsFs). Ran go generate ./neo4j-cli/internal/skill/... — TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green. make test/fmt-check/lint all clean.

## TASK-005 — 2026-06-04T15:41:10Z
Added docker load leaf (neo4j-cli/internal/subcommands/docker/load.go) + registered in docker.go. Two paths: NEW container (no existing --name) downloads dump, runs one-shot `--rm` loader container (bash entrypoint: cp mounted /import dump → /tmp/load/<db>.dump → neo4j-admin database load <db> --from-path=/tmp/load --overwrite-destination=true) on a named volume neo4j-cli-<name>-data, then starts long-lived server container reusing that volume with NEO4J_PLUGINS=<json-array> from manifest, image neo4j:<version>-enterprise; stores dbms credential; --wait via waitForBoltFn. EXISTING managed container (--force required, else usage error naming the db; unmanaged → unknownContainerError; missing manifest plugin → refused): resolves stored dbms cred, downloads dump, CopyTo into scratch dir as <db>.dump, STOP DATABASE → neo4j-admin database load → deferred START (mirrors PushToAura).
Exported reusable LoadDumpIntoNewContainer(ctx,cfg,client,NewContainerLoad)->NewContainerResult for the aura loader (task-008).
Files: +docker/load.go, +docker/load_test.go, M docker/docker.go, M docker/client.go (+CopyTo iface method + execClient impl; +parseNeo4jPluginsEnv; inspect now parses Config.Env), M docker/labels.go (+Container.Plugins), M docker/helpers_test.go (+CopyTo fake + CopyToCall), regenerated bundle (M references/docker.md).
Decisions: dataset.Resolve/Download wrapped behind resolveDatasetFn/downloadDatasetFn package seams (dataset internals are unexported, can't stub cross-package) — mirrors waitForBoltFn/stopStartFn idiom. Existing-container dump staging uses new docker cp verb (CopyTo) since a running container can't bind-mount a new volume; host path is non-secret so argv is fine. Container.Plugins parsed from NEO4J_PLUGINS env (JSON array) and fails-closed (nil) on missing/unparseable so the plugin gate refuses rather than crashes. --version defaults to 5 (resolves manifest + image tag). Plugin compare is case/order-insensitive. Tests swap clientFactory + dataset seams (no httptest needed at leaf level since dataset is mocked); cover new/existing, force gating, missing-plugin, unmanaged, resolve-err, inspect-operational-err, db override, max-size, wait, no-plugins-empty-array. make test/fmt-check/lint all clean; bundle regenerated (TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green).

## TASK-006 — 2026-06-04T19:05:00Z
Added Client.LoadDump(ctx, dbmsID, db, sourceFilePath, overwrite) to neo4j-cli/internal/desktopclient/upload.go: POST /dbmss/:id/databases/:db/load-dump with body {sourceFilePath, overwrite}, url.PathEscape on both id and db. SYNCHRONOUS (relate runs neo4j-admin database load inline; no task to poll, unlike async UploadDatabase) — dedicated loadDumpTimeout=30m (not the 2m upload kick timeout). DBMS must be stopped; overwrite maps to neo4j-admin --overwrite-destination.
Files: M upload.go (+LoadDump +loadDumpTimeout), M upload_test.go (+TestClient_LoadDump_SendsBodyAndAuthHeaders, +TestClient_LoadDump_PropagatesServerError).
Decisions: tests reuse the existing newAuthedServer/pinClientSeams httptest harness (same pattern as UploadDatabase) rather than the e2e desktop_fixture binary; asserts path/Content-Type/body + 500-error propagation. Did NOT add a load-dump route to test/e2e/desktop_fixture/handlers.go — that belongs with task-007's leaf where the e2e flow is exercised. make test/fmt-check/lint all clean.

## TASK-007 — 2026-06-04T20:30:00Z
Added desktop dbms load leaf (neo4j-cli/internal/subcommands/desktop/dbms/load.go) + registered in dbms.go after create. Flags: --dbms-id/--name (mutually exclusive, exactly one required → usage error), --database (default neo4j), --version, --password, --max-size (DefaultMaxDumpBytes), --force, --wait.
EXISTING (--dbms-id, requires --force, gated in RunE BEFORE client creation so a Desktop-down probe doesn't mask the force usage error): GetDbms → resolve dataset against the DBMS's OWN version → download → if status==started StopDbms+pollUntilStatus(stopped) → LoadDump(db,path,overwrite=true) → InstallPlugin per manifest plugin → StartDbms → renderLoaded.
NEW (--name): if --version omitted, ListDbmsVersions + pickLatestStableEnterprise (reused from create.go) and emit stderr breadcrumb; resolve against that version → download → CreateDbms (CreateDbmsRequest has NO dumpOrBackupPath field, so create-then-LoadDump per task fallback) → LoadDump → InstallPlugin → StartDbms → optional --wait pollUntilStarted → renderLoaded. Password via promptCreatePassword (TTY) so it never lands in argv; non-TTY without --password → usage error.
Files: +dbms/load.go, +dbms/load_test.go, M dbms/dbms.go, regenerated bundle (M references/desktop.md).
Decisions: dataset.Resolve/Download wrapped behind resolveDatasetFn/downloadDatasetFn package seams (+ SetResolveDatasetFnForTest/SetDownloadDatasetFnForTest) — mirrors docker load leaf; dataset internals unexported so cross-package stubbing needs a seam. Tests reuse the create_test.go httptest+newDesktopClientFn harness (salt-load), stub dataset seams with a fixed Spec + recording of resolve (ownerRepo@version) calls; stateful handler toggles GetDbms status started→stopped after stop POST so the stop poll settles. dbmsLoadResult mirrors dbmsInfoResult shape (id/name/version/status/connectionUri); MarshalJSON full DbmsInfo. installManifestPlugins aborts on any plugin failure (dataset unusable without its plugins). renderLoaded falls back to slim {id} envelope on post-load GetDbms failure (load already succeeded → exit 0). Covered: exactly-one, mutually-exclusive, existing-needs-force, new-needs-password-nonTTY, existing stop/load/install/start + overwrite=true + sourceFilePath, existing-stopped-skips-stop, new create/load/install/start + create body, new auto-pick-version, --database override. make test/fmt-check/lint all clean; bundle regenerated (TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green).

## TASK-008 — 2026-06-04T21:30:00Z
Added aura instance load leaf (neo4j-cli/aura/internal/subcommands/instance/load.go) + registered NewLoadCmd in instance.go after deploy. Reuses the create flag set (--version/--region/--memory/--name/--type/--cloud-provider/--vector-optimized/--credential-*) via validateInstanceFlags + buildCreateInstanceBody + createAndStoreInstance + utils.ResolveAndValidateOrgProject; adds --database (default neo4j, system rejected in PreRunE) + --max-size (DefaultMaxDumpBytes). Positional <owner/repo> ExactArgs(1).
Order of ops (matches PRD REQ-F-020/021): resolve manifest → (1) GDS hard-error if plugins contain graph-data-science → (2) dockerAvailableFn probe → resolveInstanceName → create instance → PollInstance(running) → download dump → stageViaDocker → render {load_status}. GDS + docker checks BOTH before any instance creation; stage failure leaves the instance in place (load_status=failed, retry/delete hint to stderr, returns the error). Download failure same leave-in-place treatment.
stageViaDocker: docker.LoadDumpIntoNewContainer (fixed name "neo4j-cli-aura-load"; resolveContainerName auto-suffixes collisions, returns chosen name) with Wait:true + NEO4J_PLUGINS from manifest → docker.PushToAura(chosenName,db,target) → deferred teardown (client.RemoveForce + cfg.Credentials.RemoveDbms). Mirrors deploy.go's deployViaDockerFn seam idiom.
Files: +instance/load.go, +instance/load_test.go, M instance/instance.go, regenerated bundle (M references/aura.md).
Decisions: seams = resolveDatasetFn/downloadDatasetFn (dataset internals unexported cross-package) + dockerAvailableFn (PsAll probe — fails when binary missing OR daemon down; no Ping on dockerClient iface, PsAll is the lightest reuse) + stageViaDockerFn. Tests reuse deploy_test.go's deployOrgID/deployProjectID/deployCreateResponse + a parallel loadHarness; cover GDS-hard-error-before-docker-probe-and-create, docker-absent-before-create, resolve-error-before-create, success create→stage→push order, stage-failure-leaves-instance, system-db reject. make test/fmt-check/lint all clean; bundle regenerated (TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green).

## TASK-009 — 2026-06-04T22:10:00Z
Verification-only task: all four new leaves (dataset list / docker load / desktop dbms load / aura instance load) already carried compliant flush-left Example fields from their respective tasks (each ≥2 invocations, # comment per invocation, --rw on writes, ≥1 --format json on the read/JSON-emitting examples). No edits needed. Ran go generate ./neo4j-cli/internal/skill/... — no bundle diff (already in sync). Gates: TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples pass; make test/fmt-check/lint all clean. Only the task-file/progress updates committed.

## TASK-010 — 2026-06-04T18:05:00Z
Added user-facing changelog entry via changie (kind Minor) for the new dataset/load commands: `changie new --projects neo4j-cli --kind Minor --body '...'` → .changes/unreleased/neo4j-cli-Minor-20260604-180134.yaml (project: neo4j-cli / kind: Minor / RFC3339 time). Body: "Add `dataset list` and per-target `load` commands (`docker load`, `desktop dbms load`, `aura instance load`) to load example datasets from neo4j-graph-examples repos."
Files: +.changes/unreleased/neo4j-cli-Minor-20260604-180134.yaml.
Gates: make test (all pass, no fail/panic), make fmt-check (ok), make lint (0 issues), go generate ./neo4j-cli/internal/skill/... (no bundle diff). No source changes — changelog-only.
NOTE: golang-security review portion of this task's acceptance is deferred to the orchestrator (running it separately as a formal final security gate over the risky code: network download, LFS handling, neo4j-admin exec, temp files). This iteration covers changelog + gates only.

## REVIEW — 2026-06-04T19:19:16Z
Added 2 review tasks (task-011..task-012).

## TASK-011 — 2026-06-04T23:10:00Z
Removed exported test-only setters SetResolveDatasetFnForTest/SetDownloadDatasetFnForTest from dbms/load.go (they leaked into the shipped package surface, diverging from docker/instance sibling load leaves + AGENTS.md local-seam idiom). Added in-package dbms/load_seam_test.go (package dbms) with StubDatasetSeams(t, resolve, download) that swaps the unexported resolveDatasetFn/downloadDatasetFn vars directly and restores via t.Cleanup. Because StubDatasetSeams lives in a _test.go it is NOT in the shipped package surface but IS visible to the external dbms_test load_test.go (same dir, same test binary) — so the external HTTP-fixture assertions stay intact: loadHelper.stubDataset now calls dbms.StubDatasetSeams instead of the deleted setters. All other external test coverage unchanged.
Files: M dbms/load.go, +dbms/load_seam_test.go, M dbms/load_test.go.
Gates: desktop dbms pkg tests green (149 passed); make test (no fail/panic), make fmt-check (ok), make lint (0 issues). No cobra-tree change → no skill regen needed.

## TASK-012 — 2026-06-04T23:45:00Z
Scoped the docker new-container loader bind-mount to a dedicated per-load dir. In LoadDumpIntoNewContainer, os.MkdirTemp("","neo4j-cli-load-*") -> copyFile(load.DumpPath, stageDir/<base>) -> bind-mount stageDir at /import:ro (was filepath.Dir(load.DumpPath), i.e. the whole shared os.TempDir() because dataset.Download uses os.CreateTemp("")). defer os.RemoveAll(stageDir) on all return paths.
Files: ~neo4j-cli/internal/subcommands/docker/load.go (+copyFile helper, +os import), ~load_test.go (download seam now os.WriteFile-creates the dump so copyFile has a real source; +os/+filepath imports)
Decisions: COPY not move — leaves the original temp for the leaf's existing cleanup(); Download contract + leaf signatures unchanged; loader script/paths identical (no behavior change). filepath.Join/Base for host paths (Windows-safe); docker -v string keeps ':' separators. Gates green (make test/fmt-check/lint).

## REVIEW — 2026-06-05T07:20:36Z
Added 1 review task (task-013).

## TASK-013 — 2026-06-05T00:00:00Z
Fixed dataset Download: was always fetching media.githubusercontent.com (LFS host), broke regular-blob dumps (neo4j-graph-examples/movies stores data/movies-50.dump as a regular blob — raw=200 real bytes, media=404). Now fetches raw.githubusercontent.com FIRST, sniffs lfsSniffBytes; if body is an LFS pointer it closes raw, fetches media and streams that (media-also-pointer/404 = clear error); otherwise streams raw bytes directly, never touching media. Extracted fetchAndSniff(ctx,url)->(*resp,sniff,err) helper (host-validate pre+post-redirect, status, sniff) reused by both attempts; streaming-to-0600-temp stays in one place after source chosen. All protections preserved (HTTPS-only, allowlist pre+post, cap limit+1, 0600, redactURL, no io.ReadAll).
Files: ~neo4j-cli/internal/dataset/download.go, ~neo4j-cli/internal/dataset/download_test.go, ~AGENTS.md
Decisions: download URL now built from rawBaseURL (raw-first) not mediaBaseURL; rawBaseURL/mediaBaseURL still resolve.go-owned seams. Test helper renamed startMediaServer->startHosts: one mux httptest server, both rawBaseURL+mediaBaseURL point at it, routed by r.URL.Path (/<owner>/... raw, /media/<owner>/... media), host added to allowedDownloadHosts + requireHTTPS off. New cases: RawBlobStreamedDirectly (asserts media never hit), PointerFallsBackToMedia, PointerThenMediaMissing, PointerThenMediaPointer; cap/at-cap/0600/not-found/non-allowlisted/non-https/redact adapted to raw host. Gates green.

## REVIEW — 2026-06-05T07:30:35Z
Added 1 review task (task-014).

## TASK-014 — 2026-06-05T00:00:00Z
Fixed docker new-container loader (LoadDumpIntoNewContainer) so the loaded DB comes online. Root cause: loader ran `--entrypoint bash -c "<script>"`, bypassing docker-entrypoint.sh → neo4j-admin ran as ROOT, writing /data/databases/<db>/* owned by uid 0; the server container drops to neo4j (uid 7474 via su-exec) and can't write the root-owned files → quarantine_marker AccessDeniedException → db 'neo4j' stays offline → --wait times out / DatabaseUnavailable (Bolt + system db up).
Fix: drop the entrypoint override and -c shell script; stage the dump as <stageDir>/<database>.dump (0644) in a 0755 MkdirTemp dir; run the loader via the image DEFAULT entrypoint with the direct command `neo4j-admin database load <db> --from-path=/import --overwrite-destination=true` and mounts `-v <stageDir>:/import:ro -v <volume>:/data` — docker-entrypoint.sh then runs neo4j-admin as the neo4j user so /data ownership matches the server. copyFile now writes 0644 (was 0600) because uid 7474 must read the bind-mounted (:ro) staged dump. Existing-container path (ExecAs as neo4j user inside a running container) untouched. Removed the loaderScript/`/tmp/load` machinery; loaderImportDir="/import" retained.
Files: M docker/load.go, M docker/load_test.go.
Decisions: test asserts no `--entrypoint`/`-c` tokens in loader argv, exact `neo4j-admin database load <db> --from-path=/import --overwrite-destination=true` substring, `:ro` /import mount, and image-arg-precedes-command ordering (slices.Index). Dropping the bash `-c %q` string also clears the security-review shell-quoting concern. make test/fmt-check/lint all clean. Live docker verification deferred to orchestrator (neo4j-graph-examples/movies).

## REVIEW — 2026-06-05T07:38:23Z
Added 1 review task (task-015).

## TASK-015 — 2026-06-05T08:00:00Z
Pass NEO4J_ACCEPT_LICENSE_AGREEMENT=eval to the docker one-shot loader. After task-014 the loader runs via the image DEFAULT entrypoint (docker-entrypoint.sh), which on the enterprise image enforces the license gate: without the env it prints the notice and never execs neo4j-admin → loaded DB ends up online but EMPTY (verified live: docker load movies = 0 nodes). Fix: added `-e NEO4J_ACCEPT_LICENSE_AGREEMENT=eval` to loaderArgs in LoadDumpIntoNewContainer, placed before the image arg (alongside the -v mounts), matching the server container; license acceptance is not a secret so argv -e KEY=VALUE is fine.
Files: M docker/load.go (one line + short WHY clause), M docker/load_test.go (assert license -e flag present + precedes image arg), M AGENTS.md (terse license-gate clause on the loader note).
Decisions: only production change is the single loaderArgs entry; no cobra-tree change → no skill regen. Gates: docker pkg 242 passed; make test (no FAIL/panic), make fmt-check ok, make lint 0 issues. Live e2e (movies = 171 nodes, files owned uid 7474) verified manually per task desc — orchestrator re-verifies.

## REVIEW — 2026-06-05T08:59:03Z
Added 2 review tasks (task-016..task-017): calver/latest dump compatibility + docker latest default.

## TASK-016 — 2026-06-05T12:00:00Z
Resolver now treats calver (>=2025.0) and literal "latest" targets as the 5.x-line continuation: a dump entry whose range lower bound is >=5.0.0 matches. Concrete (non-calver) targets keep exact range matching unchanged.
Files: M neo4j-cli/internal/dataset/resolve.go (Resolve accepts "latest" + isCalver branch; selectEntry takes calver bool; new entryMatches dispatches exact-vs-lower-bound), M version.go (isCalver, rangeLowerBound, stripLeadingZeros/allDigits), M resolve_test.go + version_test.go (table-driven calver/latest/tie-break/no-compat cases).
Decisions: calver months are zero-padded (2026.05.0) but golang.org/x/mod/semver forbids leading zeros in numeric components, so canonicalVersion strips leading zeros from all-digit dot components before Sscanf — non-numeric components untouched. Newest-lower-bound tie-break reused as-is so an explicit ">=2025.0.0" manifest entry wins over the 5.x entry for calver/latest while concrete 5.26 still picks the 5.x entry. "no dump entry compatible" error preserved (pre-5-only manifest + calver/latest). No cobra-tree change → no skill regen. Gates: dataset 71 passed, make test clean, make fmt-check ok, make lint 0 issues.

## TASK-017 — 2026-06-05T13:30:00Z
docker load now defaults to the LATEST neo4j (was pinned "5"). --version flag default "5"→"latest"; help updated to "Accepts 5, 5.26, calver (e.g. 2026.04.0), or latest (default)". Factored the enterprise image tag mapping into shared docker-package helper enterpriseImage(version): "latest"→neo4j:enterprise (Docker Hub has NO neo4j:latest-enterprise), else neo4j:<version>-enterprise. Used by BOTH create.go (replaced inline if/else enterprise branch) and LoadDumpIntoNewContainer (replaced hardcoded "neo4j:"+version+"-enterprise"). LoadDumpIntoNewContainer defaults empty load.Version→"latest" before validateVersion (so direct-helper callers w/o a version get latest too). The (possibly "latest") version is still passed verbatim to resolveDatasetFn — task-016's Resolve selects the 5.x dump for latest/calver. Community branch in create.go untouched; existing-container path untouched; aura loader's own --version default ("5", an Aura version token) untouched.
Files: M docker/create.go (+enterpriseImage helper, create enterprise branch calls it), M docker/load.go (--version default+help, empty→latest, image=enterpriseImage(version), doc comment), M docker/load_test.go (default-version assertions 5→latest, loader/server image neo4j:5-enterprise→neo4j:enterprise, +TestLoad_NewContainer_ExplicitVersionImageMapping table: default/latest→neo4j:enterprise, 5.26→neo4j:5.26-enterprise, 2026.04.0→neo4j:2026.04.0-enterprise + resolve-version forwarding), regenerated bundle (M references/docker.md --version row).
Decisions: helper lives in create.go (next to the original mapping it replaces) — both leaves are same package. WHY-comment on latest→neo4j:enterprise retained (unpublished latest-enterprise tag). create_test.go enterprise-image tests already cover the mapping and stay green (behavior unchanged by extraction). Gates: docker 247 passed, make test clean, make fmt-check ok, make lint 0 issues; go generate produced the expected docker.md flag-help diff only.

## REVIEW — 2026-06-05T09:20:12Z
Added 2 tasks (task-018..019): dataset-loading discoverability (help/skill/bundle) + README.

## TASK-018 — 2026-06-05T15:00:00Z
Made dataset loading discoverable in help + skill bundle. (1) dataset/dataset.go: added Example to the non-runnable parent showing the same movies dataset loaded into docker / desktop dbms / aura instance (# comment per invocation, --rw on each, accurate flags: --name, --password <pw>, --type free-db). (2) dataset/list.go: appended a `docker load` example after the existing list+`--format json` examples (read-cmd gate retained). (3) description.txt: folded in dataset discovery+loading; condensed existing wording (run a local Docker container, drop "embedded"/"Neo4j Browser"→"Browser", incl. abbreviations) to 1019/1024 bytes (incl. trailing newline). (4) additions.md: added a Tips bullet (addressed by owner/repo, suggestions not a constraint, three per-target verbs, defaults to latest Neo4j, --database default neo4j, --rw under agents).
Files: M dataset/dataset.go, M dataset/list.go, +dataset/dataset_test.go, M skill/description.txt, M skill/additions.md, regenerated bundle (M SKILL.md, M references/dataset.md).
Decisions: regression test TestParentExample_ReferencesAllThreeLoadVerbs (package dataset) builds NewCmd(cfg) via testfs and asserts parent Example contains all three load verbs. go generate idempotent (md5-verified). Gates: make test PASS, fmt-check ok, lint 0 issues; TestGenerator_RoundTrip + TestAllLeafCommands_HaveExamples green. No AGENTS.md change (description.txt byte-budget + parent-Example-renders-in-help already documented).

## TASK-019 — 2026-06-05T16:30:00Z
Added "## Example datasets" README section between Local Neo4j and Querying Neo4j (datasets populate both local + Aura, so it sits between). Intro: discovery via `dataset list` (+ that suggestions aren't a constraint, any repo w/ relate.project-install.json works, `dataset --help` signposts); addressing by `<owner>/<repo>`; --database default neo4j (must already exist on an EXISTING target, load overwrites not creates); latest-Neo4j default + --version accepts 5/5.26/calver/latest. Three ### subsections w/ comment-prefixed ```bash blocks matching the surrounding README style: docker load (new auto-created via --name; existing overwrite via --force --wait), desktop dbms load (exactly-one of --name/--dbms-id mutually exclusive, existing needs --force, TTY password preferred), aura instance load (always NEW instance, ephemeral-docker staging note, free-db simplest + paid --cloud-provider/--region/--memory, GDS rejected up front).
Files: M README.md.
Decisions: flags cross-checked against the actual leaves (docker/load.go, desktop/dbms/load.go, aura/instance/load.go, dataset/list.go) — --wait on docker new only, --force on both existing paths, aura free-db vs professional-db. No TOC in README (nothing to update). README-only → no skill regen, no AGENTS.md change. Gates: make fmt-check ok, make test no FAIL/panic, make lint 0 issues.
