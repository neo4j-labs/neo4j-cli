================================================================================
TASK-001: Rename credential storage layer: database → dbms
Date: 2026-05-06T09:15:00Z
================================================================================

Summary:
Renamed all types and files in the credential storage layer from `database` to `dbms`.
Deleted database.go and database_test.go; created dbms.go and dbms_test.go with all
types renamed. Updated credentials.go struct fields and JSON tags from `database` to `dbms`.
All 10 tests pass for the `common/clicfg/credentials` package.

Files Changed:
- common/clicfg/credentials/database.go (deleted)
- common/clicfg/credentials/database_test.go (deleted)
- common/clicfg/credentials/dbms.go (created - renamed types: DbmsCredentials, DbmsCredential, PrintableDbmsCredentials)
- common/clicfg/credentials/credentials.go (modified - Database→Dbms field, json tag "database"→"dbms")
- common/clicfg/credentials/dbms_test.go (created - all test fixtures updated to use "dbms" JSON key)

Key Decisions:
- Ran package-scoped test `go test ./common/clicfg/credentials/...` rather than `make test ./...`
  because other packages (credential/database/, query/connect.go) still reference cfg.Credentials.Database
  and will be updated in tasks 002 and 003.
- JSON tag changed from "database" to "dbms" — existing credentials.json files with "database" key
  will not migrate automatically; migration is a separate concern.
- gofmt required alignment fix in DbmsCredentials struct (extra space in slice field type).

Next Task: task-003

================================================================================
TASK-002: Rename credential command package: database → dbms
Date: 2026-05-06T10:00:00Z
================================================================================

Summary:
Renamed the credential command package from `database` to `dbms`. Deleted all
files under `credential/database/` (add.go, database.go, list.go, remove.go,
use.go, and their test counterparts). Created `credential/dbms/` with all files
updated to use `package dbms`, `cfg.Credentials.Dbms`, and "dbms" in Short
descriptions. Updated `credential.go` to import `dbms` and call `dbms.NewCmd`.
All credential package tests pass.

Files Changed:
- neo4j-cli/internal/subcommands/credential/database/ (deleted - all 9 files removed)
- neo4j-cli/internal/subcommands/credential/dbms/ (created - add.go, dbms.go, list.go, remove.go, use.go + 4 test files)
- neo4j-cli/internal/subcommands/credential/credential.go (modified - import database→dbms, database.NewCmd→dbms.NewCmd)

Key Decisions:
- Only ran package-scoped tests for `credential/...` — query package still fails to compile because
  connect.go still references cfg.Credentials.Database (will be fixed in task-003).
- Test helpers in add_test.go use "dbms" JSON key to match the updated credentials.go JSON tag.
- Package-level test helper is in add_test.go (dbmsTestHelper), list/remove/use test files use it.

Next Task: task-003
