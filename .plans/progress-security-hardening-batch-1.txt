## task-001 — 2026-05-08T14:24:04Z
Added clievents.RedactArgs single source of truth for secret-flag scrubbing across --flag value, --flag=value, and -flag value shapes; covers password, client-secret, api-key, instance-password.
Files: +common/clievents/redact.go, +common/clievents/redact_test.go

## task-002 — 2026-05-08T14:35:00Z
Added urlcheck.ValidateRemoteURL SSRF allowlist (https any, http loopback only; rejects private/link-local/multicast/metadata-IP/malformed). Stdlib-only (net, net/url, fmt, strings) so any package can import.
Files: +common/clicfg/urlcheck/urlcheck.go, +common/clicfg/urlcheck/urlcheck_test.go
Decisions: metadata-IP guard is explicit string match (catches trailing-dot variant); DNS rebinding documented out of scope.

## task-003 — 2026-05-08T14:48:00Z
Added shared dotenv.Find helper walking up from startDir, stopping at first .git ancestor or $HOME boundary; returns (path, found, walkedAboveCWD). No clicfg/cobra dependency.
Files: +common/clicfg/dotenv/dotenv.go, +common/clicfg/dotenv/dotenv_test.go
Decisions: homeDirFn is package-level overridable (cleaner than t.Setenv("HOME") and platform-portable); .git boundary inclusive (repo-root .env still loaded), $HOME boundary inclusive.

## task-004 — 2026-05-08T15:02:00Z
Wired RedactArgs into aura/skill/default telemetry branches in clievents.Emit; query branch left untouched (already command-name only). Added table-driven Mixpanel-payload test asserting *** present, flag name preserved, secret value never leaked across AURA/SKILL/COMMAND suffixes including --flag=value form.
Files: ~common/clievents/clievents.go, ~common/clievents/clievents_test.go

## task-005 — 2026-05-08T15:18:00Z
Wrapped every os.Args[1:] interpolation in panic/error templates with clievents.RedactArgs: 10 sites in api/response.go (handleResponseError + formatAuthorizationError), 1 in allowedorigin/utils.go, 1 each in neo4j-cli/main.go and aura/cmd/main.go panic-recover. Extracted recoverPanic helpers in both binary mains so the format is unit-testable. Added in-package response_test.go (415/502/307/400-malformed/default-status) and main_test.go in both mains; all assert *** present and secret value absent.
Files: ~neo4j-cli/main.go, +neo4j-cli/main_test.go, ~neo4j-cli/aura/cmd/main.go, +neo4j-cli/aura/cmd/main_test.go, ~neo4j-cli/aura/internal/api/response.go, +neo4j-cli/aura/internal/api/response_test.go, ~neo4j-cli/aura/internal/subcommands/dataapi/graphql/corspolicy/allowedorigin/utils.go
Decisions: response_test uses package api (in-package) to access private handleResponseError; nil credential/cfg is safe on the panic paths exercised. recoverPanic helper accepts io.Writer so tests don't have to redirect os.Stdout.

## task-006 — 2026-05-08T15:35:00Z
Atomic credentials.json write: WriteFile now writes <path>.tmp via OpenFile O_CREATE|O_WRONLY|O_TRUNC mode 0600, Sync, Close, Rename; cleanup on any failure. createFile collapses Create→Chmod chain to a single OpenFile (umask race window closed). Both fileutils MkdirAll and clicfg.go config-dir MkdirAll now use 0o700.
Files: ~common/clicfg/fileutils/fileutils.go, +common/clicfg/fileutils/fileutils_test.go, +common/clicfg/fileutils/umask_unix_test.go, +common/clicfg/fileutils/umask_windows_test.go, ~common/clicfg/clicfg.go
Decisions: rename-failure test uses an Fs wrapper (renameFailFs) injecting a fixed error rather than reflecting on os internals; OS-fs mode assertions guarded by syscall.Umask(0) on POSIX with a Windows-no-op shim so tests stay cross-platform; Sync errors that wrap os.ErrInvalid are tolerated (some afero backends).

## task-007 — 2026-05-08T15:55:00Z
load() returns error instead of panicking on Unmarshal failure; on corrupt file backs up to <path>.corrupt-<unix-ts>, resets in-memory + on-disk state to empty creds, returns clierr.NewFatalError naming backup. NewCredentials wraps load() and panics with that error so existing main() recover-and-print flow stays intact (no signature change to 13 call sites).
Files: ~common/clicfg/credentials/credentials.go, +common/clicfg/credentials/credentials_test.go, +common/clicfg/credentials/export_test.go
Decisions: package-level nowUnix var + SetNowUnixForTest helper for deterministic backup-path assertions (cleaner than freezing time globally); panic-with-error preserved over signature change to keep blast radius local.

## task-008 — 2026-05-08T16:10:00Z
Set 60s Timeout on both Aura http.Client constructions (api.go MakeRequest, token.go getToken) via shared httpClientTimeout package var; embed clients untouched per ctx-owned contract. Added timeout-fires test using stalling httptest.Server + recover (MakeRequest panics on client.Do errors).
Files: ~neo4j-cli/aura/internal/api/api.go, ~neo4j-cli/aura/internal/api/token.go, ~neo4j-cli/aura/internal/api/api_test.go, +neo4j-cli/aura/internal/api/export_test.go
Decisions: httpClientTimeout as package-level var + SetHTTPClientTimeoutForTest seam so test runs in 50ms instead of 60s; handler guarded with 5s fallback per AGENTS.md ctx-propagation-timing rule.

## task-009 — 2026-05-08T16:25:00Z
Wired urlcheck.ValidateRemoteURL into 5 request sites: aura MakeRequest base-url, getToken auth-url, and the three embed providers (openai, ollama, huggingface) BaseURL pre-flight. Aura sites also propagate the previously-discarded ParseRequestURI error as clierr.NewUsageError. Tests assert SSRF-blocked URLs (metadata IP, RFC1918, link-local, cleartext-non-loopback) error before any HTTP traffic.
Files: ~neo4j-cli/aura/internal/api/api.go, ~neo4j-cli/aura/internal/api/api_test.go, ~neo4j-cli/aura/internal/api/token.go, ~neo4j-cli/query/embed/openai.go, ~neo4j-cli/query/embed/openai_test.go, ~neo4j-cli/query/embed/ollama.go, ~neo4j-cli/query/embed/ollama_test.go, ~neo4j-cli/query/embed/huggingface.go, ~neo4j-cli/query/embed/huggingface_test.go
Decisions: aura sites use clierr.NewUsageError (matches existing in-package style); embed sites use fmt.Errorf %w to keep wrapping consistent with surrounding error returns; APIKey check still precedes URL check so missing-key error path stays unchanged.

## task-010 — 2026-05-08T16:45:00Z
Replaced both findDotenv duplicates (connect.go, embed/embed.go) with shared dotenv.Find helper; loadEnvFile and loadDotenv now take an io.Writer and emit `info: loading .env from <path>` to stderr when the discovered .env lives strictly above startDir/cwd. Added .git-boundary, $HOME-boundary, and overlay-announcement tests at both call sites.
Files: ~common/clicfg/dotenv/dotenv.go, ~neo4j-cli/query/connect.go, ~neo4j-cli/query/connect_test.go, ~neo4j-cli/query/embed/embed.go, ~neo4j-cli/query/embed/embed_test.go
Decisions: exported SetHomeDirFnForTest from dotenv pkg for cross-package hermetic tests (cleaner than t.Setenv("HOME") which is non-portable to Windows USERPROFILE); embed-side test uses os.MkdirAll (std lib) for the chdir target to honor AGENTS.md "no afero.NewOsFs in query tests" rule.

## task-011 — 2026-05-08T17:05:00Z
normalizeURI now returns a 4th value `warning string`: non-empty when resolved scheme is cleartext bolt-family (bolt:// or neo4j://) AND host is not loopback. Warning uses (*url.URL).Redacted() so userinfo password is masked. Both connect.go call sites print via cmd.PrintErrln. Loopback (localhost / 127.0.0.1 / ::1) and encrypted (+s/+ssc) variants stay silent. Existing http→neo4j rewrite to non-loopback now ALSO emits the cleartext warning post-rewrite.
Files: ~neo4j-cli/query/uri.go, ~neo4j-cli/query/uri_test.go, ~neo4j-cli/query/connect.go, ~neo4j-cli/query/run_test.go
Decisions: cleartextWarning + isLoopbackHost as private helpers in uri.go (kept small, no new pkg); warning fires post-rewrite too so http://prod.example also warns about the resulting cleartext neo4j://.
