feature: cli-116-schema-discoverability
prd: ./prd-cli-116-schema-discoverability.md
created_at: 2026-05-13T00:00:00Z
updated_at: 2026-05-13T00:00:00Z

tasks:
  - id: task-001
    title: "Strengthen `description.txt` trigger phrasing for `:schema`"
    description: |
      Edit `neo4j-cli/internal/skill/description.txt` to name `:schema` (or
      `neo4j-cli query :schema`) explicitly as the way to inspect the database
      schema / data model. Today it says "introspect a schema" once in clause 3 —
      weak match for "look into the schema of neo4j" / "generate a data model
      from the schema". Stay ≤1024 chars, single paragraph, third-person.
      See PRD REQ-F-001.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "`description.txt` contains the literal substring `:schema` or `neo4j-cli query :schema`"
      - "File is a single paragraph, third-person voice, ≤1024 chars"
      - "Phrase covers the agent-side triggers: `inspect/view/introspect the schema`, `data model from the schema`"
      - "No mention of `-f` shorthand anywhere in the new text (use long-form `--format` if a flag is shown)"
    completed_at: null

  - id: task-002
    title: "Promote schema-first directive to first bullet in `additions.md`"
    description: |
      Edit `neo4j-cli/internal/skill/additions.md` so the FIRST bullet of the
      Tips & Gotchas list is the literal schema-first directive showing the
      command. Replace the current first bullet (the bare pointer to
      `query-additions.md`) with: **Before generating ANY Cypher: run
      `neo4j-cli query :schema --format toon` first to discover the real labels,
      relationship types, and properties. Do not guess the schema.** Read
      [query-additions.md](query-additions.md) for the full schema-first
      workflow, parameters, embeddings, and Cypher 25 vs 5.
      Use long-form `--format` (not `-f`); leave other bullets and the existing
      `-f` references in this file alone (out of scope per PR #111).
      See PRD REQ-F-002, REQ-NF-001.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "First bullet contains the literal command `neo4j-cli query :schema --format toon`"
      - "First bullet retains a markdown link to `query-additions.md`"
      - "First bullet uses bold (`**…**`) on the directive sentence"
      - "Long-form `--format toon` (not `-f toon`) in the new bullet"
      - "No other bullets are reordered or modified"
    completed_at: null

  - id: task-003
    title: "Edit `query.go` Short, Long, and Example to lead with `:schema`"
    description: |
      Edit `neo4j-cli/query/query.go` (parent `query` cobra command, lines ~19–50):
      (a) Replace Short with `"Run Cypher, inspect the database schema (:schema), and embed text against a Neo4j database via the Bolt protocol"` (≤120 chars).
      (b) Prepend Long with the sentence `"Use the :schema subcommand to introspect labels, relationship types, and properties before writing Cypher — never guess the schema. "` (note trailing space). Preserve the rest verbatim.
      (c) Insert a new FIRST example into the Example field:
      `# Introspect the schema before writing Cypher (always do this first)`
      `neo4j-cli query :schema --format toon`
      followed by the existing 5 examples. Keep the block flush-left so the
      renderer at `common/skill/render/render.go:235` does not produce a ragged
      code block. Verify with `go run ./neo4j-cli query --help`.
      See PRD REQ-F-003, REQ-F-004, REQ-F-005.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "`query.go` Short equals: `\"Run Cypher, inspect the database schema (:schema), and embed text against a Neo4j database via the Bolt protocol\"`"
      - "Short length ≤120 chars"
      - "Long is prefixed with the new `:schema` directive sentence; remaining concatenated string fragments unchanged"
      - "Example field has the `# Introspect the schema before writing Cypher (always do this first)` comment as the first line and `neo4j-cli query :schema --format toon` as the second line"
      - "Existing 5 examples follow in their original order"
      - "Example block is flush-left (no leading whitespace on the first line)"
      - "`go run ./neo4j-cli query --help` shows the new Long lede and the new first example"
    completed_at: null

  - id: task-004
    title: "Edit `schema.go` Long and Example for stronger `:schema` framing"
    description: |
      Edit `neo4j-cli/query/schema.go` (`:schema` leaf, lines ~71–91):
      (a) Prepend Long with the sentence `"Run this BEFORE generating Cypher to discover the database's actual labels, relationship types, and properties — never guess. "` (note trailing space). Preserve the rest verbatim.
      (b) Replace the first Example with:
      `# Discover labels, rel types, and properties (run this before writing Cypher)`
      `neo4j-cli query :schema --format toon`
      Existing JSON and `jq` examples follow in their current order. Verify with
      `go run ./neo4j-cli query :schema --help`. See PRD REQ-F-006, REQ-F-007.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "`schema.go` Long starts with `\"Run this BEFORE generating Cypher to discover the database's actual labels, relationship types, and properties — never guess. \"`"
      - "Remaining Long content (\"Introspect the connected database. Runs a sequence…\") is unchanged"
      - "Example field first non-blank line is `# Discover labels, rel types, and properties (run this before writing Cypher)`"
      - "Example field second line is `neo4j-cli query :schema --format toon`"
      - "Existing `--format json` and `jq` examples are preserved in original order"
      - "`go run ./neo4j-cli query :schema --help` shows the new Long lede and the new first example"
    completed_at: null

  - id: task-005
    title: "Regenerate the neo4j-cli skill bundle"
    description: |
      Run `go generate ./neo4j-cli/internal/skill/...` to refresh the committed
      bundle. Without this step `TestGenerator_RoundTrip` fails with
      `references/query.md differs` or `SKILL.md differs`. Inspect the diff and
      confirm only `bundle/SKILL.md` and `bundle/references/query.md` changed —
      the aura skill bundle (`neo4j-cli/aura/internal/skill/`) MUST NOT be
      touched (query is not under aura). See PRD REQ-F-008 and AGENTS.md
      "Cobra Help / Skill Bundle Rendering Notes".
    status: pending
    dependencies:
      - task-001
      - task-002
      - task-003
      - task-004
    acceptance_criteria:
      - "`go generate ./neo4j-cli/internal/skill/...` exits 0"
      - "`bundle/SKILL.md` frontmatter `description:` contains `:schema` (or `neo4j-cli query :schema`)"
      - "`bundle/SKILL.md` Subcommands table row for `query` mentions schema introspection / `:schema`"
      - "`bundle/SKILL.md` first Tips & Gotchas bullet contains the literal `neo4j-cli query :schema --format toon`"
      - "`bundle/references/query.md` Long lede mentions `:schema`"
      - "`bundle/references/query.md` first Example is `neo4j-cli query :schema --format toon`"
      - "`git diff --stat neo4j-cli/aura/internal/skill/bundle/` shows zero changes"
    completed_at: null

  - id: task-006
    title: "Add `Minor`-kind changelog entry for CLI-116"
    description: |
      Add a user-facing changelog entry. Prefer `changie new --projects neo4j-cli
      --kind Minor --body "skill bundle: surface query :schema as the
      schema-first workflow on SKILL.md, query --help, and :schema --help
      (CLI-116)"`. Fall back to hand-authoring the YAML at
      `.changes/unreleased/neo4j-cli-Minor-<YYYYMMDD>-<HHMMSS>.yaml` per
      `.agents/build.md` if changie isn't installed locally.
      See PRD REQ-F-009.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "Exactly one new file under `.changes/unreleased/neo4j-cli-Minor-*.yaml`"
      - "YAML has `project: neo4j-cli`, `kind: Minor`"
      - "`body:` references `CLI-116` and mentions `:schema` / schema-first"
      - "YAML loads without error"
    completed_at: null

  - id: task-007
    title: "Run final gates: `make test`, `make fmt-check`, `make lint`"
    description: |
      Run all three CI gates locally as the final check per AGENTS.md "Feedback
      Instructions". `make test` exercises `TestGenerator_RoundTrip` (bundle
      drift) and `TestAllLeafCommands_HaveExamples` (Example field invariants);
      `make fmt-check` mirrors CI's gofmt linter; `make lint` runs golangci-lint
      v2. All three must pass. Also do a final `--help` sweep to confirm the
      user-visible outcome on `query --help` and `query :schema --help`.
      See PRD REQ-NF-002, REQ-NF-003, REQ-NF-004.
    status: pending
    dependencies:
      - task-005
      - task-006
    acceptance_criteria:
      - "`make test` exits 0 (TestGenerator_RoundTrip and TestAllLeafCommands_HaveExamples pass)"
      - "`make fmt-check` exits 0"
      - "`make lint` exits 0"
      - "`go run ./neo4j-cli query --help` Long lede mentions `:schema`; first example is `:schema --format toon`"
      - "`go run ./neo4j-cli query :schema --help` Long lede starts with `Run this BEFORE generating Cypher`; first example is `:schema --format toon`"
    completed_at: null
