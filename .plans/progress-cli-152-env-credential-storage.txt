## TASK-001 — 2026-06-01T13:05:00Z
Added StorageModeEnv constant, getenv test seam (var getenv = os.Getenv), env-mode no-op branch in save(), and SetGetenvForTest helper.
Files: ~common/clicfg/credentials/credentials.go, ~common/clicfg/credentials/export_test.go

## TASK-002 — 2026-06-01T13:25:00Z
Added env.go with loadFromEnv + per-type synthesis helpers reading via the getenv seam, mutating in-memory structs directly; Aura needs both id+secret (warns on one), Dbms on NEO4J_PASSWORD, Embed on NEO4J_EMBED_PROVIDER; overlays onto existing "env" cred via findOrAppend*.
Files: +common/clicfg/credentials/env.go, ~common/clicfg/credentials/export_test.go
Decisions: exposed loadFromEnv via export_test.go LoadFromEnv so the `unused` linter passes while wiring is deferred to task-003 (and the seam is reused by task-008 tests).

## TASK-003 — 2026-06-01T13:00:49Z
Wired env mode into SetStorageMode: switched the if to a switch with a StorageModeEnv case calling loadFromEnv(warnW); keyring case unchanged, insecure stays implicit no-op. NewConfig call site needed no change. Removed the now-redundant LoadFromEnv test wrapper from export_test.go (loadFromEnv is reachable from production, so the `unused` linter is satisfied) along with its io import. No //nolint:unused existed on env.go to remove.
Files: ~common/clicfg/credentials/credentials.go, ~common/clicfg/credentials/export_test.go

## TASK-004 — 2026-06-01T13:40:00Z
GlobalConfig.Set credential-storage validation now also accepts StorageModeEnv; invalid-value error message lists all three modes (keyring, insecure, env) via the constants. CredentialStorage() getter unchanged (still defaults to insecure). Updated the stale expected-message assertion in config set_test.go.
Files: ~common/clicfg/clicfg.go, ~neo4j-cli/internal/subcommands/config/set_test.go

## TASK-005 — 2026-06-01T14:05:00Z
config set credential-storage migration gate now blocks env->keyring/insecure: returns a clierr.NewUsageError (SilenceUsage) before any migration or cfg.Global.Set, so the persisted value is unchanged. Switching TO env (value=="env") matches no branch and falls through to persist with no migration. Existing keyring/insecure transitions unaffected (new block only fires when currentMode==env).
Files: ~neo4j-cli/internal/subcommands/config/set.go

## TASK-006 — 2026-06-01T14:30:00Z
Reserved "env" credential name across all three types: guard at top of AuraCredentials.Add / DbmsCredentials.Add / EmbedCredentials.Add returns clierr.NewUsageError via shared errReservedEnvName helper (mirrors the desktop reservation in dbms.go). Synthesis untouched — findOrAppend* mutate structs directly, never via Add, so loadFromEnv still synthesizes the "env" cred.
Files: ~common/clicfg/credentials/{aura.go,dbms.go,embed.go,env.go}
Decisions: shared errReservedEnvName helper in env.go (where envCredentialName lives) instead of inlining the message thrice.

## TASK-007 — 2026-06-01T15:10:00Z
Added Credentials.WarnIfEnvMode(w io.Writer) method (no-op unless storageMode==env) and called it at top of each write-leaf RunE: aura-client add; dbms add/use/remove/set-embed; embed add/use/remove. Warning goes to cmd.ErrOrStderr(); commands proceed (warn-but-allow). Not wired into save() or any automatic path, so token refresh / instance create auto-store stay silent.
Files: ~common/clicfg/credentials/credentials.go, ~neo4j-cli/internal/subcommands/credential/credential.go, ~neo4j-cli/internal/subcommands/credential/dbms/{add,use,remove,set_embed}.go, ~neo4j-cli/internal/subcommands/credential/embed/{add,use,remove}.go
Decisions: helper lives as a method on *Credentials (common pkg) so both the credential parent pkg and its dbms/embed subpkgs can reach it without an import cycle.

## TASK-008 — 2026-06-01T16:00:00Z
Added env-mode test coverage. env_test.go (table-driven, getenv seam) covers Aura/Dbms/Embed synthesis, one-of-pair Aura warning, embed provider gating (api-key alone no synthesis), overlay onto existing "env" cred, no-op save (MemMapFs unchanged + zero mock-keyring Set), Aura token refresh staying in-memory, reserved-name Add rejection (all 3 types), and WarnIfEnvMode. Extended config set_test.go (env value persists, env->keyring/insecure blocked + config unchanged, switch-to-env no migration). Added command-level reserved-name 'env' cases to credential aura-client/dbms/embed add tests, and an env-mode warn-but-allow test for aura-client add. Added app test asserting env never auto-selected as first-run default. Added setCalls counter to mockKeyringProvider.
Files: +common/clicfg/credentials/env_test.go, ~common/clicfg/credentials/keyring_test.go, ~neo4j-cli/internal/subcommands/config/set_test.go, ~neo4j-cli/internal/subcommands/credential/credential_test.go, ~neo4j-cli/internal/subcommands/credential/dbms/add_test.go, ~neo4j-cli/internal/subcommands/credential/embed/add_test.go, ~neo4j-cli/app/credential_storage_default_test.go

## TASK-009 — 2026-06-01T13:20:32Z
Docs + changelog + final gates. Added "Storage modes" subsection to README Credentials section: 3-mode table (keyring/insecure-default/env), env-mode CI/CD usage with all env-var names (Aura id+secret, dbms URI/USERNAME/PASSWORD/DATABASE, embed PROVIDER/MODEL/BASE_URL/DIMENSIONS/API_KEY), reserved "env" name, ephemeral/no-persist semantics, warn-but-allow writes, blocked switch-away. go generate left bundle clean (no help-text drift from tasks 1-8). Added Minor changie entry. Gates: make test/lint/license-check pass, gofmt clean, no bundle drift.
Files: ~README.md, +.changes/unreleased/neo4j-cli-Minor-20260601-151936.yaml

## REVIEW — 2026-06-03T00:00:00Z
Added 4 review tasks (task-010..task-013).

## TASK-010 — 2026-06-03T00:00:00Z
Added env-mode-only ephemeral Aura JWT cache module: tokenIdentity (full sha256 of clientID\x00secret\x00authURL), tokenCachePath (os.TempDir + identity[:16]), loadTokenCache (best-effort afero Exists/ReadFile, reuses HasValidAccessToken, no panic), saveTokenCache (atomic 0600 via fileutils.WriteFileErr, stores full identity hash, never the raw secret). Module only — getToken wiring is task-011.
Files: +neo4j-cli/aura/internal/api/token_cache.go, ~neo4j-cli/aura/internal/api/export_test.go
Decisions: exposed loadTokenCache/saveTokenCache via export_test.go (LoadTokenCache/SaveTokenCache) so the unused linter passes until task-011 wiring lands.
