feature: cli-194-virtual-graph-limitations
prd: ./prd-cli-194-virtual-graph-limitations.md
created_at: 2026-05-28T11:49:24Z
updated_at: 2026-05-28T11:49:24Z

tasks:
  - id: task-001
    title: "Add OPTIONAL MATCH and vector/full-text bullets to query-additions.md"
    description: |
      Append two new bullets to the "Forbidden on Virtual Graphs (today)" list
      in neo4j-cli/internal/skill/query-additions.md, after the existing final
      "apoc.*" bullet and before the "Stick to MATCH … WHERE … RETURN…" paragraph.
      Bullets must be: "- `OPTIONAL MATCH`" and "- Vector search and full-text
      search (not supported in any form)". Preserve existing ordering, indentation,
      and style of the surrounding bullets. Do NOT regenerate the bundle in this
      task — the round-trip gate is verified in a later task.
    status: pending
    dependencies: []
    acceptance_criteria:
      - "neo4j-cli/internal/skill/query-additions.md has two new bullets appended to the Forbidden list"
      - "First new bullet is exactly: - `OPTIONAL MATCH`"
      - "Second new bullet is exactly: - Vector search and full-text search (not supported in any form)"
      - "No other lines in the file are changed"
    completed_at: null

  - id: task-002
    title: "Regenerate skill bundle so committed copy matches source"
    description: |
      Run `go generate ./neo4j-cli/internal/skill/...` to refresh
      neo4j-cli/internal/skill/bundle/query-additions.md from the updated source.
      The generator (neo4j-cli/internal/skill/gen/main.go) copies the source
      verbatim into the bundle. Stage both source and bundle together.
    status: pending
    dependencies:
      - task-001
    acceptance_criteria:
      - "neo4j-cli/internal/skill/bundle/query-additions.md is byte-identical to the source query-additions.md"
      - "`rg \"OPTIONAL MATCH\" neo4j-cli/internal/skill/` returns hits in both source and bundle"
      - "`rg \"full-text search\" neo4j-cli/internal/skill/` returns hits in both source and bundle"
      - "No other generated files have drifted (git diff scoped to the two query-additions.md paths)"
    completed_at: null

  - id: task-003
    title: "Add changie Patch entry for the prompt update"
    description: |
      Create a Patch changelog entry under project neo4j-cli describing the
      prompt change. Prefer `changie new --projects neo4j-cli --kind Patch
      --body "Add OPTIONAL MATCH, vector and full-text search to the query
      skill's Virtual Graph forbidden-construct list"`. If changie is not on
      PATH, hand-author the YAML under .changes/unreleased/ with filename
      `neo4j-cli-Patch-<YYYYMMDD>-<HHMMSS>.yaml` and fields project / kind /
      body / time per the AGENTS.md fallback.
    status: pending
    dependencies:
      - task-001
    acceptance_criteria:
      - "A new YAML file exists under .changes/unreleased/ tagged project: neo4j-cli and kind: Patch"
      - "Body references OPTIONAL MATCH and vector / full-text search additions to the Virtual Graph forbidden list"
      - "File follows the existing naming convention used by other entries in .changes/unreleased/"
    completed_at: null

  - id: task-004
    title: "Run final gates: make test, make fmt-check, make lint, make generate-check"
    description: |
      Run the canonical CI-equivalent gates locally and ensure all pass.
      `make generate-check` proves source/bundle are in sync.
      `make test` runs TestGenerator_RoundTrip (the bundle-drift gate) along
      with the rest of the suite. `make fmt-check` and `make lint` are the
      formatting and lint gates per AGENTS.md.
    status: pending
    dependencies:
      - task-002
      - task-003
    acceptance_criteria:
      - "`make generate-check` exits 0"
      - "`make test` exits 0 (TestGenerator_RoundTrip in particular passes)"
      - "`make fmt-check` exits 0"
      - "`make lint` exits 0"
    completed_at: null
